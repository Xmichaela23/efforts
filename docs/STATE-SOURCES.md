# STATE-SOURCES — every number on the State screen, and where it comes from

**Rule (2026-09-04, Michael):** no number, threshold, window or formula reaches the State screen
without a source beside it in the code — or an explicit `OURS` marker with the reason and a row here.
Say "ours" the first time it comes up. A number borrowed from one context (a plan rule, a reporting
threshold) is not licensed as a filter in another.

Legend: **FIELD** = an outside source the code cites · **OURS** = no outside source; the reason is
stated and it is a decision Michael can reverse.

## Trends section

| Number | Where | Source |
|---|---|---|
| Efficiency factor, ride = judged power (the row below: the device's normalized power first for ≥ 20 min, ours as fallback; else average power; coasting counts as 0 W) ÷ the ride's whole-ride average heart rate (the row below), 2 dp. Computed once by the analyser; `compute-facts` copies it, State and Performance print the same number (2026-09-15, TRUTH-MAP §9 Q4) | `cycling-v1/ride-physiology.ts computeRideEfficiency` ← `compute-workout-analysis`; copied by `compute-facts` | FIELD — TrainingPeaks EF ("average heart rate for the workout"); intervals.icu Efficiency = NP ÷ average HR; 2 dp as every published EF value prints |
| Judged ride power (whole ride) = the provider's normalized power when one was sent (`workouts.normalized_power`: Garmin `normalized_power`, Strava `weighted_average_watts`) for a ride of ≥ 20 min; our normalized power from the samples (coasting 0 W) when none; average power under 20 min. Feeds the ride efficiency factor (2026-09-15, WORKORDER §1 rule 7) | `_shared/ride-power.ts judgedPowerW` (third argument) ← `compute-workout-analysis` | FIELD — Coggan NP / TrainingPeaks 20-min rule (file header); rule 7: the device's number wins. **OURS** — the order (provider first, ours second). Segments send no provider number and keep ours |
| Average heart rate of a session = the provider's activity average (`workouts.avg_heart_rate`); our sample mean (`computed.overall.avg_hr`) only when the provider sent none. One resolver for Details, Performance, Today, ride efficiency and the fact packet (2026-09-15, TRUTH-MAP §9 Q3, D-477) | `_shared/fact-packet/queries.ts getOverallAvgHr` | FIELD — Garmin Connect and Strava show the device average. **OURS** — the fallback order (provider first, sample mean second); the book is silent on averaging |
| Efficiency factor, run = grade-adjusted pace ÷ avg HR | `compute-facts` | FIELD — TrainingPeaks EF on NGP |
| Grade-adjusted pace | `_shared/gap.ts` | FIELD — TrainingPeaks NGP |
| Which sessions are on the drift line: steady only — a run is OFF the line only if it was an interval session (>2 planned steps, or the grader's `interval` verdict). The analyser's mixed-effort stamp is a hedge, never a filter | `compute-snapshot driftReadForPoint` | FIELD — Friel: Pa:Hr applies to steady aerobic efforts; TrainingPeaks prints it per workout with no verdict. D-372 item 3 (restored 2026-09-04 after a0ca339a re-added the stamp as a filter and blanked the run line) |
| Drift per session = first half vs second half, warm-up skipped, 5% line | `analyze-running-workout/lib/heart-rate/efficiency.ts`, `ride-physiology.ts` | FIELD — Friel; TrainingPeaks Pa:Hr / Pw:Hr; the 5% line is Friel and Viada p107 (a plan rule — reports and flags only, never a filter: D-372) |
| Drift is a trend, one dot per session | `StrengthReadCards.tsx`, `TrendSparkline.tsx` | FIELD — TrainingPeaks dashboard trend; intervals.icu per-activity points |
| 12-week trend window | `TrendSparkline.tsx` | FIELD — TrainingPeaks 90-day default |
| Headline = the value of a straight line fitted through the last 12 weeks (start → today), never one session | `sport-summary.ts fitTrend`, `StrengthReadCards.tsx`, `StatePerformanceSection.tsx` | FIELD — TrainingPeaks EF/Pa:Hr per workout; the fitted trendline is WKO5's chart trendline (least squares). Replaced the single-session headline 2026-09-04 (which had itself replaced the 28-day Garmin average) |
| One colour on the line, a dot per session | `TrendSparkline.tsx` | FIELD — TrainingPeaks / intervals.icu |
| Fitted trendline through the dots + "start → end" caption | `sport-summary.ts fitTrend`, `TrendSparkline.tsx` | FIELD — WKO5 chart trendline (least squares); TrainingPeaks' dashboard is bare dots, WKO5 adds the fitted line |
| Ride counts for the efficiency trend when it has ≥ 10 min in the aerobic band — any type, any intensity | `bike-fitness.ts MIN_EFFICIENCY_IN_BAND_S` | FIELD — Garmin: the fitness estimate updates from any ride with ≥10 min at aerobic intensity. (The type gate and the 90%-FTP "hard ride" exclusion were removed 2026-09-04.) ⚠️ The one Garmin rule left on the screen: it picks which rides are steady, it does not compute the number |
| Easy / hard pace rows = median of the group's last five runs' recorded pace and HR | `run.ts recentGroupPaceHr` | Recorded values. The warm-up stand-in (a hard run's warm-up joining the easy pool, `warmup_easy`) was **OURS** and was removed 2026-09-04 evening from `compute-snapshot` (both the spine and `runEffHistory`) |
| e1RM = average of Epley and Brzycki, nearest 5 lb | `estimate-1rm.ts estimate1RMRounded` — read by `compute-facts` (logged sets), `save-baseline-test` (the tested max written to `performance_numbers`; the floor to the 5 below was removed 2026-09-15) and `analyze-strength-workout` (the test result card) | FIELD — Viada Module 3 / p215: average two or three formulas for a range; a true single returns as-is; p215's worked example rounds 224.25 to 225 |
| Tested max — which set, what it is compared to, what the card says (2026-09-15, TRUTH-MAP §9 Q5): the set is `save-baseline-test/pick.ts pickTestLifts` for the save AND the result card; "last test A → B" compares to the most recent earlier TEST session for the lift (`isTestSession`), omitted with none; "updated to N" when `performance_numbers` holds this test's number, "kept P" when it holds another, no line when it holds none. §9 Q5's "new baseline" clause is **struck**: nothing on file says whether the lift had a number before the save (no history in `performance_numbers`, no history table), so the card cannot tell a first number from a raised one (Michael, 2026-09-15) | `save-baseline-test/pick.ts`, `analyze-strength-workout buildStrengthTestResult` / `getPriorTestNumbers` | FIELD — p215 (the test set through the formula). **OURS** — test-to-test comparison (the book says tests are for troubleshooting, p120); the outcome read off the file rather than the tap; the line omitted with no prior test |
| 1RM on file, learned from logged sets = the max of the trusted e1RMs (reps ≤ 10, `estimateIsTrusted`) in the last 84 days, RIR ≤ 4 preferred, at least 3 rows for medium confidence and 6 for high, last logged within 42 days (TRUTH-MAP §9 Q1). It reads the same rep gate as State's "estimated 1-rep max" tile, and because the tile's "best" is the max over ALL history of the same trusted rows, the learned number can never exceed the best State prints. No change needed (Michael, 2026-09-15) | `compute-facts updateLearnedStrengthFromExerciseLog` → `strength-facts-lib.ts aggregateLearnedStrengthMaxes`; `state-trend/capacity-resolver.ts` | **OURS** — the 84-day window, the 3/6-row confidence cut, the 42-day freshness, max of the window rather than the latest set (no page; p215 sets the number by test, p247 moves it slowly) |
| A set's estimated max counts toward a record, trend or the learned 1RM only at 10 reps or fewer, every lift (the estimate is still computed and shown on every set) | `src/lib/estimate-1rm.ts trustedMaxReps` / `estimateIsTrusted` | **OURS** — ten is the range the Epley / Brzycki formulas are described as valid over (the file's own "validated to ~10 reps, best 2–6"); p215 answers formula divergence with the average, not a cap. The header's "deadlift 5, else 8" is stale (one ceiling of ten since 2026-08-29); marked in the code 2026-09-16 (Stage 5) | 2026-09-16 |
| Lift chart shows the last 84 days (12 weeks), the same cut as the run efficiency chart. The 52-week fetch (`STATE_TREND_WINDOWS.liftHistoryWeeks`) feeds the all-time best, not the chart. ⚠️ Corrected 2026-09-16 (Stage 5): this row said the chart showed up to 52 weeks | `assemble.ts CHART_WINDOW_DAYS` (the lift series filters on `_chartStart`); `STATE_TREND_WINDOWS.liftHistoryWeeks` | FIELD — TrainingPeaks 90-day default (the 12-week trend window row above); the 52-week fetch: Hevy / Fitbod ranges |
| Trend arrow ↑ → ↓ and the verdict words (up / down / needs data / holding) | — | **OFF EVERY ATHLETE SURFACE (2026-09-04 evening).** Garmin's three states (28 days vs the 28 before) over TrainingPeaks' EF — two products on one number. `state-trend/classify.ts` still computes the 28/28 verdict; State's dot blocks print label + dot + count, the session screen's "discipline trend" chip is removed, and the coach makes no direction claim (`fitness_direction` null). Data, not copy |
| FTP over time on the bike row: one dot per stored FTP reading in the 12-week window, the fitted trendline through them, caption "FTP over N weeks: X → Y" | `compute-snapshot` (`bikeFitness.ftpHistory` from `fitness_baselines`), `StatePerformanceSection.tsx BikeFitnessRow`, `TrendSparkline.tsx` | FIELD — TrainingPeaks threshold history ("track previous thresholds": FTP plotted as it changes); WKO5 sFTP history chart; the fitted line is WKO5's chart trendline (least squares, the same `fitTrend` as efficiency and drift). **Replaced the dot 2026-09-04** — "position in your own 12-week min/max" (`position-in-range.ts`, OURS) is off the bike row; the function still serves the run-decoupling and strength ranges, neither on screen. Fewer than two readings → the number alone, no line |
| 56-day window, every discipline | `thresholds.ts TREND_WINDOW_DAYS` | FIELD — Garmin, two 4-week halves |
| "provisional" tag: 3–4 sessions in the window, or all inside 21 days | `bike-fitness.ts isProvisionalTrend` | **OURS** — no longer rendered on State (2026-09-04 evening); still on the payload |
| Race-projection gate: ≥ 8 observed runs | `assemble.ts projectionMinRuns`, `compute-snapshot` | **OURS** — a count with no outside source; gates a projected race time, not an arrow (was the run direction floor) |

| Heat note on the run and ride cards, shown when a session in the window was ≥ 72°F | `StrengthReadCards.tsx` | FIELD — 72°F / 22°C is Garmin's heat-correction cut-off; TrainingPeaks applies no correction; Friel: compare like with like. The sentence is fixed text, not generated. |

| Which session a run was (easy / long / interval) | `compute-facts classifyRunIntent` | The plan's own tag on the planned session, stamped onto the workout (`workout_metadata.plan_tags`) so it survives a plan rebuild; no plan word → grouped as easy, no inference. Michael, 2026-09-02/04. |

| Easy/hard row pace | `compute-snapshot`, `state-trend/run.ts` | FIELD — grade-adjusted pace (TrainingPeaks NGP) when the run has elevation; raw recorded pace otherwise (TrainingPeaks' own fallback), flagged "flat pace, no elevation". |
| Which runs are graded interval vs easy | `compute-facts classifyRunIntent` reads `workout_analysis.classified_type` | FIELD — the analyser's auto-detection from interval structure + name (Garmin/Strava/TrainingPeaks all auto-detect); heart rate ≥90% LTHR is the secondary rung when no plan/detection — 90% is Friel's Z2/Z3 run-zone boundary (Z2 85-89, Z3 90-94), the same zone table workload.ts reads, FIELD not ours. |

| One chart template: label · big number · qualifier · chart · "over N weeks: start → end" · coverage · key | `TrendSparkline.tsx` | Layout is the app's (2026-09-05, Michael: "burying the lead"). Numbers unchanged; the fitted line is WKO5's. |
| Athlete-set row order (reorder, up/down arrows) | `StatePerformanceSection.tsx`, `user_baselines.ui_prefs.state_row_order` | FIELD — Garmin Connect reorderable cards; TrainingPeaks reorderable dashboard charts. Default = goal-led order. |
| Deload week = the TAPER/DELOAD column, deployed by the athlete from Adjust | `rematerialize-standing-block` (`taper_weeks`), `compose.ts composeBlock` | FIELD — Viada p274 (the column), p120 (never scheduled: overreach-to-deload rejected), p247 (switch to it two weeks out from a race or meet) |
| Adjust tab edits (lifts → `locked_baselines` and the `performance_numbers` seed; FTP → `performance_numbers.ftp` + `ftp_source`; threshold pace → `threshold_pace_min_per_mi` + `threshold_pace_source`; threshold HR → `configured_hr_zones.manual_run_lthr` for the run AND `configured_hr_zones.manual_ride_lthr` for the ride). ⚠️ Corrected 2026-09-16 (Stage 5): the row omitted the ride threshold HR write and named `lthr_source` as written with the run HR | `StateAdjustLens.tsx` via `AppContext.saveUserBaselines` → `save-baselines` (`derive.ts`) | The same save Training Baselines uses; no second write path. |
| Post-ride / post-run popup: a new measured FTP or threshold pace shows with accept | `PostWorkoutFeedback.tsx` | FIELD — TrainerRoad and Garmin surface a detected FTP as a card with accept. The learner runs when the popup opens so the number is fresh. |

## FTP

| Number | Where | Source |
|---|---|---|
| Power-curve read: critical-power fit over 2–20 min, ≥ 3 durations, W′ 5–40 kJ, r² ≥ 0.9, FTP = 0.97 × CP | `src/lib/bike-ftp-estimator.ts` | FIELD — Hill 1993, Jones 2010, Vanhatalo 2011; TrainerRoad / intervals.icu practice |
| Power only — no heart-rate read, no steady-minutes rule | `bike-ftp-estimator.ts compoundFtp` | FIELD — TrainerRoad AI FTP Detection and intervals.icu eFTP are power-only (Michael, 2026-09-04: "just do what intervals.icu and TrainerRoad do"). A heart-rate read with a 15-minute floor (OURS) was built and removed the same night. |
| Hard ceiling = best 20-min actually pedalled, 18 months | — | **DELETED 2026-09-04 evening** (was OURS) — `compoundFtp` takes the fit alone |
| Rate limit ± 5% per learn | — | **DELETED 2026-09-04 evening** (was OURS) — `rateLimitFtp` is gone |
| ⚠️ intervals.icu's eFTP model | `bike-ftp-estimator.ts fitCriticalPower` | UNVERIFIED — intervals.icu and TrainerRoad pages are blocked from the build container; the 2-parameter CP fit on 2–20 min and 0.97 × CP are cited to Hill / Jones / Vanhatalo and Morgan 2019, not to intervals.icu's page. Q-298 |
| 5-minute all-out FTP test | `_shared/baseline-test-rows.ts ftp5MinTestRow` | FIELD — Viada, course Module 3: the 5-minute test is more repeatable (no pacing strategy). The module's ~80%-of-5-min extrapolation is NOT a second FTP formula here: the effort lands as the 5-min point on the power-duration curve and the one FTP rule (the CP fit) reads it. |
| Run threshold pace: proposed, then accepted | `resolve-current-run-pace.ts` (accepted tier, `pendingRunThresholdProposal`, `acceptLearnedRunThreshold`); the learner seeds/keeps `run_threshold_pace_accepted`; accept on Adjust, the post-run popup, the checkpoint | FIELD — the same proposed-then-accepted door as FTP (TrainerRoad; Garmin's detected-threshold prompt). A learned value never re-prices the plan on its own. 2026-09-05. |
| Proposed, then accepted; auto is the default | resolver, checkpoint, Baselines | FIELD — TrainerRoad (validated on 22,000 athletes; default auto with accept) |
| Fallback when both reads abstain: 95% × best 20-min | learner STEP 4 | FIELD — Coggan 20-min test |

## Load (top of State)

| Number | Where | Source |
|---|---|---|
| Fitness (CTL, 42-day EWMA) · fatigue (ATL, 7-day) · form (yesterday's CTL − ATL), over the athlete's whole history | `_shared/fitness-fatigue.ts computeFitnessFatigue`, coach `load.fitness_fatigue`, `LoadBar.tsx` | FIELD — TrainingPeaks Performance Management Chart, exactly (2026-09-04 evening: THE load read on State; the coach's fetch widened from 84 days to all history) |
| The week's change beside fitness / fatigue / form (+3, −2) and the one-line key under the LOAD row | `LoadBar.tsx`, coach `load.fitness_fatigue.week_ago` | FIELD — intervals.icu's fitness tile prints the 7-day delta; the key is TrainingPeaks' own definitions (Fitness = 42-day, Fatigue = 7-day, Form = Fitness − Fatigue). 2026-09-05, Michael: "hard with no context". |
| Form zone word: transitional > +25, fresh +5..+25, grey zone −10..+5, optimal −30..−10, high risk < −30; a value on the line takes the zone below | `fitness-fatigue.ts formZone` | FIELD — Friel "Managing Training Using TSB", as the TrainingPeaks PMC legend reproduces it. The on-the-line rule is a reading of a printed range, stated in the code |
| ACWR and the reconciled load word ("balanced") | `_shared/acwr.ts`, `load-status-reconcile.ts`, coach payload | **OFF EVERY ATHLETE SURFACE (2026-09-04 evening)** — Gabbett's ratio and the app's reconciler are neither product's rule. The coach's week verdict, title, kicker, label, receipts and narrative facts read Form (TSB) instead; ACWR is withdrawn as an input to the readiness word, the marathon read, the week accent and the LLM context. `load.acwr` stays on the payload as data nothing prints |
| Strength workload = minutes ÷ 60 × RPE × 10; RPE = session rating, else 10 − avg logged RIR; nothing → 0 | `workload.ts calculateStrengthWorkload`, `strengthSessionRpe`, planned mirror `calculatePlannedStrengthWorkload` | FIELD — Friel "Estimating Training Stress Score" (TrainingPeaks); RIR → RPE Zourdos 2016. Replaces tonnage ÷ 10,000 × intensity² (OURS) 2026-09-04 evening. ⚠️ Stored strength points are on the old scale until `backfill-strength-load` runs. ⚠️ No hrTSS for a strength session with heart rate: the app holds no strength threshold heart rate; the rating rung applies |
| Rating fallback for cardio: TSS per hour = rating × 10 | `workload.ts mapRPEToIntensity` | FIELD — Friel's table (RPE 1 → 10/hr … 10 → 100/hr) |

## BODY
⚠️ Corrected 2026-09-16 (Stage 5): the BODY row IS on State — restored 2026-09-04 on Michael's instruction; `StateTab.tsx` renders `response_model.visible_signals` (endurance category) as the BODY row. The sentence below is history. Effort and soreness rows: **REMOVED from State 2026-09-04 evening** (Foster + our 7-vs-28 comparison; Hooper + our z-score — neither product's rule). Server computation untouched, coach still reads it.

| Reschedule popup: daily workload cap 120 (140 in a peak week) and the warning line 80 (100 in a peak week); the "better dates" search uses the same 120 / 80 | `validate-reschedule/index.ts` | **OURS** — no app publishes a daily workload ceiling, and these arrived with the popup's own workload formula, which is deleted (2026-09-15, §8.0 #29: the popup now sums the stored `workload_planned`). Stage 5 sources them or strikes them |
| Reschedule popup daily and weekly numbers = the sum of `planned_workouts.workload_planned` for that date, and for the plan week (`week_number`) holding it — the same field the week totals and the drawer's Workload chip read. A row with none makes the popup say "Workload not worked out yet for this session." | `validate-reschedule/index.ts`, written by `materialize-plan` / `rematerialize-standing-block`, read by `get-week` | FIELD — one stored workload per session (TrainingPeaks' planned TSS model); nothing re-derived at read time (2026-09-15, §8.0 #29, #30) |

## Not yet swept
The week-execution bars (a count). Add rows when touched.

## Wizard · "Know your numbers?" (D-467)

| Number | Where | Source |
|---|---|---|
| Working number from a number on file = 1RM × 0.96 | `working-number.ts workingNumberFromFile` | FIELD — Viada p215, the same fraction the test read uses |
| A lifting session's header length: 2 to 4 seconds a rep under the bar, plus the logger's rest clock per set, rounded to five-minute steps | `_shared/strength-session-minutes.ts SEC_PER_REP_LOW/HIGH` (sent as get-week `planned_duration_label`) | **OURS** — the source gives no tempo or time-under-tension figure; 2 s is a brisk controlled rep, 4 s the slow end field guidance gives for hypertrophy work. The rest seconds under it are `_shared/strength/rest-seconds.ts` `REST_MINUTES_ARE_OURS` (stamped on each row as `rest_seconds`) |
| Week-one retest placement: run test on day 3, FTP test on day 5 of the block (inserted by the server with the plan, labelled with the plan week the day falls in: `create-goal-and-materialize-plan/week-one-tests.ts`) | `_shared/baseline-test-rows.ts RETEST_OFFSET_DAYS` | **OURS** — p210 and p212 print no rest rule before either test; the day inside week one is this app's choice. ⚠️ Corrected 2026-09-16 (Stage 5): this row quoted "no hard training 48 hours prior" from those pages; the code comment says the sentence was never on the page (removed 2026-09-11) and SOURCE-viada holds no "48 hours" |
| Run MLSS level-2 round: 45 s @ 125% / 45 s @ 115% / 30 s @ 100% / 1:30 @ VT1; 2 sets of 4; 2 min between sets | `endurance-library/source-rules.ts long_surge_float` (`hold` step) + `generate.ts mkRepeat` | FIELD — Viada p232, step for step. Before 2026-09-05 the float was sampled from a 45–60 s range the page does not give and the 30 s at threshold was missing (both were OURS, now removed) |
| MLSS row text: time above threshold, fatigue spread evenly, hills allowed if the effort is held; no effort number | `standing-plan/session-vocabulary.ts describeSession` | FIELD — Viada p231 in substance, not copied. The old line described threshold pace and the "Effort 8–10 of 10" was OURS (2026-09-02); both removed 2026-09-05. The near-threshold "Effort 5–6" remains OURS |
| Easy pace = ONE RANGE off the accepted threshold pace, threshold × 1.14 (fast edge) to × 1.29 (slow edge), on Adjust, Baselines, the plan's easy steps (as the reference pace under the heart-rate range), the endurance library's easy/VT1 targets and the run summary's easy-portion line. Midpoint only where one number is structurally required (step minutes ↔ miles, the coach entry). No threshold → no easy pace (heart-rate range only). Not pinned: a plan's easy range follows its pinned threshold. The learner's last-five easy median is NOT a source: it stays the State row's receipt, the checkpoint's evidence, and the measurement read by the time-trial check, the adaptation easy gate, long-run minutes → miles and the VT1 fraction (`resolveMeasuredEasyPaceSecPerMi`) (2026-09-15, TRUTH-MAP §9 Q2, D-478) | `src/lib/friel-zones.ts EASY_PACE_FAST_X_THRESHOLD / EASY_PACE_SLOW_X_THRESHOLD` → `run-paces-from-threshold.ts` → `resolve-current-run-pace.ts resolveCurrentRunEasyPace` | FIELD — Friel run Zone 2 = 114–129% of threshold pace (trainingbible.com; TrainingPeaks "Joe Friel for Running" calculator); Garmin/TrainingPeaks print a pace zone beside the heart-rate zone; Daniels' easy is a range (±20 s/mi on a given day); p235: easy by feel, the percentage moves with conditions. **OURS** — printing a pace range under the heart-rate prescription rather than heart rate alone |
| Easy-step heart-rate range on the calendar = Friel Z2 off threshold HR, 85%–89% (129–135 at 152) | `_shared/endurance/hr-zones.ts frielZones` → `src/lib/friel-zones.ts frielRunZones` | FIELD — Friel; one table for Profile, learner, grader and calendar (2026-09-06; the calendar copy topped Z2 at 90% and is gone) |

## Race plan · course strategy and the long-run readiness block (2026-09-07, no-AI work order)

| Number | Where | Source |
|---|---|---|
| Per-group race pace = flat race pace × Minetti metabolic cost of the group's average grade, flat pace solved so the distance-weighted paces sum to the anchor finish | `_shared/course-strategy-build.ts runGradeTimeFactor` via `_shared/gap.ts` | FIELD — Minetti et al. 2002, J Appl Physiol 93:1039 (the GAP model already on the run readouts) |
| Bike-leg grade cost: +10 s/mi per % up (cap 60), −6 s/mi per % down (cap −20) | `course-strategy-build.ts bikeGradeTimeFactor`, `course-detail` terrain-adjusted finish | **OURS** — the linear heuristic the terrain-adjusted finish already used; cycling plan is a future build |
| Pace band ±2.5% around the group pace | `course-strategy-build.ts PACE_BAND_FRACTION` | **OURS** — wide enough to read as a range on a watch, narrow enough that the band mid-points still add up to the finish |
| Race heart-rate band: marathon Z3, half Z4, ≤10K top zone, from the athlete's configured zones | `course-strategy-build.ts raceHrBand` | FIELD — Friel run zones by %LTHR (Total Heart Rate Training) |
| Race heart-rate band with no zones on file: %maxHR marathon 80–88, half 85–92, 10K 88–94, 5K 92–97 | `course-strategy-build.ts raceHrBand` | **OURS** — common %maxHR ranges; replaced by the athlete's zones as soon as they exist |
| Fuel note: 30–60 g carbohydrate/h for 1–2.5 h efforts, 60–90 g/h beyond, first at ~45 min | `course-strategy-build.ts fuelNoteFor` | FIELD — Jeukendrup 2014, Sports Med 44:S25; ACSM/AND/DC 2016 position stand |
| Display groups: 7 for ≥20 mi, 6 for ≥12, 5 for ≥8, 4 for ≥5, else 3; late race = last 30% of distance | `course-strategy-build.ts targetGroupCount` | **OURS** — the group counts the old prompt asked for, kept so the course screen reads the same |
| "First 2–3 miles 15–20 s/mi slower than goal pace" when the session has no pace or heart-rate anchor | `session-detail/race-readiness.ts raceReadinessDeterministicFallback` | **OURS** — the pre-existing fallback line; common coaching guidance, no single citation |
| Ride heart-rate readings (Pw:Hr decoupling, cardiac drift) are withheld when the two halves' pedalling power differ by more than 10% | `_shared/ride-halves-steady.ts` | **OURS** — TrainingPeaks/Friel: Pw:Hr is for steady aerobic rides, no numeric cut published; the structural twin is D-372 (>2 planned steps). Negative decoupling ≤ −5% now reads "rose", not "held steady" |
| Job retry schedule: 1 / 5 / 25 minutes after the 1st / 2nd / 3rd failed attempt; 3 attempts, then failed | `_shared/jobs.ts DEFAULT_BACKOFF_MINUTES`, `DEFAULT_MAX_ATTEMPTS` | **OURS** — a ×5 exponential backoff, the shape most queue defaults use; three tries covers a gateway blip and a one-off compute death |
| Alarm emails: at most one per kind per 15 minutes (the rest only in `alarms`) | `_shared/alarm.ts ALARM_EMAIL_WINDOW_MS` | **OURS** — a dead analyser fails every ride; one message says so |
| An `analyzing` / `pending` analysis older than 10 minutes reads as "did not finish" | `src/lib/analysis-state.ts STALLED_AFTER_MS` | **OURS** — four times the edge runtime's 150 s wall-clock cap (Supabase docs), so a chain still inside its limit is never called stalled |
| run-jobs stops claiming after 40 s; one job may take at most 90 s | `run-jobs/index.ts CLAIM_BUDGET_MS`, `JOB_TIMEOUT_MS` | **OURS** — keeps a tick inside the gateway limit; the next minute takes the rest |
| Rest after a warm-up set = 60 s (the work's rest is 90–180 s by intent) | `_shared/strength/rest-seconds.ts WARMUP_REST_SEC` (stamped as `warmup_rest_seconds`), `StrengthLogger.tsx autoStartRestForSet` | OURS — a warm-up is not the work; Strong/Hevy run warm-up sets on a short timer | 2026-09-07 |
| Post-workout feedback asks only about a run, ride or swim dated today or yesterday (36 h). ⚠️ Corrected 2026-09-16 (Stage 5): the row said "run or ride"; swims get the popup too (D-162) | `check-feedback-needed/index.ts` | OURS — the 7-day window surfaced rows a history pull had just created for old dates | 2026-09-07 |
| Two sessions on one date: "Six to eight hours apart" | `src/lib/today-lines.ts spacingLineFor` (rendered by `TodaysEffort.tsx`) | Viada p145, rule 6 — the page the code cites ("at least 6–8 hours before the resistance session"; the 4–6 h short-VT1-morning case not shown). The same 6–8 h between two-a-days is on p108. ⚠️ Corrected 2026-09-16 (Stage 5): the row cited p108 only and named `TodaysEffort.tsx` as the place | 2026-09-08 |
| Braced hinge (p274 days 2, 5) admits the reverse hyper family by name; home default Weighted Reverse Hyper | `standing-plan/frames.ts alsoAdmits` on both slots | Viada p221 (braced hinge lower: reverse hyperextension first) | 2026-09-08 |
| Exercise how-to lines (back extension, leg curl, chest-supported row, reverse hyper, calf raise) | `strength-grid/grid.ts EXECUTION_HOW_TO` | OURS — Michael's words, from Strong/Hevy exercise cards and field sources; not the book | 2026-09-08 |
| DE cue "move the bar fast" on barbell rows, "move fast" on the rest | `StrengthLogger.tsx intentLine` | Viada p218 (DE = submaximal load, maximal speed); the wording split is OURS | 2026-09-08 |
| Rebuild replaces an accessory movement whose cell answer changed; never deletes, never a done session | `standing-plan/restate.ts` | OURS — rebuild rule, 2026-09-08 | 2026-09-08 |
| The plan adds no row the page does not print: muscle floor and core-pick placement OFF | `standing-plan/compose.ts ATHLETE_ADDITIONS_ON` | Michael 2026-09-08: no rules of ours, only the book's; anything more is added in the logger on the day | 2026-09-08 |
| HYP row cue: "8 to 12 reps, 1 to 2 in reserve. Reps slow as the set goes." No add-weight trigger | `strength-focus-copy.ts STANDING_ACCESSORY_SET_CUE`, `advance-nudge.ts` | Viada p86 (1–2 RIR, 8–10 preferred), p218 (reps slow), p111 (overload is not more weight each session). The top-of-band add-weight trigger was OURS and is removed. Words Michael's, 2026-09-09 | 2026-09-09 |
| Power zones on Profile and the ride analysis: seven levels, Z2 from 56% of FTP | `_shared/endurance/display-zones.ts`, sent by `save-baselines` | FIELD — Allen & Coggan power training levels (TrainingPeaks "Power Training Levels") | 2026-09-10 |
| Swim pace bands at +12 / +8 / +3 / −2 s per 100 from threshold | `_shared/endurance/display-zones.ts`, sent by `save-baselines` | **OURS** — no outside source; `docs/SWIM-PROTOCOL.md` §7.3 cites nothing and gives different offsets (Recovery +15 or slower, Endurance +8 to +15) | 2026-09-10 |
| Workout chart smoothing: pace 60 s (min 10 m), heart rate 15 s, cadence 30 s, power 30 s, VAM 60 s, grade over 100 m (min 20 m); climb counted in 1.5 m steps over 20 m, scaled to end on the recorded gain | `compute-workout-analysis/display-series.ts` | **OURS** — display smoothing with no outside source (the pace, grade and climb figures moved unchanged from the phone chart) | 2026-09-10 |
| A chart's fitted trend needs 3 points or more ("too few" below) | `_shared/state-trend/trend-fit.ts TREND_FIT_MIN_POINTS` | **OURS** — moved unchanged from the phone | 2026-09-10 |
| Logged sets on State: last 8 weeks, a lift listed from 2 sessions, 5 recent sets; 5 main lifts and 8 others shown | `_shared/state-trend/logged-sets.ts`, `coach/strength-logged-sets.ts` | **OURS** — moved unchanged from the phone | 2026-09-10 |
| Folding a lift variant into its slot allows 0.5 lb when matching the latest reading | `_shared/state-trend/fold-lift-slots.ts` | **OURS** — moved unchanged from the phone | 2026-09-10 |
| Interval colour on the Performance tab: a run's work rep against its pace range with no allowance, on its real pace in both columns; warm-up, cool-down and recovery not coloured; watts against the planned range | `_shared/session-detail/interval-compare.ts` (`intervals[].executed.band`), `_shared/run-pace.ts paceRangeBand` | FIELD — the book prints the range and no allowance (p233); warm-up, cool-down and recovery are an easy jog or walk (p231–235). The 5 s allowance that stood here (OURS) is gone | 2026-09-14 |
| Goal-race interval view: 90–110% on, 80–120% near, capped at 100; projection 0.5 s/mi ahead, 60 s/mi behind; pacing variability cut-offs 3 / 7 / 10 | `_shared/session-detail/interval-compare.ts` | **OURS** — moved unchanged from the phone | 2026-09-10 |
| Finish after race day: shown from the day after race day; the label reads "N days after race" up to 7 days, then "after your race" | `coach/post-race-unofficial.ts` (`post_race_unofficial`) | **OURS** — moved unchanged from State | 2026-09-10 |
| My Record's "Logged suggests" line for a lift or swim pace: at least 3 samples in the learned aggregate (a lift: `exercise_log` rows, one per lift per session, so three sessions; a swim: the learner's sample count) AND the aggregate's confidence medium or high; the newest within 42 days ONLY when the aggregate carries a last-logged date (a swim carries none, so no freshness gate); and 5% or more away from the number the app runs on. ⚠️ Corrected 2026-09-16 (Stage 5): the row omitted the confidence gate and applied the 42 days to swim | `_shared/baseline-suggestions.ts` (rules from `_shared/state-trend/reconcile.ts SUGGEST_MIN_SAMPLES` / `SUGGEST_FRESHNESS_DAYS` / `SUGGEST_MIN_DIVERGENCE_PCT`) | **OURS** — no outside source; the code calls it approved, with no ledger row until now | 2026-09-10 |
| My Record bests: equal finish times keep the earlier race; equal FTPs keep the first date | `athletic-record/record.ts` | **OURS** — a tie-break, no source needed beyond saying so | 2026-09-10 |
| Sample week's balance sentence: a session of 75 minutes or more counts as long | `_shared/week-one-summary.ts` | **OURS** — moved unchanged from the phone grid | 2026-09-10 |
| Club-night note: "about 24 hours" between the two hard days, against "the 48 to 72 most plans leave" | `create-goal-and-materialize-plan/race-readout.ts` | **OURS** — the words moved as written; no source in code or here | 2026-09-10 |
| Season wizard history: 38 km counts as marathon-like; 25 km and 21 km pick the long-run sentence; run and ride cut-offs 10 / 6 / 3 / 1 sessions in 28 days; swim pre-select 2 = race, 3 = focus and steady, 0 = learning; Saturday and Sunday are the default long days for the conflict rows | `get-arc-context/history-readout.ts` | **OURS** — moved unchanged from the phone | 2026-09-10 |
| Run-and-strength week: long-run chips stop at 90 minutes, default 75 | `_shared/standing-plan/frames.ts` (`runStrengthWeek`) | **OURS** — the easy run's 30 minutes is the book's (p246, p235); 90 and 75 are ours | 2026-09-10 |
| Race intake: the long run may be at most 35% of the week for a beginner and 30% for intermediate and advanced, and the tier note shows when the typed miles sit 25% below the seed | `src/lib/run-volume-tables.ts` | **OURS** — no source | 2026-09-10 |
| Week one of a run plan starts no lower than 0.7 of the table's start | `src/lib/run-volume-tables.ts ENGINE_START_CLAMP_FRACTION`, read by `generate-run-plan` and the intake's mileage floor | **OURS** — no citation for 0.7 | 2026-09-10 |
| Today's hot-day line on a run set by heart rate: shown when it is warmer than 60°F | `get-weather` (`heat_note`), `_shared/heat-adjust.ts TEMP_REF_F` | **OURS** — the heat model's own chosen reference (its comment: studies put neutral at 50–55°F, Garmin corrects from about 72°F). The phone's 75°F had no source and is gone | 2026-09-10 |
| A strength row with no reserve target reads "1-2 in reserve" | `_shared/strength/strength-display-lines.ts` | **OURS** — moved from the phone; materialize-plan writes "with 2 in reserve" on an unpriced percent row, and the two still differ | 2026-09-10 |
| A mobility row that names no set count is one set | `_shared/mobility-sets.ts` | **OURS** — moved from the calendar's text parser. ⚠️ 2026-09-16 (Stage 5): a copy of the phone parser still stands inside `TodaysEffort.tsx formatRichWorkoutDisplay`, which has no caller (dead) | 2026-09-10 |
| A planned pool swim with no pool set is a 25 yd pool (22.86 m) for an imperial athlete and a 25 m pool for a metric one | `_shared/swim/planned-pool.ts` (materialize-plan writes `pool_unit`, `pool_length_m`) | **OURS** — both are standard short-course lengths, but defaulting to one is ours; the same default activate-plan and the Garmin export already use | 2026-09-10 |
| The line of good news on a done session: the four power durations checked are 20 min, 5 min, 1 min and 5 s; the heart-rate and pace lines compare against the last 8 sessions; the drift line needs 2 sessions in a row under the line; a set logged with 1 or more in reserve counts as reps to spare; windows are since the block started, else since January | `_shared/session-boom/line.ts` (stored as `computed.session_boom_v1`, sent as `session_detail_v1.boom`) | **OURS** — moved unchanged from the phone. The 5% drift line itself is the book's (p107) | 2026-09-10 |
| Workout chart lines stored at most 600 points per line (`workouts.display_series`), thinned at one uniform stride after smoothing | `compute-workout-analysis/display-series.ts DISPLAY_SERIES_MAX_POINTS`, `thinSeries` | **OURS** — the phone draws the chart about 390 px wide; the full recording in the row timed the list reads out (2026-09-10) | 2026-09-10 |
| Recording screen cue: outside the step's pace range but within 5% faster or 7% slower of its midpoint reads "PICK IT UP" / "EASE OFF"; beyond that "SPEED UP" / "SLOW DOWN"; the outer band is never tighter than the range | `_shared/live-cue.ts` (materialize-plan stamps `computed.steps[].live_cue`) | **OURS** — the 5 and 7 are the running analyzer's own rep reading (`classifyRepExecution`), uncited there too; the phone's 10% had no source and is gone | 2026-09-10 |
| Recording screen cue by heart rate: 10 beats outside the step's range before the cue escalates | `_shared/live-cue.ts HR_CUE_OUTER_BPM` | **OURS** — moved unchanged from the phone | 2026-09-10 |
| Which session is listed first on a day: the lower-body lift before its same-day run or ride, except a long ride which goes first | `_shared/day-order.ts` (get-week `day_order`, plan-overview `overview.day_order`) | FIELD — Eddens, resistance before endurance on a concurrent day (+6.91% lower-body dynamic strength), as the phone rule cited; the long-ride exception is the legacy pairing rule (`STRENGTH-PROTOCOL.md` §6.1). Moved unchanged from the phone | 2026-09-10 |
| A day with no lift pair lists swim, then bike, then run, then strength, then the rest, then by name | `_shared/day-order.ts disciplineRank` | **OURS** — a stable tie-break, moved unchanged from the phone | 2026-09-10 |
| Was a session steady enough to read drift on: the plan's own session type first (easy, long and recovery read; tempo, threshold and harder do not), then more than two planned steps, then the athlete's own tag, then Strava's `workout_type`, then the device's lap markings, and last a swing of 75 seconds or more between the fastest and slowest mile over at least five miles | `_shared/session-detail/session-steadiness.ts` (`sessionSteadiness`, `PACE_SWING_SEC`), read by `drift-pct.ts` and so by the Drift tile, Today's good-news line and State's drift chart | **OURS for the 75 seconds and for the ORDER of the ladder.** The rule that drift is an easy/VT1 read is the book's (p107) and the easy/hard band each session type sits in is the book's (`_shared/endurance-library/classification.ts`, page-cited per family). No source gives a figure for "did this session hold one pace": 75 s is far wider than a steady run's swing on hills or in traffic and far narrower than the gap between a rep and its recovery. It is the last rung because it is the only one that reads the shape of the data rather than a statement by the plan, the athlete or the device | 2026-09-12 |
| A run second counts as stopped when slower than 40:00/mi | `_shared/run-pace.ts STOPPED_SLOWER_THAN_S_PER_MI`, read by every run pace, split, rep time and grade-adjusted pace | **OURS** — Strava takes stops out of moving time and pace but publishes no speed for it (support.strava.com, "Moving Time, Speed, and Pace Calculations"). 40:00/mi is the line `_shared/gap.ts` already used; the app also had 0.3 m/s and 0.5 m/s, both gone | 2026-09-14 |
| A jump of more than 60 s between two run samples is a recording break and counts as stopped | `_shared/run-pace.ts SAMPLE_BREAK_S` | **OURS** — the summary step's existing cap, moved unchanged | 2026-09-14 |
| Run threshold suggestion: critical speed from the fastest 400, 800, 1,000, 1,500, 3,000 and 5,000 m of the last 16 weeks, at least 24 runs, at least three distances, slope of distance against time | `src/lib/run-critical-speed.ts fitRunThresholdFromBestEfforts`, written by `learn-fitness-profile` as the measured threshold for the athlete to accept | FIELD — Smyth & Muniz-Pumares 2020, Med Sci Sports Exerc 52(12):2637–2645 | 2026-09-14 |
| …on grade-adjusted pace for every effort, moving seconds, no minimum length; net descent steeper than 1% refused | same, metres from `_shared/run-pace.ts cumulativeFlatMeters` | FIELD for grade adjustment on any stretch (the paper; TrainingPeaks Normalized Graded Pace; Garmin Grade Adjusted Pace). **OURS** — the 1% limit (course-measurement practice, 1 m/km of net drop) | 2026-09-14 |
| …the best 45 minutes as a seventh point only when its average heart rate reached 95% of threshold heart rate | same, `HARD_45_MIN_PCT_LTHR` | FIELD for the 95% (Friel run Zone 4 floor, `src/lib/friel-zones.ts`); **OURS** that the 45-minute window is a point at all (TrainingPeaks reads it as the threshold on its own) | 2026-09-14 |
| …the checks: efforts from two or more runs, R² ≥ 0.95, D′ 30–600 m, at least 4% faster than easy pace; confidence high at five or more points with R² ≥ 0.99. No heart-rate check on distance efforts, no order check | same | **OURS** — the paper reported R² 0.9999 on average and set no floor | 2026-09-14 |
| No grade-adjusted pace on a run row (rep, lap, recovery) shorter than 400 m; the column prints "—" | `compute-workout-summary GAP_MIN_ROW_M` | **OURS** — no app publishes a minimum; Strava: "the difference between GAP and actual pace generally becomes larger as the grade steepens" (support.strava.com, Grade Adjusted Pace), and over a short row one steep stretch is most of it | 2026-09-14 |
| A single printed percentage on a ride step is judged ±5% either side ("2:30 @ 90%" → 143–159 W at FTP 168). One definition, one place: `SINGLE_PERCENT_BAND`, applied where a percentage becomes watts. The Garmin sender reads the same constant for a step that reaches it as a bare centre | `_shared/plan-tokens/quality-work.ts SINGLE_PERCENT_BAND` (`wattsAt`); `_shared/garmin/convert-workout.ts` | **OURS** — no field source. Searched TrainerRoad support + forum, Zwift support and Zwift Insider, TrainingPeaks Workout Builder help + FAQ, intervals.icu forum and docs: none publishes a tolerance for judging a single-target step. TrainingPeaks offers Target OR Range per step and does not widen a Target (a device that cannot take a range gets the middle value); TrainerRoad's own workouts use a fixed 20 W spread — a different rule on a different quantity, not borrowed. Replaced four copies of ±5% (`materialize-plan` prose parser, two dead ones in `get-week`, the sender's centre fallback) | 2026-09-15 |
| p237's anaerobic work carries a power FLOOR and no ceiling: the step writes a lower and no upper, prints "202 W and up", and is green at or above the floor. It applies to a work step at or above the family's `workFloorPct`; a step below it (the 90% middle, the 50% half) keeps its band. `ride_vo2` is deliberately excluded | `_shared/endurance-library/source-rules.ts FAMILIES.ride_anaerobic.floorOnly`; `quality-work.ts wattsAt`; `_shared/ride-power.ts powerRangeBand`; `session-detail/interval-compare.ts powerBand` | FIELD — Viada p237: *"best done by feel with a power FLOOR rather than a specific power target — the numbers are guidelines"*, and the page's own "110–115%+" on its first option. p238 states the same principle from outside: the anaerobic sessions run on *"more power is generally better"*, which VO2 is then told not to do. **OURS** — that the floor is the discriminator between a floor-only step and a banded one (the page states the principle for the family and prints the `+` on one option) | 2026-09-15 |
| A floor-only step goes to the watch as floor → 130% of FTP | `_shared/garmin/convert-workout.ts FLOOR_ONLY_WATCH_CEILING_PCT_OF_FTP` | FIELD — Viada p237's own top ("start at 110%, progress to 125–130%"). A Garmin step's custom power target is a low/high pair with no open-ended form, so the page's top fills the high. ⚠️ Percent of FTP, not a multiple of the floor: at FTP 168 a 120% step sends 202 → 218 W, where `floor × 1.30` would send 262 W (156% of FTP), a number p237 never prints. ⚠️ Garmin's schema was NOT read; the pair requirement is our sender's, and the inference is flagged as such | 2026-09-15 |
| Drift is not computed or counted on a session whose family sits above or near threshold — the VT1 window inherits p107's gate instead of running before it | `_shared/session-detail/vt1-window-drift.ts` (`steadiness` → `sessionSteadiness`), read by Performance, State's chart and Today's streak | FIELD — Viada p107 (drift is the dose guide for easy/VT1 work) and `endurance-library/classification.ts`'s page-cited band per family. All three callers ran the window before the ladder, so a `read` returned a number the gate would have refused; on p237's sandwich the window's relative test crowned the 90%-of-FTP middles as the rider's easy riding and printed 3.4% on an anaerobic interval ride | 2026-09-15 |
| The Performance EFFICIENCY row appears only on a ride the engine already counts as steady aerobic, and reads "Watts per heartbeat 1.42. Your average on steady rides over the last four weeks is 1.38." | `_shared/session-detail/build.ts formatCyclingEfficiencyRow`, gated on `workout_analysis.bike_fitness_v1.counts_toward_trend` and comparing against `state_trends_v1.bike.efficiency.recentValue` | FIELD — Garmin's ≥10 min at aerobic intensity is the gate already stamped on the ride (row above) and already read by State's chart and Today's ride line; the four weeks is `TREND_HALF_DAYS`, Garmin's 28/28. The comparison is State's own series, so the two screens cannot mint different averages. The previous row printed the definition of the number instead | 2026-09-15 |
| A chart caption reads "building · N of 12 weeks" until its readings span 11 weeks or more; at 11 and over it reads "last N weeks". One cut for every chart on the screen — run and ride efficiency, drift, FTP over time, best 20-minute power, each lift's estimated max | `_shared/state-trend/trend-fit.ts TREND_BUILDING_UNDER_WEEKS`, carried on `TrendFit.building` beside the span and the low–high | **OURS** — no outside source. Searched TrainingPeaks help, WKO5 and Garmin Connect for a coverage caption on a trend chart: none publishes one. Eleven of the twelve, so a chart is not called building for the sake of a few missing days at the far end. It lived on the phone as `spanWeeks < 11` with no marker until this date | 2026-09-15 |
| A trend row dims and prints "limited data" when it rests on fewer than 5 readings AND its newest reading is more than 21 days old — both, never either | `src/lib/trend-receipt.ts LIMITED_DATA_UNDER_SAMPLES` / `LIMITED_DATA_STALE_OVER_DAYS`, applied in `_shared/state-trend/discipline.ts` | **OURS** — no outside source publishes a thin-and-stale pair. The counts are already printed beside it, so this adds a caution and never a second number. It was two literals in the State screen's render with no marker until this date | 2026-09-15 |
| State's NEXT row lists at most 3 sessions, after today. `key_sessions_remaining` is unchanged and still includes today — the coach's own `hasUpcomingLong` reads it for race-week guidance | `coach/index.ts NEXT_SESSIONS_SHOWN`, sent as `week.next_sessions` | **OURS** — a display bound (Michael, 2026-09-01: *"I dont think we need today reflected on state screen or next"*). The phone filtered and sliced the shared list itself until this date | 2026-09-15 |
| The swim re-test nudge fires after 4 weeks since the swim threshold was last set, 4 honored swims inside that time, and a honored swim within the last 10 days | `src/lib/swimBaselineNudge.ts WEEKS_MIN` / `HONORED_MIN` / `ACTIVE_WITHIN_DAYS`, run by `_shared/arc-context.ts` and sent as `swim_retest_nudge` with its finished sentence | **OURS** — no commercial app publishes a re-test trigger for swim threshold (Garmin, TrainingPeaks and Swim Smooth all leave the CSS test to the athlete). Searched DECISIONS-LOG for the "D-200" the code cites: it is not there, it lives in `docs/SPEC-intensity-baselines.md:162,226`. The whole rule ran in the browser off two table queries of its own until this date | 2026-09-15 |
| The record ("best") prints beside a lift's estimated max only when it beats the latest reading by more than half a pound — the same half-pound slack the PR verdict uses, defined once | `_shared/state-trend/assemble.ts E1RM_ROUNDING_SLACK_LB`, carried as `perLift.readout.best` (absent = do not show) | **OURS** — it absorbs rounding between the stored estimate and the all-history read; it is not a tolerance on what counts as a record (Michael, 2026-07-21: *"a PR should be a real PR"*). The screen kept its own copy of the number until this date | 2026-09-15 |
| A lift on State is written in the athlete's own unit: stored in pounds always, converted for a metric account by the definition constant (1 lb = 0.45359237 kg). The estimated max, the record, the all-out set, every logged set and the training-max status line all arrive as finished text | `_shared/state-trend/assemble.ts liftReadout`; `coach/strength-logged-sets.ts`; `rematerialize-strength-block` → `src/lib/strength-calibration-copy.ts liftStatusLine`; constant `KG_PER_LB` | FIELD — the international pound definition. Same rule `save-baselines/zones.ts` already applied to Adjust and Baselines (§8.0 #7). State printed "lb" on every account whatever unit they had chosen, until this date | 2026-09-15 |
| The State run row's easy and hard pace arrive in the athlete's own unit, the whole pace rounded once then split | `_shared/state-trend/run.ts recentGroupPaceHr` (`recentPaceDisplay`) | FIELD — 1 mi = 1609.344 m by definition; the round-once rule is the one pace formatter's (§8.0 #2). The screen multiplied seconds-per-kilometre by 1.60934 in the render until this date | 2026-09-15 |
| An easy session's interval table opens on the long block and folds the rest behind "show all": a row of 15 minutes or more is the block, a row under 2 minutes is a stride. The fold applies only when the session is easy-like, has more than two rows, and every row that is not the block is a micro one | `_shared/session-detail/build.ts LONG_BLOCK_MIN_S` / `MICRO_ROW_UNDER_S`, carried as `print_by_default` on each interval | **OURS** — no outside source. Searched Strava and Garmin: both list every lap and leave the reading to the athlete, publishing no cut-off for hiding one. It hides rows and changes no value, and the toggle still shows them all. It lived in the table's own render as two bare numbers until 2026-09-16 | 2026-09-16 |
| A session's peak temperature is named only when it clears the higher of the start and end readings by more than half a degree | `workout-detail/index.ts PEAK_OVER_ENDS_F` (`weather_lines`) | **OURS** — no source. Searched Garmin, Strava, TrainingPeaks and Open-Meteo: none publishes a threshold for reporting a peak. Half a degree is the smallest step a whole-degree readout can show, so anything under it prints a peak equal to the number beside it. It ran in `WeatherDisplay`'s render until 2026-09-16 | 2026-09-16 |
| "Feels like" prints only when it sits at least 2°F from the session's average | `workout-detail/index.ts FEELS_LIKE_MIN_DIFF_F` (`weather_lines`) | **OURS** — no source; the same search found none. Inside two degrees the two numbers round to within one of each other and read as the same fact twice. It ran in the render until 2026-09-16 | 2026-09-16 |
| A segment's overall placing is printed only inside the top ten | `workout-detail/index.ts OVERALL_RANK_SHOWN_THROUGH` (`display_metrics.segments[].show_overall_rank`) | **OURS** — Strava publishes the leaderboard itself, not a rule for when a placing is worth showing. It was a bare `<= 10` in the map's render until 2026-09-16 | 2026-09-16 |
| Every distance, pace and set weight on the Performance and Details tabs is written in the athlete's own unit by the server, and the screen prints the string | `_shared/session-detail/build.ts` (`distance_display`, `duration_display`, `avg_pace_display`, `swim_pace_display`, each interval's `pace_display` / `gap_display` / `distance_display`, each set's `weight_display`); `workout-detail/index.ts display_metrics` | FIELD — 1 mi = 1609.344 m, 1 yd = 0.9144 m, 1 lb = 0.45359237 kg, all by definition. The two tabs converted in their renders with no metric branch at all, so a metric athlete read miles, yards and pounds on their own screens; the swim pace was computed in the PLAN's unit and labelled with the ATHLETE's, which printed metres as "/100yd" (§8.0 A4) | 2026-09-16 |
