# Engine State

A current snapshot of what's load-bearing, what's known broken, and what's believed-working but unverified. Read this BEFORE proposing changes — most "obvious" bugs were either already fixed (don't re-litigate), already filed (don't re-discover), or intentionally left in place (don't "fix").

Last updated: 2026-06-16 (SWIM ARC D-159→D-172 — the session's whole body of work; full narrative in the "Swim sweep consolidation" Solid entry below. From "swims render land junk + wrong numbers" → swim-native across home/Details/Performance, single-sourced numbers (`swimPacePer100Seconds`/`resolvePoolLength` — FOOTGUN: never recompute inline), elapsed-based duration adherence (D-163), post-swim popup capturing pool/RPE/feel/equipment (D-162), second-person data-rich narrative (D-167/D-168), source-tier model + honest Connections matrix (D-169/D-172). **3 open D-172 next-up items:** manual swim entry (PRIORITY — the matrix badge now advertises it), card badge wiring (`deriveSwimTier` ready), FORM→Apple nudge (one global dismiss). **Q-060** (HealthKit ingest + 60s merge) is the dependency that makes the "never a duplicate" guarantee true end-to-end for the Apple Health pipe. Previous: 2026-06-11 (D-139 — rest timer re-architected to AUTO-START + top-pill-only + haptics (supersedes D-121's opt-in; saga D-120 auto-start-in-card → D-121 opt-in → D-139). Completing a non-last, non-duration set auto-starts its rest timer (`autoStartRestForSet`, keyed `${id}-${setIndex}`, re-arms a skipped set); the in-row rest block (Start/Pause/Resume/Skip/editor) was REMOVED — set footer is now just Done + delete-✕. The pinned D-100 top pill is the SOLE timer surface (`REST m:ss · Skip`; Skip ends the rest = clear timer + dismiss, was a pause-only ✕). Haptics via @capacitor/haptics@8 (guarded/no-op on web): light impact on auto-start, success notification at rest 0:00 (added to the existing tone/vibrate zero-handler). cap sync registered the native plugin; Xcode rebuild needed for native haptics. Optional silence toggle = noted follow-up. Previous: 2026-06-11 (D-134 — RIR confirm-on-Done. Reps/Weight prefill from the plan but RIR stays NOT pre-committed (D-126: assessed after the set, protects e1RM). Friction was manual keypad entry on every Done. Now tapping Done on a set with no RIR surfaces a quick INLINE amber "Confirm RIR" row on that set's card — `0..5+` pills with the suggested (`target_rir`) value pre-highlighted/ringed; one tap confirms, a different tap adjusts (both complete), "skip" completes without RIR, second Done cancels. Not a modal. The suggested value is NEVER auto-committed — the tap IS the assessment (the reason RIR isn't prefilled). If RIR was already set (cell/strip) → no prompt. Verified: build + 380px render (w-9 pills, 15px gaps, no overflow). Previous: 2026-06-11 (D-133 — suppress iOS autofill bubble on logger text inputs (exercise-name/search + tel timer editors; the Reps/Weight/RIR keypad is buttons, never a trigger). Previous: 2026-06-11 (D-132 — DATA-INTEGRITY: fixed cross-workout draft bleed (logged Upper's sets/identity into Lower; survived hard quit). Root cause: the logger's localStorage draft key was DATE-ONLY (`strength_logger_session_${performedDate}`) where performedDate = the *viewing* day (defaults to today, NOT the workout's planned day), so any two workouts opened the same day shared one slot; restore rehydrated it with no check it belonged to the opened workout; and it persisted eagerly (bare +/- nudge wrote a phantom draft). Three layers: (1) IDENTITY GUARD — restore only if `saved.sourcePlannedId === openedId` (null===null for ad-hoc), else load fresh; (2) IDENTITY-AWARE KEY — `..._${plannedId||'adhoc'}`, legacy date-only key read as fallback but still identity-guarded; (3) GATE-ON-DONE — draft written only after ≥1 completed set (phantom +/- drafts no longer persist; un-completing back to zero clears the draft) + one-time legacy cleanup of phantom/stale date-only keys. PRESERVES D-110 orphan fail-safe, 24h window, same-workout resume. No path mislogs the stuck blob (worst case = load fresh). Verified scripts/_session-trace.mjs 11/11 + device repro. Previous: 2026-06-11 (D-129 — quick-adjust strip now sizes to the device, not the 380px harness floor. Nudge buttons are `flex-1` (grow with real width) in count-weighted groups (reps 2 / wt 4 / rir 2) at `h-10` (40px); dropped the `w-9` leader so the bar uses full card width. ≈32×40px at 380px (no overflow) → ≈37×40px on a 414px iPhone — comfortable thumb targets. Lesson: the harness only knows "fits/doesn't," not "cramped" — sizing was tuned to 380px and shipped cramped on real phones; eyeball ~414px for sizing changes. Previous: 2026-06-11 (END-OF-SESSION CONSOLIDATION — strength logger + e1RM.

**STRENGTH LOGGER is now compact keypad-primary** (NOT the circles version — that's retired). Plan-prefilled Reps/Weight/RIR cells are the primary input (tap = keypad; Q-042 pencil signals it) + ONE thin quick-adjust strip (reps ±1 / wt −5/−2.5/+2.5/+5 / rir ±1; the RIR pair is amber per D-128). The three old control rows (reps circles + RIR pills + 2×2 weight stepper) collapsed into that strip — ~156px shorter (D-125). **Prefill rule:** PLAN prescription when a plan exists; last-actual fallback when unplanned (D-126 + D-127) — never empty when we have history. Persistent **"last: w × r @ RIR" anchor** per set (D-122, last-actual, blank on overflow indices / history-less exercises). **Rest timer is OPT-IN** (D-121, after the reverted D-120 auto-start experiment): row on every set except the last, idle by default, user taps Start; Pause/Resume/Skip. **"Deload" header pill** (D-124). Header is two rows (title+pill / date+Pick-planned), title wraps instead of truncating. Verified throughout via `scripts/verify-strength-row-380.mjs` (380px, overflowPx -10, 18px strip inter-group gap).

**e1RM PIPELINE is single-source-of-truth** via `compute-facts/brzycki1RM` (Brzycki on `reps + RIR`) → `exercise_log.estimated_1rm` + aggregated `learned_fitness.strength_1rms` → plan loads (`materialize-plan`) + UI (`useExerciseLog`). RIR-aware via **preference-with-fallback** (D-118): aggregate from `avg_rir ≤4`/no-RIR sessions when any exist; lifts with only RIR≥5 fall back to using them, flagged `confidence:"low"` (0 lifts go dark). Deployed + backfilled (trap_bar_deadlift 160→150, barbell_row 130→120; others unchanged). The Epley×(1+rir/10) `compute-adaptation-metrics/estimate1Rm` path (D-116) is **DEAD** — `workout_adaptation` has 0 readers; D-116's RIR≥5 exclusion is inert (live equivalent is D-118). Retire-vs-wire is Q-041.

**LOAD-BEARING INVARIANTS (future-me, do not break):** (1) the planned-vs-unplanned prefill split rides on **planned sets having `rir: null`, NOT `undefined`** (`parseFromComputed` sets `null`; `undefined` = untouched = unplanned). An init path that leaves a planned set's `rir` undefined would make it wrongly accept last-actual. (2) **Deload detection is name-based** (`/deload/i` on `scheduledWorkout.name`) — the app's convention, but NOT a structured flag. The robust version (wire the server `WeekPhase` type in `_shared/athlete-snapshot/body-response.ts` through to the client logger) is **unbuilt**.

Previous: 2026-06-11 (Q-048 follow-up fixes. D-127: unplanned-only last-actual fallback — D-126 left unplanned sessions with empty boxes; restored the last-actual prefill but ONLY for untouched (unplanned/fresh) sets, so planned sets keep the prescription (deload stays correct) and unplanned sessions are never empty when history exists. Rule: plan in the box whenever a plan exists, else last-actual. D-128: RIR ±1 strip buttons tinted amber (the app's RIR=amber language, per D-123) so they read distinct from the identical-looking reps ±1 pair — color not labels (zero added width, keeps the 18px margin). Build clean; harness unchanged (overflowPx -10, gap 18px). Previous: 2026-06-11 (Q-048 — compact "keypad-primary" set logger, shipped in two commits. D-125 (layout): the three control rows (reps circles + RIR pills + weight stepper) collapse into pre-filled keypad cells (tap = keypad, the primary input) + ONE thin quick-adjust strip (`reps ±1 / wt −5/−2.5/+2.5/+5 / rir ±1`, no inline labels — cell order signals the groups). ~156px shorter (336→180). "↑ Same" removed. Buttons h-8 px-1 → 380px harness `overflowPx -10`, 18px inter-group gap. D-126 (prefill source, supersedes D-097's field prefill): fields now prefill from the PLAN PRESCRIPTION, not last-actual — planned sessions show the plan's weight/reps (rir stays null → ghost target + honest Done-prompt); UNPLANNED sessions now start EMPTY (the one real behavior change — flagged); last-actual lives only in the D-122 "last:" anchor. The prior-session fetch is kept (the anchor needs it); only the field-write block + `from_previous` path were retired (left dormant). Kept as shipped: last: anchor, deload pill, pencil, rest timer, left labels. Previous: 2026-06-11 (two logger intuitiveness items. (1) D-123 / Q-046 — Reps vs RIR circle rows were near-identical twins (both rounded-md, white-selected); now RIR = full circles + amber-selected, Reps unchanged (rounded squares + white). Shape+color, no added text, footprint-neutral (380px harness overflowPx -10). (2) D-124 / Q-047 — "Deload" header tag. Investigation of "why is bench's suggestion (100×4@RIR4) lighter than last: (105×6@RIR2)?" found correct-by-design: planned-session prefill + target come from the PLAN prescription (`prefillFromPlanned` → `parseFromComputed(row.computed.steps)`), and the generator prescribes lighter on a deload week; "last:" (D-122) is last-actual. User confirmed deload week → NO logic change. Communication gap fixed with a subtle amber "Deload" pill in the header, detected by name-string parse (`/deload/i` on scheduledWorkout.name — the app's standard method; no structured week_type flag is plumbed to the logger, accepted limitation). Previous: 2026-06-11 (D-122 / Q-045 — persistent "last:" per-set anchor in the strength logger. Surfaces the prior session's actuals per set index as a muted line (`last: 100 × 5 @ RIR 3`) under the top cells — the "what did I do last time" anchor (biggest gap vs Strong/Hevy). REUSES the existing D-097 prefill fetch (`workouts.strength_exercises` JSONB, last 10 sessions, matched by normalized name + set index) — no new query/table/server; the map is hoisted to `previousSessionByName` state so one fetch feeds both prefill and anchor. Intentional & filed in Q-045: overflow set index (today has more sets than last time) shows NO anchor — blank, not clamped — a truth-anchor must not lie (diverges from prefill's `?? last` clamp on purpose); history-less exercise shows NO line at all (not "last: —" repeated); prefill (clears on edit) and anchor (stable) coexist. `formatLastSet` handles duration (`last: 0:45`), bands (resistance_level), missing RIR (drops `@ RIR`). `exercise_log` was the wrong source (aggregate-per-session, not per-set). Client-only. Verified: build + 380px harness (anchor adds height, width overflowPx -10, footer below open picker, steppers 44→308). Previous: 2026-06-11 (D-121 — rest timer is OPT-IN; reverts D-120's auto-start. The rest row appears on every set EXCEPT the last, shows its duration **idle** (does NOT auto-count), and the user taps **Start** to launch it (Pause/Resume to control, Skip to dismiss + hide via `restDismissed`). `showRestTimer = !isDurationBased && !isLastSet && !restDismissed.has(key)` — NO `set.completed` gate, NO auto-trigger; the `startRestAfterSet` function + its 4 callers were deleted. Kept from D-120: Pause/Resume/Skip controls, `restDismissed`, the `set.reps` duration source. Toggle label = Start (never-started) / Pause (running) / Resume (paused mid-count). Client-only. Verified: build + 380px harness (Rest+Start+Skip on every non-last set, overflowPx -10; last sets no rest; steppers 44→308). Doc note: the "auto-start experiment, reverted" banner sits on D-120 (which introduced auto-start), not D-115. Chain D-115→D-120→D-121. Previous superseded entry: 2026-06-11 (D-120 — rest timer ownership moved to the just-finished set + auto-started; reverted by D-121). Previous: 2026-06-11 (Q-040 decision #1 / D-118 — RIR preference-with-fallback in the LIVE e1RM path (`compute-facts`), the one that actually drives plan loads + UI. `updateLearnedStrengthFromExerciseLog` now aggregates `strength_1rms` from `avg_rir ≤4`/no-RIR sessions when any exist; only-RIR≥5 lifts fall back to using them (Brzycki on reps+RIR) flagged `confidence:"low"` — preference, not blanket exclusion (0 lifts go dark). Deployed + backfilled: trap_bar_deadlift 160→150, barbell_row 130→120 (both had a RIR≥5 window-max), bench/squat/ohp value-unchanged (confidence/sample_count down). Plan loads (`materialize-plan` → `learned_fitness.strength_1rms`) now derive from corrected numbers. NOTE the dead `compute-adaptation-metrics` path (D-116) is separate + still inert (Q-041); and the UI trend `useExerciseLog` still shows raw per-session e1RM incl. RIR-5 (follow-up in Q-040). Also shipped today: Q-039 step 3 / D-117 (rep-circle picker + 2×2 stepper + reactive entry). Previous: 2026-06-11 (Q-039 set-logger refactor steps 1+2 — D-116. RIR picker scale 1–5 → 0,1,2,3,4,5+ (5+ stored as integer 5, no separate flag); manual keypad entry clamps RIR 0–5. ENGINE CHANGE: `compute-adaptation-metrics/estimate1Rm` now returns null for RIR ≥5 (excluded from e1RM — "far from failure", autoregulation-only) instead of inflating Epley by ×(1+rir/10); RIR 0–4 unchanged, null RIR = raw Epley. `avg_rir` still recorded for RIR-5 sets; downstream `useExerciseLog` already null/≤0-safe. Pre-check found RIR-5 was inflating e1RM ×1.5, so steps 1+2 shipped together per the working contract. effective-reps not implemented anywhere yet (N/A). Interim layout: 6th pill kept w-9 via gap-1 + paddingLeft 4 (right edge still anchored under RIR cell at 240); 44px targets + rep-circle/2×2-stepper + grid are Q-039 steps 3–4, pending. Verified: app build + 380px harness (pillRight 240). Previous: 2026-06-10 (strength logger D-114 follow-ups — see the D-114 addendum: expanded-stack reorder (equipment/rest moved inside the flex-col, Done/✕ footer now last) + collapsed summary always shows RIR (`caa25da2`); the "Target: N" RIR-pill caption was a distinct DISPLAY mislabel — the 1–5 pills write `set.rir` but showed only the prescribed target, so they read as target reps — relabeled with an always-visible "RIR" heading + secondary "target N" caption (`e60877d4`); and DB-confirmed that `target_rir` is independent of reps (base RIR 3 @ 8–10 reps, 0/7 match rep-low-end; deload RIR 4 @ 4–6 reps) so the deload's 4==4 is coincidence, NOT reps bleeding into RIR — no generator change. Q-034 + POLISH §7 overflow remain closed. Previous: 2026-06-08 (strength logger set-row redesign + rest-timer fix D-114..D-115 — closes Q-034. D-114: the iOS set-row overflow (Done/✕ in the gutter on ~380px) fixed by collapse/expand, NOT the grid restructure Q-034's recipe proposed. Per-exercise active set (first incomplete; UI-only state, NOT persisted to `strength_logger_session_*` so D-108/D-109 resume is untouched) renders full controls stacked vertically with Done/✕ in a right-aligned footer; every other set collapses to a one-line summary whose `min-w-0 + truncate` + `shrink-0` action buttons kill the overflow class structurally instead of re-budgeting columns. Auto-advance: Done expands the next incomplete set. Verified at 380px via `scripts/verify-strength-row-380.mjs` (every control ≤344px vs 354px border, both states; old layout overflowed +207px — negative control proving the harness). D-115: Bench rest timer read 1:30 on set 1 / 2:30 on set 2 — rest is the gap *after* set N-1, computed render-side from the previous set's reps (`calculateRestTime`); set 0 has no previous set and hit a hardcoded `: 90` fallback. Fix: set 0 renders no rest row at all (honest — no pre-first-set rest), hardcoded default removed, sets 1+ unchanged; `startAutoRestForNextSet` unaffected (writes the `+1` key → completing set 0 still starts set 1's timer, confirmed at :459). Footgun filed: index-0 hardcoded fallback defaults are a recurring bug class, same family as D-112's failure-to-zero coercion. Both shipped client-only in commit ce83b9b0 (push origin main + cap sync ios). Previous: 2026-06-05 (cycling analyzer honesty trilogy D-111..D-113 — three-decision arc closing FTP cliff vector + NP zero-strip inflation + POWER-row intent-vs-execution contradiction. D-111: confidence floor (Tier 2 cap at 'medium') + ratchet floor in learn-fitness-profile writer — blocks overwrite ONLY when new confidence drops below prior, so a real decline at equal-or-higher confidence still goes through. D-112: preserve 0W coasting samples in the shared sensor extractor (short-circuit truthy coercion was nulling them, stripping the NP rolling window to pedaling-only and inflating NP ~20% on outdoor sweet-spot rides) + 30s Coggan startup trim. Ride 6bf694a6: NP 169→140W matching Garmin 141; 90d cycling TSS 2493→1865 (-628 phantom TSS, 25% over-count); Zwift canaries moved 2W confirming the fix targets coast-bearing outdoor rides. Detour: first edit landed inside a /* */ dead-code duplicate of normalizeSamples in compute-workout-analysis (left over from extract-to-shared-lib refactor); ANALYSIS_VERSION cache-bust diagnosed the deploy-but-no-value-change. D-113: POWER row at session-detail/build.ts:1342 swapped descriptor source from classified_type (planned intent — D-091 made it follow plan) to derived.executed_intensity (easy/moderate/hard, deterministic from IF+ftp_bins) — kills "sweet spot effort" label on 79%-of-threshold rides. classified_type left untouched for grouping/trends. Three deferred filings: Q-036 (Option B adherence field, full spec captured, open decision on secondary IF gate), Q-037 (FTP gap to Garmin 28W persists post-D-112 — hypothesis Strava power-stream smoothing, unverified), Q-038 (swim ingest unit bug + wrong analyzer routing — explicitly tabled). Previous: 2026-06-03 (iOS strength logger resume hardening D-108..D-110 — D-108 closed the cold-start gap left by D-101 (warm-resume listener doesn't fire when iOS killed the WKWebView entirely) by adding a mount-time inspection of `strength_logger_session_${today}` in localStorage; D-109 immediately superseded D-108's data-only check with a separate intent flag + AND gate (`strength_logger_open=1` AND `hasUncompletedStrengthSession()`) after observing that data-presence alone conflated with user intent — the logger force-reopened on every resume even after deliberate navigation away; D-110 closed the planned-workout-resurrection vector by clearing the localStorage session on delete (A1 — `usePlannedWorkouts.deletePlannedWorkout` captures targetDate+targetType before the supabase DELETE and `removeItem` after success) AND verifying `sourcePlannedId` still exists in the DB before hydrating, with a strict `(error == null) && (data == null)` fail-safe so network errors never wipe an in-progress session (A2 — `StrengthLogger` mount-time init). Two deferred follow-ups filed: Q-034 (strength logger viewport overflow on iOS 393pt — set row exceeds card width after D-096/D-098/D-099 widget additions; audit complete, fix recipe documented for next session, deferred because the grid restructure is ~80-100 lines of nested JSX rewrite and end-of-day shouldn't ship it under time pressure); Q-035 (actual-delete-of-planned investigation — user reports "deleting a logged strength workout also deletes the planned prescription," contrary to `useWorkouts.deleteWorkout:1685-1697` which REVERTS planned to status='planned' rather than deleting; three candidates pending repro — silent revert failure, UI staleness across hook instances per Leak B, DB trigger). Bug B Cause 2 (AuthWrapper unmount regression) remains separate scope. Five-bug silent cluster extended to include localStorage orphan: silent SQL (D-075/D-081) → runtime (D-083) → JWT (D-103) → JSONB shape (D-089) → type coercion (D-094) → SELECT-narrowing (D-105) → localStorage orphan (D-110). Previous: 2026-05-29 — run Performance tab sparkline redesign D-105..D-107 — D-105 ROUTE sparkline plots GAP-adjusted pace with per-row raw fallback (effort_adjusted_pace_sec_per_km column was already populated; SELECT just didn't pull it); D-106 TREND pool strict-intent filter + window 8 → 12 (run-only — vs_similar pool untouched, broad bucket preserved there); D-107 dropped ROUTE intent filter (post-D-105 GAP correction neutralizes effort-level per-point so intent-mixing now adds signal) + lowered HR threshold >=3 → >=2 + restored times_run as the route-header label source (matches iOS). Sparkline architecture carried forward: TREND uses strict-intent (D-106), ROUTE uses GAP without intent filter (D-105 + D-107), vs_similar uses broad bucket + pace-proximity (D-038 unchanged) — three different questions, three different filter posture, do not unify. Previous: 2026-05-28 — strength workstream D-094..D-104 — D-104 display-bug tail added the missing SessionNarrative render on the strength Performance tab; pure tree-omission, the data was reaching the client correctly via session_detail_v1.narrative_text from D-102+D-103 but the strength branch of MobileSummary just never invoked SessionNarrative the way the endurance branch does. Earlier in the batch: performance tab fixes + logger UX revamp + analyzer fixes + spec doc. D-094 Planned column shows weight × reps @ RIR; D-095 PREVIOUS column shows last session's actual; D-096 "Same as set 1" carry-forward; D-097 previous-session autofill; D-098 weight stepper; D-099 RIR pill slider replacing drawer; D-100 visible auto-rest countdown + Web Audio tone; D-101 iOS resume reopens logger (closes Bug B Cause 1); D-102 INSIGHTS three-piece — narrative lift + 4-sentence cap + strength_fact_packet_v1; D-103 cross-sport silent 401 bug — removed user-JWT gate from strength + swim analyzers (same class as D-075/D-081/D-083). docs/STRENGTH-ANALYSIS.md spec saved. Verified post-D-103 via REST recompute on May 18 (unplanned, 4-sentence narrative) + March 30 (planned, 3-sentence narrative + fully-populated fact packet). Previous: 2026-05-27 — cycling analyzer narrative + display polish D-089..D-093 — turned the analyzer into a fundamentally different product than the prior morning. D-089: per-interval execution table reaches the UI for the first time on cycling. Pre-fix the cycling analyzer wrote `interval_breakdown` as a bare array; every consumer in the codebase (session_detail/build.ts, workout-detail enrichment, cycling ai-summary, generate-training-context, fact-packet builder) expected the run-aligned `{ available, intervals }` wrapped shape. The bare array silently missed all of them — including cycling's OWN ai-summary, which had been emitting null interval narratives since it landed. Wrapped at both write sites + added `avg_power_watts` field alias for the sport-neutral session_detail reader + per-type interval numbering (work N / recovery N) instead of global indexing. session_detail/build.ts taught to derive `planned_pace_display` from `planned_power_range_lower/upper` ("150-167 W" subtitle) and to fall back to `power_adherence_percent` for the adherence badge — sport-neutral changes, no cycling branch. D-090: recoveries restored to the display. Pre-fix the cycling interval-breakdown filter required `i.power_range`, dropping rest segments without targets; a 2x15 sweet-spot ride showed 4 rows of 6. Filter extended to also accept role/kind matching `/recover|rest/`; recoveries emit with explicit-null adherence fields (`power_adherence_percent`, `performance_score`, `planned_power_range_*`) so session_detail short-circuits the adherence badge and the weighted session score skips them. Recoveries render label/duration/watts/HR, no badge. D-091: plan_intent derivation from layered signals. Pre-fix the cycling fact-packet read plan_intent only from `plannedWorkout.workout_type ?? plannedWorkout.type` — for every cycling planned row those held the discipline value `'ride'`, which normalizePlanIntent maps to null (per D-084). Net effect: plan_intent was null on EVERY structured cycling session in the system. The fallback classifier ran IF/VI-only and labelled an 88-94% FTP sweet-spot ride as `'threshold'` (IF 0.83), so the LLM led with "sub-threshold effort" instead of the prescribed intent. New `derivePlanIntentCycling(plannedWorkout)` with precedence: workout_type → tags (canonical intent tag like `'sweet_spot'`) → steps_preset (token prefix like `bike_ss_*` / `bike_thr_*` / `bike_vo2_*` / etc.). Free-text name/description deliberately NOT used — too noisy. classified_type cascade flips automatically (the line-228 short-circuit already exists). D-092: STRUCTURED PLANNED MODE prompt rule + TREND suppression. New hard rule that fires when `interval_summary != null` AND `plan_intent ∈ {sweet_spot, threshold, vo2, tempo, anaerobic, neuromuscular, race_prep}`; explicitly overrides the general LEDE rule and the HARD CONSTRAINT at ai-summary.ts:399. Requires lede to cover target-range adherence + completion count + HR response. NP becomes one trailing sentence. Bans leading with trend / vs-similar / PR on structured sessions. Data fix to interval_summary: surfaces `planned_power_range_w` + `power_adherence_pct` + `in_target_range` per work interval; fixes a phantom-key bug where the prior `avg_power_w` field was always null (cycling intervals carry `actual_power_w` / `avg_power_watts` per D-089). `pickCyclingTrendSeries` (session-detail/build.ts:1052) now returns null when `pwr20_trend_v1` has <3 type-filtered points AND the session has plan_intent in the structured set — sweet-spot rides compared to an aggregate trend across endurance + recovery + sweet-spot is not a meaningful signal; show no TREND rather than a misleading one. Unplanned rides keep the np_trend fallback. D-093: 4-sentence cap on clean structured executions. Pre-D-093 yesterday's structured-session narrative ran 7 sentences with HR mentioned twice and fatigue mentioned twice — the LLM padded with "this kind of work is exactly what …" closers on a clean ride that needed nothing more to say. New `interval_summary.clean_execution: boolean` signals when every work interval landed ≥95% power_adherence_pct. CLEAN-EXECUTION CAP sub-rule inside STRUCTURED PLANNED MODE: when clean_execution is true, output EXACTLY 4 sentences — lede (target adherence + wattage + opening HR) / one physiological observation / one fatigue/load context / one forward-looking. Explicit cut list, "skip S3 and write 3 sentences when there's no load signal worth saying." Q-033 filed deferred: "natural power variation from the terrain" phrasing for Zwift/VirtualRide; cosmetic vocabulary swap rather than correctness issue. Verified end-to-end against today's sweet spot ride (workout f9fb690b — Strava Zwift VirtualRide, 2x15 SS at 150-167 W): per-interval table renders 6 rows with recoveries; narrative leads "You held the 150–167 W sweet-spot target across both 15-min blocks at 166 W and 162 W…"; TREND suppressed (verified via direct Deno call to pickCyclingTrendSeries); sentence count 4. Five commits 9cdced4e → 806210af. Net result: cycling analyzer at parity with running for structured planned sessions for the first time. Previous: five-item audit batch D-085..D-087 — FTP→cycling-power-zones flow routed through resolveCurrentFtp like every other consumer (was on hardcoded 200W for athletes with learned-only FTP); group ride anchor false 'wednesday' fallback removed (main wizard flow correct for any day); scaledWeeklyTSS validator now reads endurance-adjusted hours (week-builder + plan_contract were already correct; validator was the holdout). Two clean audit verdicts: cycling session naming (Long/Easy/Bike-Threshold/Sweet-Spot/VO2/Tempo/Brick distinct), brick session structure (matches CYCLING-PROTOCOL §4.3/§5.7). NO_CACHE=1 matrix 486/486 post-batch. Earlier today: cycling-attach cascade D-074..D-084 — May 23 Strava ride investigation that uncovered eleven layered bugs: plans.start_date phantom column (reverted), environment:'pool' default on non-swim planned rows, two silent PostgREST 42703 errors on phantom column names in workout-detail + cycling analyzer planned_workouts SELECTs (affected every linked workout for the second one — all sports), "unplanned route" LLM hallucination, recompute preservation re-serving stale text, cycling analyzer missing recomputed_at write (cache-bust gap), isUnplanned ReferenceError silently killing every cycling LLM call since D-046, classified_type 'unknown' leak in POWER row, Duration chip showing delta instead of absolute time. Also: FTP "Edit to override" UI fix + FTP-COLD-START-SPEC saved (D-077). LLM diagnostics instrumentation kept in (D-082) for future dormant-exception hunting. Full narrative in the new Solid entry below. Previous: overnight batch D-064..D-069 — 486/486 plan-generation matrix pass. Six fixes shipped sequentially, each matrix-verified. D-064 swim placed on rest_day silently dropped; D-065 Z3 jargon strip in downgrade wrapper; D-066 strength placed on rest_day silently dropped + load balancer rest_days awareness; D-067 plan-matrix harness recovery detector switched to peak instead of median (1:1 loading false positives); D-068 WoW TSS ramp ceiling calibrated for distance-aware tri (0.20→0.24 half-IM, new 0.30 full-IM); D-069 first_race base-phase quality run emits sweetSpotRun not intervalRun. Five regression tests in week-optimizer.anchor-contract.test.ts. Full per-fix narrative in scripts/plan-test-output/overnight-report.md. Previous: D-036 — run aerobic decoupling computed on grade-adjusted pace. GAP enrichment lifted to top of `analyze-running-workout/index.ts` via new idempotent `enrichSamplesWithGAP` helper; both pace-adherence and HR analyzer consume the same enriched series. Sample-level decoupling (warmup-skipped, 20-min minimum) replaces segment-level value for runs; `derived.decoupling_basis: 'gap' | 'raw' | null` reports which series fed the ratio. New AEROBIC DECOUPLING (RUN) prompt rule with two branches: GAP basis = real fitness signal with excellent/good/moderate/high translation; raw basis = inconclusive, do not claim fitness. Contract surface: `session_detail_v1.classification.decoupling: { pct, basis, assessment } | null`. Cycling unchanged. Third spec shipped 2026-05-23: D-034 / D-035 / D-036, same root correction — stop reading signals through raw pace, stop asserting what can't actually be known. Also same day: D-035 — unlinked workouts return null adherence, not synthesized targets or hardcoded defaults. Run analyzer's fake-target synthesis deleted; cycling's 0% / swim's 100% defaults nulled; new `classification.is_unplanned` flag on `session_detail_v1`; LLM input drops prescribed-range signals and UNPLANNED MODE prompt rule fires with terrain-aware variance reading. Swim's hardcoded `duration_adherence: 100` killed for both linked and unlinked. Also same day: D-034 Performance — `is_mixed_effort` variance gate + Bug A label hygiene + cycling-B parity. Server-side variance flag on `workout_analysis.session_state_v1.glance.is_mixed_effort` computed from GAP-corrected pace CV (conservative raw-only-on-flat policy) + interval_execution + detected-intervals + plan-intent. vs_similar pool filter excludes mixed-effort rows from steady comparisons; cross-workout pace comparisons prefer GAP when both rows have it; LLM input drops `vs_similar` and adds `interval_summary` for mixed-effort sessions so INSIGHTS interprets per-interval execution instead of comparing whole-workout averages to easy history. Plan intent never overwritten — `classified_type_variance_override` flag carries the signal for pool filters. Bug A: analyzer no longer emits `'Overall session'` literal; unlinked sessions with ≥2 measured breakdown intervals render per-interval. POLISH-PUNCH-LIST.md line 242 closed. Previous: D-033 Phase 1 run pace feedback loop; D-032 Phase 0 Arc channel; D-031 rebuild-mode lerp throttle + peak-of-base floor; D-030 swim CSS-kill + per-step drill equipment; D-029 swim arc research-backed fixes; D-028 cycling arc Phase 1 bundle; D-027 validator within-phase-aware + Bundle A merge fix + D-026 week-builder canonical.)

---

## Solid (don't re-litigate)

Verified-working architecture and fixes. If you think one of these is broken, the bug is probably elsewhere — read the verification method before changing anything.

### STATE v2 per-discipline trend model — shared primitive + adapters + hybrid fallback (D-148, 2026-06-14)

The STATE screen's PERFORMANCE section. **Pure model in `src/lib/state-trend/`** (runs client or server), wired via `useStateTrends` → `StatePerformanceSection` → `StateTab` (between AERO and SIGNAL). Spec: `docs/SPEC-state-screen-v2-performance.md`.

- **One shared primitive** `classifyTrend` (window + 3-layer noise guard + **staleness gate**) classifies every discipline → Improving / Holding / Sliding / Needs data. Each discipline is a thin adapter feeding a dated metric series — **adding run/swim later = a new adapter, not new scaffolding.**
- **Sources:** strength = `exercise_log.estimated_1rm` per lift (roll-up follows primary lifts); bike = `pwr20_trend_v1` via `pickBestPwr20` (densest in-window series, NOT latest); run = `route_progress_metrics` GAP pace gated on **`workout_analysis.classified_type='easy'`** (NOT the null `workout_intent`) + a 150–750 s/km plausibility band; swim = `workout_facts.pace_per_100m` + a 40–240 plausibility band. Run/swim are `lowerIsBetter`.
- **Hybrid fallback:** performance where it has a verdict, else **weekly adherence** (7d). Co-equal-ready (`AdherenceState.context` empty until SPEC-session-context Layer 1; `DISPLAY_MODE` one-spot flip).
- **Staleness gate (load-bearing):** a verdict whose newest qualifying point is older than `freshnessDays` (strength 14 / bike 21 / run 14 / swim 10) **decays to `needs_data`** (flag `stale`). Window membership ≠ recency — without this, a discipline shows its last-known trend after training stops. This is why swim correctly reads needs_data (last swim 13d > 10d), not a stale "improving".
- **Headline** `synthesizeHeadline` ("Building — strength up, run sliding") gates untrusted disciplines (`{swim}` until Q-038) and is **neutral when empty** ("No trend yet"). **Off-plan is NOT synthesized here** — it stays on the server `intent_summary` (D-147).
- **⚠ Thresholds are sign-off-gated** (like the 500 floor): strength +2.5/−2.0/min4/14d; bike ±2.0/min3/21d; run ±2.0/min4/14d; swim ±1.5/min3/10d **provisional+gated until Q-038**. Don't retune without sign-off.
- **Scope:** display/synthesis only — **does NOT feed prescription** (adapt-plan/suggested_rir/auto-attach-planned/D-139 untouched).
- **Known-thin (filed, not bugs in this layer):** RPM `workout_intent` null at source (`compute-facts:930`); strength canonical-name split + per-lift min-4; swim Q-038; vo2 carries no pwr20 by design (no sustained 20-min power).
- **Verification:** `tsc -p tsconfig.app.json` + `npm run build` clean; read-only `scripts/state-trend-{audit,diagnose,trace}.mjs` (untracked) run the shipped model against production data. Commits `2907cfdf`→`be2edd7d`.

### Athlete-State Spine — cached single source read by STATE, session-detail, coach (D-150/D-151, 2026-06-14)

The fitness verdict is computed **once** and read everywhere — closes the screen-by-screen re-derivation that let contradictions survive.

- **One assembler:** `assembleStateTrends` (`_shared/state-trend/assemble.ts`) — pure, called identically by the client hook `useStateTrends` AND by `compute-snapshot`. Same model + same fetch windows (`STATE_TREND_WINDOWS`) → **cached == live is structural, not coincidental** (one code path). Verified 16/16 fields on real data.
- **Cache home:** `athlete_snapshot.state_trends_v1` (JSONB), written per-ingest for the current week only (verdict is as-of-now; historical rows null, unread). Carries per-discipline `{verdict, pctChange, provisional}` + the bike power/efficiency dual.
- **Readers wired:** STATE rows; session-detail `discipline_trend` (via `workout-detail` → `build.ts` → `MobileSummary`, all four disciplines); coach `fitness_direction` = `rollupFitnessDirection(state_trends_v1)` — **replaced** the old response-model derivation (not alongside). Coach narrative names each discipline with confidence framing (provisional + adherence co-explanation).
- **Bike-fitness engine** (`bike-fitness.ts`): terrain-binned 20-min power + HR-at-power efficiency, HR band from `resolveZoneBand` seam (Coggan default, personal zones drop-in). Per-ride datapoint also on the cycling session-detail.
- **pctChange:** within-window endpoint-smoothed first-pair-vs-last-pair delta (NOT a chronic baseline); raw sign = metric movement, verdict applies `lowerIsBetter`. Conservative at n=3 (endpoint overlap). Spec: `SPEC-athlete-state-spine.md`.
- **⚠ Surfaces NOT yet reading the spine — "spine done" ≠ "every surface reads it":**
  - **Partial:** Training Baselines + Athletic Record — surface spine-*derived* suggestions (suggest-with-confirm, D-150 Step 1), but do **not** fully read the spine.
  - **Not wired:** Goals (untouched); plan builder (**Step 4, GATED** — changes prescription, separate sign-off).
  - **Loop framing:** the **execution → spine → display** arc is wired (STATE / session-detail / coach); the **spine → baselines/record/plan** feedback arc is **OPEN**. "Spine done" = the core engine + read surfaces, NOT the full loop.
- **Scope:** display/synthesis only — **does NOT feed prescription.**
- **Verification:** deno check + tsc clean; `compute-snapshot` / `coach` (v39) / `workout-detail` deployed; cached==live (16/16) + coach before/after (`stable`→`mixed`) verified live. Commits `efbeee3b`→`c33ef732`.
- **Component duties (who reads what, from the ONE cache — stated, not aspirational):**
  - **STATE screen** (`useStateTrends` → `StatePerformanceSection`): duty = **the macro per-discipline verdict** — "am I getting fitter, per discipline." Reads `state_trends_v1` (live-computed client-side via the shared assembler; identical to the cached server copy). Shows the four discipline trends + headline + bike Power·Efficiency dual. **Aggregate, glanceable.**
  - **Session-detail / Performance** (`workout-detail` → `build.ts` → `MobileSummary`): duty = **this session's place in the discipline trend** — the per-session screen surfaces `session_detail_v1.discipline_trend` (the session's discipline verdict, read from the SAME cache, not re-derived) + the bike per-ride HR-at-power datapoint. **One session against its discipline's trend.**
  - **Coach** (`coach`): duty = **describe the verdict, don't re-derive** — `fitness_direction` = spine roll-up; narrative names each discipline with confidence framing.
  - **SPECCED-NOT-BUILT (the honest gaps):** the **per-session +/- engine** (`SPEC-per-session-performance-engine.md`, Reads 1–3) — the duty of saying "this session was better/worse than your comparable sessions, terrain/confound-controlled" — is **not built** (Read-3 blocked on weather/route data, Q-055). The **STATE headline** synthesis (`SPEC-state-headline.md`) and the **adherence↔performance bridge** (`SPEC-adherence-performance-bridge.md`) are **specced-not-built**. So today's division is: STATE = macro verdict, session-detail = per-session *context* (the discipline trend), NOT yet per-session *judgment* (that's the unbuilt engine).

### Tier-1 honesty sweep — data-integrity write-side + narrative anti-speculation (D-152, 2026-06-14)

First sweep off the whole-board Tier map. Correctness/honesty bugs that mislead the athlete, sequenced **data-integrity before voice**. No prescription touched.

- **Q-054 run pace (`compute-facts`):** the live `avgHr=0 → GAP=0` collapse fixed (zero-not-null class, D-112/D-115 family) — HR-less run now writes `GAP=null`, never `0`. Write-side plausibility clamp (150–750 s/km) so garbage provider pace never persists. 4 stale-residue rows recomputed clean (0/118 OOB). Filing's "GPS-dropout" premise was wrong — it was stale residue + one live zero-blind bug.
- **T2 `overall.avg_power/avg_hr=0`:** verified NOT firing (0/40 rides); hardened `buildRideFacts` with the same first-positive treatment → `compute-facts` is now **zero-safe across both disciplines** (run + ride).
- **T1 cycling narrative anti-speculation (`_shared/cycling-v1/ai-summary.ts`):** extended `validateClaimsGrounded` (holding/responding/consolidating, fitness-subject-anchored) — extends an EXISTING retry guard, no new one (honors §7). Anti-speculation prompt rule + targeted limiter-projection line (**prompt-only — retry loop untouched**). Result: **11/12 reliably clean**; 1 ride retains an intermittent soft projective tail (Q-058, documented edge).
- **Verification:** `compute-facts` + `analyze-cycling-workout` deployed; 4 runs + 12 rides recomputed against real data; `deno test` 20/0 of the grounding/lede/jargon guards (3 pre-existing trend-test failures unrelated). Commit `9c0f73d8` (Q-054/T2) + the T1 commit.
- **Tickets filed:** Q-057 (NULL-`workout_id` write-path hygiene), Q-058 (T1 soft-tail edge).

### STATE one-verdict-per-axis sweep — load de-frag · #4 glance · swim de-conflation (D-153–D-156, 2026-06-14/15)

Correctness sweep applying the spine's "one truth, in the right slot" discipline to the STATE surfaces. All display-only; no prescription touched.

- **Load (D-153):** the volume verdict is single-sourced via `acwrVolumeLabel` across **3 surfaces** (LoadBar home/week, STATE LoadBar, CoachWeek `SnapshotLoadBar`); `readiness_label` reads readiness only. The "HIGH LOAD while fresh" leak (readiness chip reading `load_status`) is closed. **Verify:** headline matches the gauge band on all three; the readiness chip never shows a load word.
- **#4 glance (D-154):** deterministic `buildLoadHeadline` (state · fitness · observation, observation never a prescription) + full narrative collapsed behind "open for more"; narrative leads credit-first, no raw "X of Y" (coach v44). `acwrVolumeLabel` is the shared band source.
- **Prescription boundary (D-155):** the narrative may **name** planned key sessions (describe), never **add/change** them (gated). Lexical "add" ban + pin-to-FACTS-key-sessions in `narrativePrompt`. The durable describe-vs-change line for the coach voice.
- **Swim (D-156):** adherence de-conflated from the trend slot — no-trend disciplines show `needs data · 0/2 planned`; judgment words removed at the `adherence.ts` source (so they can't re-leak into any slot). Same family as load: the spine model was right, the render mislabeled.
- Client live-assembled (`useStateTrends`); coach/compute-snapshot deployed. Commits `fa4e1813`→`81c88047`.

### Apple Health (HealthKit) swim integration — connected on-device, dedup-gated (D-157, 2026-06-14)

A second swim source behind a dedup gate. Same-swim Strava+HealthKit → **one merged workout** (best-field-from-each; 60s start + sport + ±10% distance). Native `HealthKitPlugin.swift` enriches pool length / strokes / HR. Platform-split "Connect Apple Health" toggle (off by default; web shows an iOS-app pointer). Verified connected + merging on device. Scope: ingest/display + native read only — no prescription.

### Swim-native render — Layer 2 DONE on BOTH surfaces (D-159/D-160, 2026-06-15)

Swims no longer fall through the land/endurance render on either tab. **Two surfaces, fixed separately** (don't conflate them):
- **Performance tab** (`MobileSummary` → `EnduranceIntervalTable`→`PoolSwimOverall`): swim-native since a44f9b2d (pace/100, distance, avg HR, HR sparkline; land rows guarded in `build.ts` at `:1502`/`:1546`, land pace nulled). D-160 added the **distance/duration headline**, the **trend-sign display fix** (`verdictSignedPct` — the engine's raw `pctChange` is correct, `lowerIsBetter` flips only the verdict; display now signs by verdict so "↑ improving +34.6%"), the **Apple-Health nudge gate** (`source==='healthkit' || pool_length>0`) + **relocation to the bottom**, and a **rich-detail block** (pool/lengths/strokes from the workout row) that fills the space the pool-swim-suppressed narrative leaves.
- **Details tab** (`CompletedTab`): D-159 made swim detection **type-based** (`isSwimType = resolvedWorkoutType==='swim'`) instead of `workoutData.swim_data` (NULL for Strava → was leaking the mph speed chart / mile splits / map / Grade-VAM-Cadence row). Applied at the readout-grid gate, the pool-swim viewer short-circuit, and the splits block. HR Zones untouched (data-gated, swim-safe).

**⚠ Load-bearing:** `swim_data` is **NULL for Strava swims** — never key swim detection off it; use the workout TYPE (`resolvedWorkoutType` / `sd.type`). This is the D-159 root cause and a recurring footgun (same class as Q-054/D-112 absent-value-selects-wrong-branch). **Trend-sign:** the fix is **display-only** (`verdictSignedPct`); do NOT "correct" the sign in `classify.ts` — `pctChange` is deliberately raw (comment at `classify.ts:76`). Pool-swim narrative is now **RE-ENABLED** (D-167) — the suppression existed only while the analyzer was broken; it now emits clean plain prose with the authoritative pace. Swim display **unit** is locale/plan-inferred pending real pool length → **Q-059**. Verification: `npm run build` clean; on-device verify owed against the June 15 swim.

**Pool length now comes from the athlete, not HealthKit (D-162):** the post-workout feedback popup (`PostWorkoutFeedback`, triggered via `AppLayout` + `check-feedback-needed`) handles swims — feel + RPE (same as run/ride) + **"What pool?"** (writes `pool_length` m + derives `number_of_active_lengths`) + **planned-aware equipment confirmation** (reads `planned_workouts.computed.steps[].equipment`; per-step Yes/No/Skip; unplanned → multi-select). Equipment stored in `workout_metadata.swim_steps_equipment_confirmed` / `swim_equipment_unplanned`; a `· some sets with fins` note shows on the Performance tab. The **HealthKit native enrichment is paused** (Q-060 — friction not worth it for pool_length alone; revisit for SWOLF/per-length). Finned-step pace **exclusion** from the trend is captured-but-not-built (**Q-061**).

### Swim sweep consolidation — D-159 → D-172 (2026-06-15 → 06-16, the full arc)

The single biggest swim reshape: from "swims render land junk + wrong numbers" to a coherent swim-native experience across home / Details / Performance — then a source-tier model that makes the messy multi-source story honest. Builds on D-156/D-157. The thread: **single-source every number so the analyzer can't re-diverge.**

- **Numbers (the recurring class — analyzer kept recomputing independently and drifting):**
  - **Pace + HR** are single-sourced via `_shared/swim/swim-pace.ts` `swimPacePer100Seconds()` (the formula) fed by `_shared/swim/swim-scalars.ts` `resolveSwimScalars()` (the **inputs** — D-182). Called by `build.ts` (Performance card), `analyze-swim-workout` (narrative), AND `workout-detail` `display_metrics` (Details tab). **All three read 2:00/100yd + HR 119.** D-167 shared the *formula* but the card had re-diverged to **3:03/125** because it fed `computed.overall` (`duration_s_moving=2202s` — larger than elapsed 2100s, physically impossible) while the narrative fed the raw `workouts` columns; D-182 pins all three to the **raw-column scalar** (swim-gated; non-swims keep `computed.overall`). Do NOT reintroduce an independent pace calc OR read `computed.overall`/`computed.analysis.swim` for swim pace/HR.
  - **Duration adherence** (D-163) uses **elapsed** (not moving) vs the planned total — raw completion ratio (>100% = did more), clamped to 100 only in the execution blend. `session_elapsed_s` surfaced; the swim block shows elapsed, pace stays on moving.
  - **Pool length** flows from the athlete (D-162 popup) through every reader: `user_corrected_pool_length_m` (resolver tier-1) + `pool_length_m` (analyzer, via `resolvePoolLength`) + `pool_length` (display) + `pool_unit`. The 1:60 pace **formatter** bug is fixed at the shared `formatSwimPace` (round-first); every surface routes through it (D-166). HealthKit `number_of_active_lengths` derived = round(distance ÷ pool) (D-161).
- **Render (one design system):** the swim Performance card is a **dark sibling of the Details READOUTS card** (D-166) — week label + discipline trend inside the card, distance/duration headline, Pace/HR/Pool/Lengths in one grid, dot-pill adherence (no duplicate top header), `· fins used` tag. Details tab is type-detected (D-159, never off `swim_data`), land rows hidden, pool-swim gap tightened. The planned-swim **bottom sheet** (D-165) gained Copy-for-FORM + pool selector + Apple-Watch placeholder.
- **Narrative (D-167/D-168):** pool-swim INSIGHTS re-enabled after fixing the analyzer — plain-prose, unit-consistent (inherits the UI's unit, no metres leak), no invented derived stats. **D-168** then matched it to the run/ride voice (**second person**, "You covered…") and built it around the data swim actually has — **HR** + **RPE/feel** (D-162, threaded from `workouts.rpe`/`feeling`) + the **work:rest split** (moving vs elapsed, observed not diagnosed). Verified on real recompute.
- **Source tiers + honest Connections (D-169/D-172):** swim richness is entirely source-dependent (Strava gated at 10 users → can't be the universal pipe; Garmin the only LIVE rich source; Apple Watch in-place; FORM thin-via-Strava or MODEST-via-Apple-Health — pool+strokes, NOT per-length SWOLF). `src/lib/swim-source-tier.ts` `deriveSwimTier()` is the pure single-source tier/badge fn (verified vs 24 real swims). `SwimSourceMatrix` (Connections, below the account cards + source preference) shows each source's honest capability + a "richest version, never a duplicate" guarantee. **Dedup verified:** source preference enforced at the webhooks (garmin↔strava) + all swim ingests route through `ingest-activity`'s `mergeSameSwimIfExists`. **"Richest data" = three layers:** matrix informs · preference chooses · merge protects. Spec: `SPEC-swim-source-tiers.md`.
- **⚠ The "never a duplicate" guarantee is END-STATE, gated on Q-060:** Garmin↔Strava dedup is LIVE (real today); the HealthKit/FORM richest-merge is **Q-060 (unbuilt)** — until it ships, connecting Apple Health enables the connection but FORM/Watch swims don't flow or merge, so the guarantee + the FORM-via-Apple-Health "Connect" badge are **forward-looking for the Apple Health pipe**. Q-060 is the dependency that makes the UI promise true end-to-end. (Residual: the 60s merge window misses when Strava's integer-minute start differs >60s from Garmin's.)
- **⚠ FOOTGUN (the load-bearing invariant — read before touching any swim number):** swim pace has ONE formula (`_shared/swim/swim-pace.ts` `swimPacePer100Seconds()`) fed by ONE input source (`_shared/swim/swim-scalars.ts` `resolveSwimScalars()` — the **raw `workouts` columns**, D-182), HR comes from that same scalar, and pool length has ONE source (`resolvePoolLength()`). **D-182 is the lesson promoted from "one number, one formula" to "one number, one SOURCE, every surface":** sharing the formula isn't enough — `computed.overall` / `computed.analysis.swim` are sample-derived and NOT authoritative for swims (observed `duration_s_moving=2202s` > elapsed 2100s), so feeding the shared formula from them still re-diverges (the D-156 / D-164 / D-167 family — 2:11-vs-2:00, stale `computed.analysis.swim`, 3:03-vs-2:00 card, metres-vs-yards leaks). There is no valid reason to recompute swim pace inline OR to read `computed.overall` for a swim scalar; call the shared resolver + fn, swim-gated. Same for swim detection: never key off `swim_data` (NULL for Strava) — use TYPE (`resolvedWorkoutType` / `sd.type`).
- **→ Q-061 — partially addressed (NARRATIVE half done, D-183); TREND half still UNBUILT:** the swim **narrative** now reads the captured equipment and honestly flags fin-assisted pace (direction-only) — `analyze-swim-workout` is the **first reader** of `swim_steps_equipment_confirmed` outside the client. **But the TREND substrate is still fin-blind:** `compute-facts` `pace_per_100m` → `workout_facts` → snapshot → `state-trend/swim` still blends finned sets, unflagged. So the *displayed prose* is now honest while the *fitness signal* is not — that signal-level exclusion is the remaining Q-061. Session-level flag-and-down-weight at the trend is buildable now (full per-step removal still needs per-LENGTH pace — Q-038/Tier-B). Data dependency satisfied (D-162, verified `[{snorkel,used:true},{fins,used:true}]`).
- **SHIPPED since (D-173→D-183):** manual swim entry (D-174) · Garmin per-discipline **swim override** (D-173 — verification owed on a real Garmin+Strava swim) · narrative now **interprets** RPE×HR coherence + work:rest with an honest downward read (D-177) · manual-swim **whole-app awareness** (D-178 — `recompute-workout` refreshes snapshot + caches at its tail; helps EVERY recompute) · **work:rest first-class in the narrative LEAD** (D-179 — was relegated to a trailing bullet; displayed lead = `insights[0]`, so a signal must land in the first observation to show) · **swim pace + HR single-sourced card==narrative** to the raw-column scalar (D-182 — card had re-diverged to 3:03/125 via `computed.overall`; now both read 2:00/119 via `resolveSwimScalars`) · **swim narrative interpretation fix** (D-183 — HR read against the athlete's zones from the **average not the peak** (`configured_hr_zones`→Friel-%LTHR fallback; neutral when no threshold on file), so RPE 3 + 119 bpm reads **coherent-easy** instead of the old manufactured "harder than perceived / peaks near 152"; AND narrative **flags fin-assisted pace direction-only**. Verified on the June 15 swim. **Held:** no broader swim-narrative changes until the user's "Honest Swim Inference Boundaries" doc lands).
- **→ NEXT (non-Xcode, any session):** **D-176 work:rest as a CARD METRIC** — D-179 put work:rest in the narrative lead; D-176 is the still-unbuilt **card readout + rest-fraction trend** (same-source, comparable sessions) = the hybrid athlete's progress signal ("resting less for the same distance"). Universal (every source has moving+elapsed), no Q-060 dep. Single-source the calc off `resolveSwimScalars`.
- **→ The "Apple Watch week" (native / Xcode-iterative) — see the consolidated work order immediately below.**
- **Open swim tickets:** Q-038 (ingest reliability), Q-059 (canonical display unit — narrative inherits the UI's), **Q-060 (HealthKit ingest + 60s merge — the dependency that makes the "never a duplicate" guarantee true end-to-end for the Apple Health pipe; re-justified by SWOLF + no-doubles, no longer just pool length)**, Q-061 (finned-pace exclusion — narrative flag DONE D-183, trend-level exclusion still unbuilt).

### Apple Watch week — consolidated work order (native / Xcode-iterative; START HERE the next Apple session)

The swim arc D-159 → D-178 is complete for what's buildable headless. What's left is **one native campaign** — do it as a unit in Xcode, not reassembled from scattered D-entries. All three need build-run-test loops a headless session can't do.

1. **Q-060 — HealthKit ingest + best-field merge (BUILD then TEST).** Two-layer dedup:
   - **(a) Inside-HealthKit source-pick:** when the same swim has multiple HealthKit writers, prefer the richer SOURCE — Apple Watch native per-length/SWOLF beats FORM's summary-only write (discriminate via `HKSource`).
   - **(b) HealthKit↔Strava merge:** richest fields WIN by **field-richness precedence, NOT arrival order** — the rich copy ENRICHES the existing Strava row (fills pool/strokes/SWOLF/per-length), never rejected as a dup. Extends `mergeSameSwimIfExists` (today Strava+HealthKit-only, healthkit-side enrichment, 60s window + ±10% — and the integer-minute-vs-seconds start gap is a known miss to widen).
   - **⚠ GUARDRAIL:** do NOT claim "richest data verified" or flip the matrix to "+SWOLF live" until the merge is BUILT **and** run on a real **FORM + Apple Watch + Strava** swim. Build-THEN-test, never test-only. This is what makes the matrix's "never a duplicate" / "+SWOLF" promise true end-to-end for the Apple Health pipe (the D-172 guarantee's gating dependency).
2. **WorkoutKit send-to-Apple-Watch (D-175).** Compose a custom **POOL** swim `WorkoutPlan` from `planned_workouts.computed.steps` (warmup + repeatable work/recovery blocks + cooldown); schedule via `WorkoutScheduler`. **Time-based rests only** (WorkoutKit can't do Garmin's manual-advance — send 0:21 as-is). **Pool only** — open water unsupported by custom workouts, gate it. **watchOS 11 target.** Enable the disabled "Send to Apple Watch" button (D-165 bottom sheet) once wired. (Buildable per WWDC24 — supersedes the Q-062 "weak support" deferral.)
3. **Card badge wiring + FORM→Apple nudge.** `deriveSwimTier()` (`src/lib/swim-source-tier.ts`, verified vs 24 real swims) → render the source/tier badge on the swim card (Performance + Details). The **FORM→Apple nudge** (`isFormViaStrava` flag ready): quiet card tag → Connections, **ONE GLOBAL dismiss** (not per-workout — else the 10+ existing FORM-via-Strava swims each nag), clears on Apple Health connect, **retroactive** (keys off badge, not ingest date). (This one is mostly client, but pairs naturally with the Apple work since the nudge drives the Q-060 opt-in.)

### Whole-board Tier map (do-now / do-next / gated — captured 2026-06-14 for the next session)

The priority order weighed: **mislead-the-athlete > polish · Monday's live build-week data · unblock-leverage · gated-prescription-flagged-separately · effort:payoff.** Full punch-list section: §8 + the per-section items.

- **Tier 1 — honesty bugs, Monday-visible — ✅ DONE (D-152):** Q-054, T2, T1.
- **Tier 2 — cheap, read-only, high-unblock — NEXT:** §5 baseline→zones flow audit (surfaces the deadlift-under/bench-over class); Q-053 FTP-ingest *decision* (the implementation is gated).
- **Tier 3 — complete the spine:** Load/BODY fold-in — **label half DONE (D-153: one volume verdict via `acwrVolumeLabel`, single-sourced across 3 surfaces, readiness ≠ load); the D-146/147 ACWR-compute fold-into-`state_trends_v1` (verdicts as spine outputs) REMAINS.** Q-052 load-threshold remainder.
- **Tier 4 — spine follow-on surfaces (build once observed live):** STATE headline; per-session engine Reads 1–2; §6 adaptive-intent / §7 interference surfacing.
- **🔒 Tier 5 — GATED (prescription, explicit sign-off, NOT auto-sequenced):** Step 4 (plan builder reads spine); Step 5 (autoregulation) + adherence↔perf adjust-action; Q-053 FTP-ingest implementation; `scaledWeeklyTSS` over-prescription; §6 cadence prescription.
- **⬜ Tier 6 — deep / blocked:** iOS auth-lifecycle + Bug A/B (blocked on repro — Monday device use may produce it); Q-034 iOS logger overflow; Consolidated-mode; §5 apply §4.21 to other generators.
- **→ The throughline behind Tier-3→Tier-5 — AUTOREGULATION FLAGGING:** tonight's load-fix (D-153) makes body-response a clean, properly-labeled signal on its own axis → that clean signal is the input for flagging when the plan doesn't fit the athlete. **Detect/flag is buildable; the adjust-action stays gated (Step 5).** Full arc (the foundation→feature throughline, the three detection patterns, the detect-vs-adjust boundary): `SPEC-adherence-performance-bridge.md`. Don't re-derive it here — that SPEC is the source.

### Strength logger draft persistence — identity-scoped, gated on Done (D-132, 2026-06-11)

The logger's in-progress draft lives in `localStorage`, restored on cold mount (survives hard quit). After D-132 it is safe against cross-workout bleed:
- **Key (`computeSessionKey`):** `strength_logger_session_${performedDate}_${plannedId || 'adhoc'}` — scoped by viewing-date AND workout identity. (Pre-D-132 it was date-only; that legacy key is read as a fallback but still identity-guarded, and a one-time mount cleanup removes phantom/stale legacy keys.)
- **Restore identity guard (`:1499`):** rehydrate ONLY if `saved.sourcePlannedId === openedId` (planned id, or `null===null` for ad-hoc). Mismatch → `runFreshInit()`. This is why opening Lower never loads Upper's draft.
- **Gate on Done (`saveSessionProgress`):** a restorable draft is written only after ≥1 set is `completed`. Bare +/- nudges / prefill edits write nothing; un-completing all sets back to zero clears the draft.
- **⚠ Do not revert to a date-only key, drop the identity guard, or persist before a completed set** — any one of those re-opens the bleed. `performedDate` is the *viewing* day (defaults to today, not the workout's planned day), so a date-only key collides across workouts. Preserves D-110 orphan fail-safe + 24h window + same-workout resume.
- **Verification:** `scripts/_session-trace.mjs` (untracked logic harness, 11/11) replicates the key/gate/guard/cleanup and asserts the bug repro + must-preserve cases. Re-run on any change to the persistence path.

### Compact keypad-primary strength logger (D-121..D-128, 2026-06-11) — supersedes the circles/pills/stepper layout

The set card is now: **pre-filled Reps/Weight/RIR keypad cells** (tap = `openKeypadForSet`; Q-042 pencil affordance) as the primary input, a per-set **`last: w × r @ RIR` anchor line** (D-122), and **ONE thin quick-adjust strip** — `reps ±1` (neutral) / `wt −5/−2.5/+2.5/+5` (neutral) / `rir ±1` (amber, D-128) — that replaced the three former control rows (reps circles + RIR pills + 2×2 weight stepper). ~156px shorter (D-125). `target N` / `suggested N` captions sit under the Reps/RIR cells. "↑ Same" was removed with the stepper.

- **Prefill (D-126 + D-127):** *plan in the box when a plan exists, last-actual when unplanned, never empty when we have history.* Planned sessions show the plan's weight/reps (rir stays `null` → ghost target; **on Done a one-tap inline RIR confirm-or-adjust appears with the suggested value pre-highlighted — D-134** — never auto-committed). Unplanned/fresh sets fall back to last-actual (dimmed `from_previous`). One fetch (last 10 strength sessions, `workouts.strength_exercises` JSONB) feeds both the `last:` anchor and the unplanned prefill.
- **Rest timer (D-139, supersedes D-121):** **auto-starts on Done** (non-last, non-duration set) and lives ONLY in the pinned top pill (`REST m:ss · Skip`; Skip ends it). No in-row rest controls (removed). Haptics: light on auto-start, success at 0:00. Saga: D-120 auto-start-in-card → D-121 opt-in → **D-139 auto-start-top-pill**.
- **Deload pill (D-124)** in a two-row header (title+pill row / date+Pick-planned row); title **wraps** rather than truncating.
- **Strip sizing (D-129):** the nudge buttons are `flex-1` (basis-0) inside count-weighted groups (`reps 2 / wt 4 / rir 2`) at `h-10` (40px) — they **grow with the real row width** (≈32×40px at the 380px floor → ≈37×40px on a 414px iPhone) and never overflow 380. Do NOT re-pin them to a fixed minimum size tuned to the harness — 380px is the floor, not the target.
- **Strip grouping (D-130):** the three clusters are separated by **spacing contrast, not borders** — `gap-1` (4px) within a group, `gap-4` (16px) between groups (echoing the top cells' `gap-4`). Keep that 4×-ish ratio if you touch the gaps; uniform gaps make the strip read as undifferentiated buttons.
- **Column alignment (D-131):** the top cells AND the strip share identical column structure — both `[w-9 set-# leader][gap-2][flex-1 weighted 2:4:2 with gap-4]` — so each keypad cell centers exactly over its nudge group (`cell_center − group_center = 0`, verified). The **Weight cell is wider** (`flex-[4]`, 4 controls below) than Reps/RIR (`flex-[2]`); that's intentional. If you change one row's columns, change the other to match or alignment breaks. (This re-added the `w-9` leader D-129 had dropped — a deliberate ~6px button-width cost for alignment; nudge ≈26px@380 / ≈30px@414.)
- **Verification:** `scripts/verify-strength-row-380.mjs` (untracked harness) renders the real markup at 380px — asserts `overflowPx -10` (full p-2 clearance) AND nudge buttons ≥28px wide / ≥40px tall. Re-run after any set-card change; for sizing changes also eyeball ~414px (the harness can't feel "cramped," only "fits/doesn't" — D-129 was a device-only catch).

**⚠ Two load-bearing invariants (don't break):**
1. **Planned-vs-unplanned prefill rides on `rir: null` vs `undefined`.** `parseFromComputed` sets planned sets' `rir: null` (a value → "touched" → keeps the prescription). Fresh/unplanned sets have `rir === undefined` (→ "untouched" → accept last-actual). An init path that leaves a *planned* set's `rir` as `undefined` would make it wrongly inherit last-actual. This is the hinge of D-126/D-127.
2. **Deload detection is name-based** (`/deload/i` on `scheduledWorkout.name`) — matches the app's convention (WorkoutCalendar/UnifiedWorkoutView/AllPlansInterface) but is NOT a structured flag. A deload session whose name omits "Deload" won't show the pill. The robust fix — wire the server `WeekPhase` (`_shared/athlete-snapshot/body-response.ts`) through to the client logger — is **unbuilt**.

### RIR preference-with-fallback in the live e1RM path + strength_1rms backfill (Q-040 #1 / D-118, 2026-06-11)

The **live, plan-driving** e1RM is `compute-facts/brzycki1RM` (Brzycki on reps+RIR) → `exercise_log.estimated_1rm` + (aggregated) `learned_fitness.strength_1rms` → coach / `materialize-plan` load prescription. (NOT `compute-adaptation-metrics`, which is dead — see the dead-table note + D-116.)

- **Rule (`updateLearnedStrengthFromExerciseLog`, ~:984):** aggregate a lift's `strength_1rm` (window max) from sessions with `avg_rir ≤4` **or null** when any exist; only-RIR≥5 lifts fall back to using them, flagged `confidence:"low"`. Preference-with-fallback — **0 lifts go dark** (verified: every lift in the test user's 12-wk window has ≥1 usable session). Per-session `exercise_log.estimated_1rm` unchanged; only the aggregate selection changed.
- **Backfilled** (one `compute-facts` trigger re-aggregates the full window): trap_bar_deadlift 160→150, barbell_row 130→120 (RIR≥5 was the window-max for both); bench 165 / squat 105 / ohp 105 value-unchanged (confidence/sample_count reduced). Plan loads now read the corrected `strength_1rms`.
- **Deliberately NOT extended (Q-040 follow-up):** the UI trend `useExerciseLog` (StrengthSummaryView/BlockSummaryTab) still reads raw per-session e1RM, so RIR-5 sessions still appear as trend points / "current". Decision #1 was framed around the aggregate (plans); extending the `avg_rir ≤4` preference to the UI current/peak is a separate lower-stakes call.
- **Files:** `supabase/functions/compute-facts/index.ts`. Deployed `yyriamwvtvzlkumqrvpm`.

### RIR scale 0–5+ and RIR≥5 excluded from e1RM (Q-039 steps 1+2 / D-116, 2026-06-11)

First slice of the Q-039 set-logger refactor (the full layout refactor — windowed rep-circle picker, 2×2 stepper, 3-col grid, 44px targets — is steps 3–4, still pending; see Q-039).

- **UI/storage (step 1):** RIR picker is `0, 1, 2, 3, 4, 5+` (was `1–5`). "5+" stores integer `5` — no separate `rir_capped` flag (the engine branches on `>= 5`, so it's redundant). Cell value, target caption, and the picker all render `5+` for ≥5. Manual keypad entry clamps RIR to 0–5 (`commitKeypad`, was 0–10). `src/components/StrengthLogger.tsx`.
- **Engine (step 2):** `compute-adaptation-metrics/index.ts` → `estimate1Rm(weight, reps, avgRir)` returns **`null` when `avgRir >= 5`** (RIR ≥5 = far-from-failure, autoregulation-only, excluded from e1RM). RIR 0–4 → `epley × (1 + rir/10)` (unchanged); `null` RIR → raw Epley (unchanged). The caller (`:430`) stores `estimated_1rm: null` for excluded sets; downstream `useExerciseLog` already skips `estimated_1rm ?? 0 <= 0`, so it's null-safe end-to-end. **`avg_rir` is still recorded** for RIR-5 sets — only the e1RM data point is dropped, not the autoregulation signal.
- **Why together:** the pre-check found RIR-5 was already inflating e1RM by ×1.5 (the old `1 + rir/10` factor with no cap), so promoting "5+" to a first-class option without the engine fix would have spread the inflation. Shipped as one step per the working contract.
- **Verification:** app `npm run build` clean; 380px harness (`scripts/verify-strength-row-380.mjs`) — 6-pill row, `pillRight 240 == rirCellRight 240`, no border crossing. Engine: single call site, downstream null-safe.
- **⚠️ CORRECTION (2026-06-11, Q-040): the step-2 engine change landed on a DEAD path.** `compute-adaptation-metrics` writes `workout_adaptation`, and **`from('workout_adaptation')` has 0 readers in the codebase** — so D-116's RIR≥5 exclusion has **no UI or plan effect**. The live, consumed e1RM is `compute-facts/brzycki1RM` (Brzycki on `reps + RIR`) → `exercise_log.estimated_1rm` + `learned_fitness.strength_1rms` (UI progression via `useExerciseLog`, and plan-load prescription via coach / materialize-plan). That path is **not** RIR-capped. The D-116 change is left in place (harmless) but inert; the real RIR≥5 decision lives in `compute-facts` and is pending — see **Q-040**. No backfill of `workout_adaptation` is worthwhile (nothing reads it).

### `workout_adaptation` is a DEAD table (2026-06-11)

`compute-adaptation-metrics/index.ts` computes and writes `workout_adaptation` (strength e1RM via Epley×rirFactor, plus other adaptation fields), but **nothing reads the table** — `from('workout_adaptation')` returns zero hits across `src` + `supabase/functions`. Don't build on its output or assume it drives anything (it doesn't drive e1RM/trends/plans — `compute-facts` → `exercise_log` / `learned_fitness.strength_1rms` does). Whether to retire `compute-adaptation-metrics` or wire its output up is filed as **Q-041** (decision owed; do not act). If you're debugging strength e1RM, look at `compute-facts/brzycki1RM`, not here.

### Strength logger set-row redesign + rest-timer fix D-114..D-115 (2026-06-08)

Closes Q-034 (set-row overflow) and the long-standing rest-timer mismatch, in one client-only pass.

- **D-114 — collapse/expand set rows (Q-034 overflow).** The set row at `StrengthLogger.tsx:3256` was a single horizontal `flex items-start gap-2` packing set#/reps/weight+stepper/RIR/Same/Done/✕ (~470px of fixed-width children) into a ~316px content box with **no width or overflow handling** — Done/✕ rendered in the gutter on ~380px viewports. Fixed by collapse/expand, **not** the 2-row `grid grid-cols-12` restructure Q-034's recipe specced (left in Q-034 as historical audit; do not implement it). Per-exercise active set = explicit override (re-opened set) else first incomplete, else -1 (all collapsed); **UI-only state in `activeSetByExercise`, deliberately NOT written to `strength_logger_session_*`** so D-108/D-109 resume is untouched. Collapsed rows = one-line summary (`{weight} lb × {reps} · RIR {x}`) with `min-w-0 + truncate` on the summary + `shrink-0` Done/✕ — the summary absorbs the squeeze so the buttons can never cross the border (this is the structural fix for the whole overflow family). Expanded (active) row stacks controls vertically with Done/✕ in a right-aligned footer. Auto-advance: Done drops the override → active falls through to the next incomplete set, which auto-expands.
- **D-115 — rest is the gap after set N-1; set 0 renders no rest row (Bug A).** Bench Press showed 1:30 rest on set 1, 2:30 on set 2. Rest isn't stored — it's computed render-side from the **previous** set's reps via `calculateRestTime` (`:115`), the same "rest after set N-1" model `startAutoRestForNextSet` writes to. Set 0 has no previous set → hit a hardcoded `: 90` (1:30) fallback; set 1 → `calculateRestTime(Bench, 4)` = 150 (2:30). Fix: `showRestTimer` now requires `setIndex > 0` — set 0 renders no rest row at all (you don't rest before the first working set), hardcoded default removed, sets 1+ unchanged.

**Verification:**
1. `scripts/verify-strength-row-380.mjs` — renders the exact collapsed + expanded markup against the app's compiled Tailwind CSS at viewport 380 (deviceScaleFactor 3); every control ≤344px vs the 354px card border in **both** states; the old horizontal layout overflowed **+207px** (negative control proving the harness detects overflow, not a no-op).
2. `npm run build` + `tsc --noEmit` clean. `npm run ios` (cap sync) succeeded.
3. Auto-rest key math confirmed by code at `StrengthLogger.tsx:459`: `startAutoRestForNextSet` writes `${exerciseId}-${completedSetIndex + 1}` → completing set 0 starts **set 1's** timer; it never writes a `-0` key, so suppressing set 0's rendered row leaves the auto-start path intact.

**Caveat (not yet device-verified at runtime):** the harness renders markup with real CSS, not the live authenticated component — it proves the layout/overflow math and a clean build, but does not exercise the active-set state machine or auto-advance at runtime. On-device confirmation of those interactions is the remaining check.

**Footgun added — index-0 hardcoded fallback defaults.** When a per-item value is derived from the *previous* item, item 0 has no predecessor and tends to get a silent hardcoded default that diverges from every real value and reads as a bug (here `: 90` on set 0's rest). Same family as the failure-to-zero / truthy-coercion footgun behind **D-112** (0W coasting samples coerced to null). When a per-set/per-sample default is only hit by the first (or zeroth) element, ask whether that element should carry the value at all — don't pick a less-wrong default to paper it over.

**Files:** `src/components/StrengthLogger.tsx` (active-set state ~:420; per-set compute + collapsed/expanded branch in `exercise.sets.map`; `showRestTimer` gate ~:3210). **Commit:** `ce83b9b0`. Verification harness: `scripts/verify-strength-row-380.mjs` (local, untracked, per the np-verify/ftp-trace convention).

**Addendum (2026-06-10) — follow-ups (commits `caa25da2`, `e60877d4`):** (1) the first cut left equipment/plate/rest *outside* the flex-col, putting the Done/✕ footer mid-stack — moved inside so the footer is last (order: steppers → RIR → equipment → rest → footer); collapsed summary now always shows RIR. (2) **Distinct DISPLAY bug:** the inline RIR pill row (1–5, writes `set.rir` — the real RIR input) was captioned only `Target: {target_rir}`; "RIR" never appeared on screen, so with the deload's `target_rir=4` + 4–6 rep target it read as target *reps* and the RIR input looked missing. Label-only fix: always-visible "RIR" heading + secondary `target N` caption; binding/updateSet/Done-prompt unchanged. (3) **`target_rir` is independent of reps — confirmed via DB read, do NOT re-investigate:** base hypertrophy = RIR 3 at reps 8–10 (0/7 match rep-low-end), deload = RIR 4 at reps 4–6; the deload's 4==4 is coincidence, not reps bleeding into the field. No generator change. **Q-034 and POLISH §7 overflow remain closed.**

### Cycling analyzer honesty trilogy D-111..D-113 (2026-06-05)

Three-decision arc that closes a class of "the analyzer says one thing while the data shows another" bugs in cycling. Triggered by user noticing FTP 176W (Efforts) vs 204W (Garmin) divergence, expanded through "20% NP inflation on outdoor sweet-spot rides," closed by "the POWER row says '79% of threshold — sweet spot effort' in one breath."

- **D-111 — FTP cliff guard (confidence floor + ratchet floor).** Pre-D-111 the FTP learner's `Math.max(p20)` cascade was protected only as long as Tier 1 had ≥2 eligible 20-min bests in the 90d window. When the window slid old hard rides out without replacement, Tier 2 (best NP × 0.95) fired and could emit a 'high' confidence tag — letting the resolver trust a 32W cliff (176→144W) with no real fitness change. Fix at `supabase/functions/learn-fitness-profile/index.ts:928,300`: (a) cap Tier 2 confidence at 'medium' (Tier 3/4 already capped at medium/low), (b) writer-side ratchet that blocks overwrite ONLY when new confidence drops below prior — a real decline measured at equal-or-higher confidence still goes through. Worked example: post-fix Tier-1→Tier-2 cliff (176 high → 144 medium) is now blocked because medium<high; a genuine 176 high → 158 high decline overwrites freely. Verified post-deploy: ride_ftp_estimated stable at 176 high, last_updated advanced, no value drift.
- **D-112 — NP zero-strip + Coggan 30s startup trim.** The shared sensor extractor at `supabase/lib/analysis/sensor-data/extractor.ts:246` coerced 0W coasting samples through `(s.power && ...) || ...` short-circuit into `undefined`→`null`, which the NP rolling-window filter stripped. Result: NP and VI computed over a *pedaling-only* power series — ride 6bf694a6 (3×15 sweet spot, 34% coasting) read 169W NP instead of Coggan-correct 141W (Garmin). 28W = ~20% inflation per ride, cascading into IF (0.96 vs 0.79), VI (1.13 vs 1.41), TSS (89 vs 62). Fix: explicit ternary preserves 0 as 0; added 30s startup trim of incomplete rolling windows per Coggan. Backfill across 20 cycling rides via deployed `compute-workout-analysis` (v0.1.8 → v0.1.9 cache-bust marker): -628 phantom TSS across the 90d cycling block (25% over-count). Indoor Zwift rides moved only 2W (no coasting), confirming the fix targets the right surface. **Detour learning:** initial edit landed inside a `/* ... */` dead-code duplicate of `normalizeSamples` left in `compute-workout-analysis/index.ts:1051-1095` from an earlier extract-to-shared-lib refactor. The version-bump diagnostic (v0.1.8 → v0.1.9) is what surfaced the deploy-took-effect-but-values-didn't anomaly. Dead block left intact but flagged in code.
- **D-113 — POWER row reads execution, not intent.** D-091 made `classified_type` follow planned intent (correct for grouping / pwr20_trend_v1 / np_trend), but `_shared/session-detail/build.ts:1342` glued `classified_type` to executed metrics in one sentence: `"Normalized power 140W (79% of threshold) — sweet spot effort"`. 79% of threshold is endurance/tempo, not sweet spot — the renderer was asserting a planned intent as the executed reality. Fix swaps the descriptor source: read `derived.executed_intensity` (easy/moderate/hard, already computed deterministically from IF + ftp_bins per `_shared/cycling-v1/utils.ts:65`), NOT `classified_type`. Worked verification: 6bf694a6 (IF 0.79, executed=moderate) → "Normalized power 140W (79% of threshold) — **moderate effort**"; f9fb690b (IF 0.83, executed=hard) → "...— **hard effort**". `classified_type` left untouched — still grouping/trend authority. **Caveat (known, deferred):** persisted `workout_analysis.session_detail_v1` JSONB still holds old strings until next user-JWT view triggers rebuild (no service-role refresh path). The LLM lede in `ai-summary.ts` can also still slip into "right in the sweet-spot zone" on under-executed rides whose `interval_summary` is null — that's Option B's job, deferred to Q-036.

**Verification (live trace on user 45d122e7):**
1. `node scripts/ftp-trace.mjs` post-D-111 deploy: ride_ftp_estimated `{ value: 176, confidence: high, source: "95% of 20-min best power (12 efforts)" }` stable. last_updated advances on re-trigger without disturbing value.
2. `node scripts/np-verify.mjs` post-D-112 backfill: 6bf694a6 NP 169→140W (within 1W of Garmin 141W); all 20 rides match the pre-deploy dry-run within 0-1W; Zwift canaries move 2W (30s trim only); total 90d TSS 2493→1865.
3. `node scripts/power-row-verify.mjs` post-D-113: deployed code renders "moderate effort" / "hard effort" against the same fact packets that produced "sweet spot effort" pre-fix.

**Files:** `supabase/functions/learn-fitness-profile/index.ts` (D-111 — tier-2 cap at 928, ratchet floor at 300-321), `supabase/lib/analysis/sensor-data/extractor.ts` (D-112 — zero-preserve coercion at 246), `supabase/functions/compute-workout-analysis/index.ts` (D-112 — 30s Coggan trim at 1262 + ANALYSIS_VERSION 0.1.8→0.1.9), `supabase/functions/_shared/session-detail/build.ts` (D-113 — executed_intensity descriptor at 1342).

**Commits:** `e46aed65` (D-111) → `05b69313` (D-112) → `3f9d6df3` (D-113).

**Deferred this session, filed:**
- **Q-036 — Option B adherence field (`derived.intent_execution_match`).** Deterministic TSS-based adherence so renderer AND LLM read one ground truth. Spec complete (TSS primary from `planned_workouts.computed.steps` summed Coggan, ±20%/±50% bands, named constants in `_shared/cycling-v1/adherence.ts`); open decision: secondary IF gate on/off. Without it, target ride 6bf694a6 classifies `on_target` by 0.5pp. Awaiting go-decision.
- **Q-037 — FTP gap to Garmin remains 28W after D-112.** Garmin auto-FTP 204W implies best-20min ~215W; Strava-ingested power-curve max in 90d window is 185W (→ 176W FTP). Hypothesis: Strava power-stream smoothing/downsampling vs native Garmin .fit. Unverified — requires comparing native Garmin Connect activity vs Strava stream for the same ride.
- **Q-038 — Swim ingest renders through wrong analyzer + duration unit bug.** FORM → Strava → Efforts swim activities render with "5:03/mi pace", "61 spm cadence", "Splits (mi)", and duration adherence 2263% / +670:01 (Efforts thinks a 31-min plan took 11.7 hours — actual Strava moving time 18:12). Activity-type mapping in the ingest webhook isn't selecting swim analyzer; duration unit parsing is mangled. User explicitly tabled to a separate session.

### iOS strength logger resume hardening D-108..D-110 (2026-06-03)

Three-decision arc that hardened the iOS strength-logger resume path against (a) cold WKWebView kills, (b) flag-vs-data conflation, and (c) planned-workout-delete orphans in localStorage. All three commits on main; iOS bundle rebuilt + reinstalled.

- **D-108 — Cold-start gap closed.** D-101 (2026-05-27) wired `@capacitor/app` `appStateChange` for warm resume. But cold start (iOS kills the WKWebView entirely after long sleep / memory pressure / manual swipe-kill) → React mounts fresh → `showStrengthLogger` initializes `false` → `appStateChange` never fires (no transition; app boots into already-active). User lost path back despite localStorage holding the sets. Fix: synchronous mount-time read of `strength_logger_session_${today}` + `setShowStrengthLogger(true)` when exercises array non-empty. Also dropped `[showStrengthLogger]` dep from the listener-bind useEffect (was rebinding a new listener on every toggle, stacking them).
- **D-109 — Intent-vs-data conflation fixed (AND gate).** D-108's `hasUncompletedStrengthSession()` check conflated "session data exists in localStorage" with "user wants the logger open." Regression observed immediately: logger force-reopened on every warm resume AND every cold start even when user had navigated to dashboard before backgrounding. Fix introduced a separate `localStorage['strength_logger_open']` intent flag. Reopen now requires **both** flag set AND data present (AND gate). Implementation pattern: useState lazy initializer reads both at mount (runs synchronously BEFORE the flag-write useEffect, so no race); flag-write useEffect mirrors `showStrengthLogger` → localStorage; warm-resume handler reads same gate. AND gate also defends day-rollover: stale flag from yesterday is harmless because today's session key is empty. Code-pattern reproduced in `AppLayout.tsx` (module-level `todayDateString()` + `hasUncompletedStrengthSession()` helpers).
- **D-110 — Resurrection vector closed (A1 + A2).** Bug: athlete deletes today's planned strength workout from the calendar; tap "Log Strength" later resurrects it from localStorage. Root cause: session JSON keyed by date persists `sourcePlannedId` pointing at the planned row; DELETE doesn't touch localStorage; next restore loads orphan. **Two-sided fix shipped together:**
  - **A1 (delete-side)** `usePlannedWorkouts.deletePlannedWorkout` — capture `targetRow.date` + `targetRow.type` BEFORE the supabase DELETE; after successful DELETE, if `targetType === 'strength'` and `targetDate` set, `localStorage.removeItem(\`strength_logger_session_${targetDate}\`)`. Narrows the orphan window for hook-flow deletes.
  - **A2 (restore-side)** `StrengthLogger.tsx` init useEffect — async IIFE, if persisted `sourcePlannedId` non-null, verify with `supabase.from('planned_workouts').select('id').eq('id', sourcePlannedId).maybeSingle()`. **Critical fail-safe**: only clear when `(error == null) && (data == null)` — strict definitive "no row." Errors (offline, network timeout, RLS hiccup) → return `false` → preserve session. NEVER wipe in-progress logger because the network blinked. If missing: clear session + intent flag, fall through to `runFreshInit()` (extracted as local function). A2 catches resurrection *regardless of how the orphan was created* (not just hook-flow deletes).

**Verification (test cycle, executed against the rebuilt iOS bundle on device):**
1. Open logger, add a set, close logger explicitly, navigate to dashboard, kill app → cold-relaunch lands on dashboard, no auto-reopen. (D-109 intent flag removed on close.)
2. Reopen logger, add a set, swipe-kill app → cold-relaunch reopens logger with set intact. (D-108 mount-time check + D-109 AND gate.)
3. Reopen logger, add a set, background app → foreground reopens logger. (D-101 warm path still works.)
4. Delete planned strength workout from calendar → tap "Log Strength" → fresh logger, no resurrection. (D-110 A1.)
5. (Hand-tested) network disabled mid-mount with valid `sourcePlannedId` → session preserved. (D-110 A2 fail-safe.)

**Out of scope / deferred (filed):**
- **Q-034 — Strength logger viewport overflow on iOS 393pt** (audit complete, fix recipe documented). Set row track exceeds card width after D-096 (Same button) + D-098 (stepper) + D-099 (RIR pills) additions. Available width: 393 − 24 (px-3) − 16 (card p-2) − gaps ≈ 265pt; current set row consumes >330pt because the D-098 stepper nests inside the weight column (forcing its intrinsic width to ~134px) and the D-099 RIR pills are w-7 each (5×28 + 4×2 = 148px). Fix shape: convert outer `flex items-start gap-2` at `StrengthLogger.tsx:3256` to a 2-row CSS grid (`grid grid-cols-12 gap-2 items-start`), extract D-098 stepper from weight column to row 2, extract Same button from spacer to row 2, shrink RIR pills `w-7 → w-5`, move Set# + Delete to row 2. Estimated ~80-100 lines of nested JSX rewrite (conditional branches for duration-based / baseline-test / band-resistance must be preserved). Deferred to fresh session.
- **Q-035 — Actual-delete-of-planned investigation** (awaiting user repro). User reports deleting a logged strength workout also deletes the planned prescription. `useWorkouts.deleteWorkout:1685-1697` REVERTS planned to `status='planned'` rather than deleting. Three candidates: (1) silent revert failure (DB error swallowed), (2) UI staleness across hook instances (Leak B — `usePlannedWorkouts` per-instance React state), (3) DB cascade trigger. Needs repro artifacts.
- **Bug B Cause 2** (AuthWrapper `dc85e9d0` regression that unmounts AppLayout on every auth event) — D-101 + D-108 + D-109 close Cause 1 thoroughly; Cause 2 remains separate scope.

**Files:** `src/components/AppLayout.tsx` (D-108 + D-109), `src/hooks/usePlannedWorkouts.ts` (D-110 A1), `src/components/StrengthLogger.tsx` (D-110 A2 — init useEffect + `runFreshInit` extraction + fail-safe verify).

**Commits:** `58cc16c1` (D-108) → `0d466474` (D-109) → `93339e71` (D-110).

**Footgun added to the silent-cluster catalog:** localStorage is a second state store independent of the DB. Any mutation that conceptually invalidates cached client state (DELETE planned, complete a workout, change date semantics) must explicitly invalidate the corresponding localStorage key — there is no automatic propagation. The seven-bug silent cluster is now: silent SQL (D-075/D-081) → runtime (D-083) → JWT (D-103) → JSONB shape (D-089) → type coercion (D-094) → SELECT-narrowing (D-105) → **localStorage orphan (D-110)**.

### Run Performance tab sparkline redesign D-105..D-107 (2026-05-29)

Three-decision arc cleaning up the run Performance tab's TREND + ROUTE sparklines. Triggered by "ROUTE sparkline plots raw pace, not GAP — same effort at different grades shows as pace variation" (D-105 GAP fix), continued through "TREND pool is too short + mixes intents" (D-106 strict-intent + window extension), and closed by "ROUTE chart not rendering on web while iOS shows it" (D-107 intent-filter removal + HR threshold relaxation).

- **D-105 — ROUTE sparkline plots GAP-adjusted pace, falls back to raw per-row.** The grade-adjusted pace column (`route_progress_metrics.effort_adjusted_pace_sec_per_km`) was already populated on every recent row for the test user (10/10 verified); the fact-packet SELECT just didn't pull it. Three coordinated edits: `_shared/fact-packet/build.ts:720` extended SELECT + adds `gap_pace_s_per_km` to history row shape; `_shared/session-detail/types.ts:382` extends history contract with the new field (additive, back-compat preserved); `src/components/SessionNarrative.tsx` `RouteSparkline` derives `effective_pace = gap_pace_s_per_km ?? pace_s_per_km` per row, subtle "GAP" badge in header when any plotted point uses GAP. Pure SELECT-narrowing bug, same class as D-094 / D-075 / D-081 — data correct at source, reader didn't pull the column. May 21 today's run flipped from misleading 351 (raw, downhill-benefit) to honest 365.5 (grade-corrected).

- **D-106 — TREND pool strict-intent filter + window 8 → 12 (run-only).** Pre-D-106 the trend pool inherited `typeMatch`'s `comparableKeys` bucket (`easy_run` pooled with recovery / easy / steady_state / run / long_run via `getComparableTypeKeys`). Fine for the vs_similar per-session comparison (pace-proximity D-038 narrows it), but pollutes the TREND chart that visualizes aerobic adaptation OVER TIME for a single intent. New strict-type filter at `_shared/fact-packet/queries.ts` between `trendPoolBase` construction and `trend_points` serialization: `trendPool.filter((r) => inferWorkoutTypeKey(r) === workoutTypeKey)`. No fallback to the broad bucket — existing `sample_size < 3` gate suppresses the trend entirely rather than showing mixed-intent. `.slice(-8)` → `.slice(-12)` extends window from 8 → 12 sessions (~6-8 weeks of regular training). vs_similar pool intentionally untouched — broader bucket retained for cross-session HR-vs-pace reads. Verified on May 27 easy run: 3 strict `easy` points (Mar 26 / May 11 / May 20), correctly excludes adjacent steady_state runs.

- **D-107 — Drop ROUTE intent filter + lower TREND HR threshold + restore times_run label (post-D-106 web cleanups).** Two coupled web-Performance issues + one label-source fix:
  1. **ROUTE intent filter dropped.** D-039 Fix 6.1's same-intent gating on route history made sense pre-D-105 (intent-mixing on raw pace was noise), but post-D-105 GAP correction neutralizes effort-level variance per-point, so mixing intents now adds signal. Deleted intent-filter block at `fact-packet/build.ts:746-773`. Net: ROUTE history returns all matched-cluster rows (8 for today's test-user easy run vs the prior 5 same-intent). chart_eligible at `session-detail/build.ts:881` evaluates true (8 ≥ 8), web renders the chart. TREND keeps strict intent (D-106) — different question.
  2. **TREND HR dashed line gate** lowered from `>= 3` to `>= 2` at `SessionNarrative.tsx:220`. Direct consequence of D-106's strict pool: thin pools (e.g. 3 trend points, only 2 with HR) hid the HR line entirely. 2-point line renders as a single segment — sparse but honest.
  3. **Route-header label uses `times_run` first** instead of `comparable_runs ?? times_run` at `SessionNarrative.tsx:374`. Post-D-107 `comparable_runs` resolves to `history.length` (≤10 due to route_progress_metrics SELECT cap), reading as the chart's data-point count rather than athlete's cumulative experience with the route. `times_run` is the cluster sample_count (43 for today's test-user run) — the honest "how many times have I run this route" answer that iOS has been using all along.

**Verification (test user May 27 easy run, workout `fd820df6`):**
- Pre-D-107 web: "Same route · 5 comparable runs — not enough history to trend" text fallback.
- Post-D-107 web: ROUTE chart renders, label "Same route · 43×" matches iOS, 8 GAP-corrected + 8 HR data points plotted.
- TREND chart: 3 strict-easy points, HR line renders as 2-point segment (Mar 26 missing HR, May 11 + May 20 present).
- Web INSIGHTS confirmed correct: "Clean easy run on familiar terrain — grade-adjusted effort steady across the climbs, low RPE confirms recovery work as intended."

**Open caveats (intentionally not fixed):**
- iOS-vs-web bundle freshness: iOS Capacitor app may run an older JS bundle than web until `npx cap sync ios && cap run ios`. The chart-rendering divergence pre-D-107 was a real data difference (strict intent filter on web vs older iOS code without it), not a bundle drift — but bundle drift is a real concern for any client-side change. Not a blocker; rebuild on the next iOS release cut.
- `route_progress_metrics` per-cluster row cap (~8-10): cluster `times_run` (sample_count) goes higher (43), but `route_progress_metrics` only computes metric rows for a subset of runs. Sparkline data ceiling is the metric-row count, not sample_count. SELECT `.limit(10)` at `fact-packet/build.ts:724` could be bumped if denser sparklines become valuable; 8-10 points is reasonable visual density today.

**Sparkline architecture notes (carried forward):**
- TREND answers "am I getting fitter at this intent?" → strict same-classified_type filter (D-106).
- ROUTE answers "how is this terrain getting easier over time?" → no intent filter; GAP correction neutralizes effort-level per-point (D-105 + D-107).
- vs_similar answers "how did THIS session compare to similar recent sessions?" → broad `comparableKeys` bucket + pace-proximity filter (D-038 unchanged).
- These three questions warrant three different filter posture; do not unify.

**Files:** `supabase/functions/_shared/fact-packet/build.ts` (D-105 + D-107), `supabase/functions/_shared/fact-packet/queries.ts` (D-106), `supabase/functions/_shared/session-detail/types.ts` (D-105), `src/components/SessionNarrative.tsx` (D-105 RouteSparkline + D-107 HR threshold + label).

**Commits:** `eb3104be` (D-105) → `00bb45b5` (D-106) → `4faa9c35` (D-107).

### Strength workstream D-094..D-103 (2026-05-28) — the day the strength analyzer started working end-to-end

Ten-decision arc covering the strength performance pipeline + logger UX. Triggered by "Planned column shows '—' for every set" (D-094), expanded through six logger UX wins (D-096..D-101), the INSIGHTS three-piece refactor (D-102), and closed by finding the silent 401 auth gate that had been killing strength + swim analyzer narratives on the recompute path for months (D-103).

**Performance tab fixes:**
- **D-094 — Planned column renders weight × reps @ RIR instead of "—".** Display-layer parser bug: planned `reps` are strings ("4-6"), planned `weight` can be qualitative ("Bodyweight"), the parser coerced to 0 and `fmt()` returned "—". Fix at `src/components/StrengthPerformanceSummary.tsx` + `StrengthCompareTable.tsx`: parse string rep ranges to midpoint, recognize qualitative weight strings, fix fmt guard to render when any of (reps, weight, weight_display, RIR target) is meaningful. Data shape was correct all along; reader was wrong.
- **D-095 — PREVIOUS column on strength Performance tab.** Server-side `workout-detail` fetches last 10 strength workouts pre-current, builds `previous_strength_by_exercise` map keyed by normalized exercise name, attaches to `session_detail_v1`. Client renders new Set / Previous / Planned / Completed 3-col layout when `hasPrevious`, falls back to 2-col when no history. Per-set source from `workouts.strength_exercises` JSONB (exercise_log table only has per-session aggregates).

**Logger UX (6 commits, 1 session):**
- **D-096 — "↑ Same as set 1" carry-forward** on set 2+. Copies reps/weight/rir/duration/resistance/barType from set 1 in one tap. Disabled when already matching. Eliminates ~6 of 9 keypad cycles per exercise for the dominant "identical working sets" pattern.
- **D-097 — Previous-session autofill on logger open.** Client-side mirror of D-095's server fetch. Prefills empty sets with `from_previous: true` flag, muted text on reps/weight/RIR buttons. `updateSet` auto-clears flag on any athlete edit. The athlete arrives at a session where the work is mostly done — taps only confirm or adjust.
- **D-098 — Inline ±2.5 / ±5 weight stepper** below weight value. Barbell / dumbbell / goblet only; skipped for bodyweight / band / duration. Rounded to nearest 0.5 lb, clamped at 0. Drawer still opens on weight button tap.
- **D-099 — RIR drawer-keypad → 5-pill inline slider** (1-5). One tap commits. Visual states: selected (bright), target RIR (amber), other (subtle), selected + from_previous (muted). Drawer fallback preserved on handleSetComplete for "tap Done without RIR" path.
- **D-100 — Auto rest timer made visible.** Existing `startAutoRestForNextSet` already auto-fired but ran invisibly. Added: session-header amber countdown chip (picks smallest-remaining), Web Audio 880 Hz tone at zero (no asset file), dismiss × button. Duration-timer namespace exempted from tone.
- **D-101 — iOS resume reopens strength logger when session unfinished.** Closes Bug B Cause 1. `@capacitor/app` (newly installed) `appStateChange` listener in `AppLayout.tsx`; on `isActive === true` checks `strength_logger_session_${today}` localStorage key, reopens if exercises array has data. Bug B Cause 2 (AuthWrapper unmount regression) remains separate scope.

**Strength INSIGHTS:**
- **D-102 — Three-piece refactor of `analyze-strength-workout`.** (a) Lift narrative from `session_state_v1.narrative.text` to top-level `ai_summary` for run/cycling parity. (b) Tighten prompt from 6-section verbose (~2000 tokens) to 4-sentence cap (240 tokens) mirroring cycling D-093 — S1 lede (RIR + load adherence cite), S2 ONE physiological observation, S3 ONE phase + endurance context (skip if no signal), S4 ONE forward. HARD CUTS: no "this kind of work is exactly what...", no "monitor how you feel", no re-listing exercises. (c) New `strength_fact_packet_v1` with `phase`, `week_in_phase`, `plan_intent`, `avg_target_rir`, `avg_actual_rir`, `rir_delta`, `rir_verdict: 'too_easy'|'on_target'|'too_hard'|null`, `total_volume_lb`, `exercises_completed/planned`, `set_completion_pct`, `session_rpe`. `endurance_load_context: null` reserved for future wiring. Also adds `recomputed_at` write for D-079 parity.
- **D-103 — Silent 401 cross-sport bug, same class as D-075/D-081/D-083.** `analyze-strength-workout` and `analyze-swim-workout` both required `supabase.auth.getUser(token)` in-handler. Every internal invoker (recompute-workout, ingest-activity, bulk-reanalyze-workouts) calls with service-role token → no user.id → 401. `recompute-workout/index.ts:116-119` swallowed the error and returned `ok: true` to the client. **Strength + swim narratives had been silently failing on the recompute + ingest path since the gates were added.** Evidence: scanned 8 recent strength sessions for the test user — only ONE had a populated narrative (pre-gate). Fix: removed the in-handler JWT extract + getUser + 401 returns + cross-check on both analyzers. Mirror cycling + run analyzer pattern (trust service-role caller, read `workout.user_id` from DB row). Authorization enforced upstream by recompute-workout (user JWT validated + `workouts.user_id === user.id` cross-check at line 48 + 69) and by ingest-activity (webhook-trusted service-role context).

**Companion artifacts:**
- **`docs/STRENGTH-ANALYSIS.md`** (D-094..D-103 spec) saved. 294-line draft covering logger UX, performance tab, analytics, INSIGHTS narrative, endurance load integration, differentiation vs Hevy/Strong. Captures both shipped behavior and pending follow-ups.
- **New POLISH-PUNCH-LIST §7 Strength Analysis** added with the D-094..D-103 close-out, the open `analysis_error` HTML truncation item, and the spec's pending differentiator layer items.

**Verification:** end-to-end via service-role REST recompute against two strength sessions:
- May 18 unplanned session (`cfa77692`): post-D-103, analyzed_at populated, ai_summary 4-sentence narrative, fact packet present but most fields null (unplanned → no plan_metadata, no target_rir to compare; LLM uses `detailed_analysis.exercise_breakdown` for rich content).
- March 30 planned session (`c09d8006`): post-D-103, analyzed_at populated, ai_summary 3-sentence narrative (CLEAN-EXECUTION CAP correctly skipped S3 because no notable load signal), fact packet **fully populated**: phase="race prep", week_in_phase=5, plan_intent="traditional", rir_verdict="on_target", total_volume_lb=5440, set_completion_pct=137.5.

**Footgun added to the analyzer family:** any future analyzer-class function MUST NOT add an in-handler user-JWT gate. Internal callers are trusted by upstream authorization. The gate pattern looks defensive but creates a silent 401-and-swallow path that's invisible until end-to-end verification catches it. D-103's surfaced bug had been dormant for an unknown duration (only one historical narrative success, April 2 — likely pre-gate).

**Files:** `src/components/StrengthPerformanceSummary.tsx` (D-094), `src/components/StrengthCompareTable.tsx` (D-094 / D-095), `src/components/StrengthLogger.tsx` (D-096..D-100), `src/components/AppLayout.tsx` (D-101), `package.json` + `ios/` (D-101 @capacitor/app), `supabase/functions/workout-detail/index.ts` (D-095 server fetch), `supabase/functions/analyze-strength-workout/index.ts` (D-102 + D-103), `supabase/functions/analyze-swim-workout/index.ts` (D-103), `docs/STRENGTH-ANALYSIS.md` (new spec).

**Commits:** `f15dd302` → `ea5b75c3` → `1024db2e` → `c230ff68` → `43a52090` → `f2d6b339` → `46173e8a` → `79f5f738` → `8798c6a8` → `d491fc17` → `31f314c1` → `a217f40f` (twelve, including the spec save and the D-104 display-bug tail).

**D-104 tail (2026-05-28) — display bug surfaced after D-103 made the narrative reliably reach the client.** Strength + mobility branch of `MobileSummary.tsx:103-122` rendered only `<StrengthPerformanceSummary>` (the exercise table) — no `<SessionNarrative>`. The endurance branch (run/ride/swim) renders SessionNarrative at line 189. So strength sessions never showed an INSIGHTS narrative above the table even when `ai_summary` was populated and `session_detail_v1.narrative_text` was flowing through correctly. Pure tree-omission bug: data was on the wire, the client just didn't have the component mounted on the strength surface. Fix added `<SessionNarrative>` above `<StrengthPerformanceSummary>` with the same prop shape used in the endurance branch (sport-agnostic component; run/ride-specific blocks gracefully no-op for strength). Commit `a217f40f`.

### Cycling analyzer narrative + display polish D-089..D-093 (2026-05-27)
Five-decision arc that turned the cycling analyzer into a fundamentally different product than the prior morning. Triggered by inspecting today's sweet spot ride (workout `f9fb690b` — Strava Zwift VirtualRide, planned `b2e85f39` "Bike Sweet Spot — 2×15 min"). What was missing or mis-classified at the start; what's now true:

- **D-089 — per-interval execution table reaches the UI for the first time on cycling.** Cycling analyzer's `generateIntervalBreakdown` returned a bare array; every consumer (`session_detail/build.ts:234`, `workout-detail/index.ts:1238` enrichment, `_shared/cycling-v1/ai-summary.ts:259`, `generate-training-context/index.ts:1669`, `_shared/fact-packet/build.ts:305`) expected the run-aligned `{ available, intervals }` wrapped shape. Bare array silently missed all of them — including cycling's OWN ai-summary, which had been emitting null per-interval narratives since `buildCyclingIntervalSummary` landed. Wrapped at both write sites (granular_analysis + detailed_analysis). Added `avg_power_watts` alias on each interval (the sport-neutral field name session_detail reads to populate `IntervalRow.executed.power_watts`). Per-type interval numbering (work N / recovery N) replaces the global `index + 1`. `session_detail/build.ts` derives `planned_pace_display` from `planned_power_range_lower/upper` ("150-167 W" subtitle) and falls back to `power_adherence_percent` for the adherence badge — sport-neutral changes, no cycling branch. Client `EnduranceIntervalTable` already rendered watts/adherence for rides (line 247-248, 238); no client change needed.
- **D-090 — recoveries restored to the display.** Pre-fix the cycling interval-breakdown filter required `i.power_range`, dropping rest segments. A 2x15 sweet-spot ride showed 4 rows of 6. Filter extended to also accept role/kind matching `/recover|rest/`. Recoveries emit with explicit-null adherence fields (`power_adherence_percent`, `performance_score`, `planned_power_range_*`, `adherence_percentage`, `duration_adherence_percent`) — null === "explicitly ungraded," distinct from missing. session_detail short-circuits `pace_adherence_pct` on `iv.power_adherence_percent === null`; the weighted session-score loop skips ungraded intervals so recoveries don't depress the aggregate to 0%. Recoveries render label "Recovery", duration, avg watts, avg HR, no badge.
- **D-091 — plan_intent derivation from layered signals.** Pre-fix the cycling fact-packet read plan_intent only from `plannedWorkout.workout_type ?? plannedWorkout.type`. For every cycling planned row those columns held the discipline value `'ride'`, which `normalizePlanIntent` maps to null (per D-084). **plan_intent was null on every structured cycling session in the system.** The fallback classifier ran IF/VI-only and labelled an 88-94% FTP sweet-spot ride as `'threshold'` (IF 0.83), so the LLM led with "sub-threshold effort" instead of the prescribed intent. New `derivePlanIntentCycling(plannedWorkout)` with layered precedence: (1) workout_type/type (current path), (2) tags (canonical intent tag like `'sweet_spot'`), (3) steps_preset (token prefix like `bike_ss_*` / `bike_thr_*` / `bike_vo2_*` / `bike_tempo_*` / `bike_recovery_*` / `bike_anaerobic_*` / `bike_neuro_*` / `bike_race_pace_*` / `bike_openers` / `bike_only_brick` / `bike_endurance_*`). Free-text name/description deliberately NOT used — too noisy. `classified_type` cascade flips automatically because the existing short-circuit at `cycling-v1/build.ts:228` swaps in `planIntent` when non-null/non-`'unknown'`.
- **D-092 — STRUCTURED PLANNED MODE prompt rule + TREND suppression.** Two display-layer cleanups. (a) New hard prompt rule that fires when `interval_summary != null` AND `plan_intent ∈ {sweet_spot, threshold, vo2, tempo, anaerobic, neuromuscular, race_prep}`. EXPLICITLY overrides the general LEDE rule and the HARD CONSTRAINT at `ai-summary.ts:399`. Requires the lede to cover target-range adherence (cite `planned_power_range_w` vs actual + `in_target_range` + `power_adherence_pct`), completion count when partial (`completed_steps / total_steps`), and HR response across the set. NP becomes one trailing sentence. Bans leading with trend / vs-similar / PR on structured sessions — athletes ride for the target, not the trend. (b) Data fix to `interval_summary`: surfaces `planned_power_range_w` + `power_adherence_pct` + `in_target_range` per work interval; fixed phantom-key bug where the prior `avg_power_w` was always null (cycling intervals carry `actual_power_w` / `avg_power_watts` per D-089, not `avg_power_w`). (c) `pickCyclingTrendSeries` (`session-detail/build.ts:1052`) now returns null when `pwr20_trend_v1` has <3 type-filtered points AND the session has plan_intent in the structured set. A sweet-spot ride compared to an aggregate trend across endurance + recovery + sweet-spot rides isn't a meaningful "is the threshold work trending up?" signal; show no TREND rather than a misleading one. Unplanned/non-structured rides keep the np_trend fallback (no intent for the mixed series to mislead against).
- **D-093 — 4-sentence cap on clean structured executions.** Pre-D-093 the structured-session narrative ran 7 sentences with HR mentioned twice and fatigue mentioned twice. New `interval_summary.clean_execution: boolean` — true when every work interval landed ≥95% `power_adherence_pct`. CLEAN-EXECUTION CAP sub-rule inside STRUCTURED PLANNED MODE: when clean_execution is true, output EXACTLY 4 sentences — S1 lede (target adherence + per-rep wattage when ≤3 reps + opening HR), S2 one physiological observation, S3 one fatigue/load context sentence, S4 one forward-looking sentence. Explicit cut list: no "this kind of work is exactly what …" filler, no "monitor how you feel" generic advice, no closing exhortation, no per-interval recovery commentary. "Skip S3 and write 3 sentences if no load signal is worth saying — brevity > completeness." Verified output is 4 sentences with the right structure; some stylistic filler still leaks ("is exactly what you need before a recovery day") — user accepted as diminishing-returns and moved on.

**Verification (workout f9fb690b — sweet spot 2×15 on Zwift):** per-interval table renders 6 rows (warmup / work 1 / recovery 1 / work 2 / recovery 2 / cooldown) with per-type numbering, planned power ranges as "X-Y W" subtitles, executed watts and adherence badges. Narrative leads "You held the 150–167 W sweet-spot target across both 15-min blocks at 166 W and 162 W, with HR steady at 154 bpm…" — exactly the required form. TREND suppressed (verified via direct Deno call to `pickCyclingTrendSeries(wa)` → `null (TREND SUPPRESSED)`). Sentence count: 4.

**Files:** `_shared/cycling-v1/ai-summary.ts` (D-089 alias, D-092 prompt rule + interval_summary data fields, D-093 clean_execution + cap rule), `_shared/cycling-v1/build.ts` (D-091 derivePlanIntentCycling), `_shared/session-detail/build.ts` (D-089 sport-neutral power subtitle + adherence fallback, D-090 explicit-null short-circuit, D-092 TREND suppression), `analyze-cycling-workout/index.ts` (D-089 wrap + per-type numbering + recoveries filter, D-090 explicit-null fields + weighted-loop skip).

**Five commits:** `9cdced4e` (D-089) → `37ba2255` (D-090) → `0149f9e9` (D-091) → `52c9c9d0` (Q-033 docs) → `9f8e7f46` (D-092) → `806210af` (D-093).

**Footgun (analyzer ↔ display contract):** every cross-sport JSONB contract in the analyzer output must be shape-aligned to the run pattern. Five consumers all read `{ available, intervals }` from `interval_breakdown`; the cycling analyzer was the lone outlier writing bare arrays. Same class as D-075 / D-081 silent-42703s but at the JSONB shape layer instead of the SQL column layer. Whenever a "the data is there but doesn't surface" bug appears for a non-run sport, check the contract shape against the run analyzer first.

**Open (Q-033, deferred):** cycling prompt's VI-translation phrase `"natural power variation from the terrain"` is correct for outdoor rides but wrong for Zwift / VirtualRide / smart-trainer. Cosmetic vocabulary swap; D-076 HARD BAN remains the more serious correctness rule. Gate on `workout.provider_sport === 'VirtualRide'` and pass an `is_virtual_ride: boolean` into the display packet when picked up.

### Five-item audit batch D-085..D-087 (2026-05-26)
Autonomous batch from POLISH-PUNCH-LIST. Five items: two clean (no code), three required fixes; matrix 486/486 post-batch.

- **D-085 — FTP → cycling power zones flow fixed.** `compute-workout-analysis/index.ts:921` was reading FTP only from `performance_numbers.ftp` (manual entry), ignoring `learned_fitness.ride_ftp_estimated`. The shared `resolveCurrentFtp` helper (`src/lib/resolve-current-ftp.ts`) — used by `send-workout-to-garmin`, `calculate-workload`, `compute-facts`, `materialize-plan`, `athlete-snapshot`, `infer-training-fitness` — was bypassed here. Athletes with no manual entry but a high-confidence learned FTP had their entire ride history's `intensity_factor` / `computeRideTss` / **power-zone bins** (line 1568 — falls back to hardcoded `200W`) computed against 200W instead of their actual learned threshold. Fix: import + use `resolveCurrentFtp` like every other consumer. Precedence preserved (medium/high learned → manual → low-confidence learned → null).
- **D-086 — group ride anchor false `'wednesday'` fallback removed.** `reconcile-athlete-state-week-optimizer.ts:82` returned `{ day: 'wednesday', intensity: 'quality' }` when `bike_quality_day` was null but group-ride signals existed (route URL, duration estimate, or "group/hammer/club" label keyword). Main wizard flow (`ArcSetupWizard.tsx:601-603` writes `state.groupRideDay → preferredDays.quality_bike → AthleteState.bike_quality_day`) was always correct for any day; the fallback fired only on edge cases where group-ride signals existed but the day didn't make it into `bike_quality_day` — producing a false Wednesday anchor regardless of actual ride day. Fix: return `undefined`; let the optimizer's `quality_bike` placement loop pick the best matrix-clean weekday.
- **D-087 — `scaledWeeklyTSS` validator parity.** The week-builder (`week-builder.ts:734-736`) and `plan_contract_v1.weekly_tss_target` (`index.ts:601`) already used `endurance_hours ?? weekly_hours_available` per D-021 / Q-005. The plan validator at `validator.ts:73 checkTSSWithinBudget` received `state.weekly_hours_available` (declared) at `index.ts:458` — its TSS target was **inflated** relative to the actual week-builder budget. The validator's `if (w.total_raw_tss > target * 1.15) return false` check effectively had a slack ceiling for hybrid athletes; weeks that should have tripped silently passed. Fix: thread `validatorHours = scheduleState.session_frequency_defaults?.endurance_hours ?? state.weekly_hours_available` and pass to `validatePlan`. Mirrors the existing pattern at both other call sites.
- **Item 2 cycling session naming — clean.** `longRide()` factory (`session-factory.ts:570`) emits `"Long Ride — Xh"` and is called only from the dedicated long-ride day slot at `week-builder.ts:1098`. `easyBike` / `thresholdBike` / `sweetSpotBike` / `vo2Bike` / `tempoBike` use distinct "Bike X" prefixes. Brick sessions named `"Brick — Bike Xh"` / `"Brick — Run X mi off the bike"`. No false "Long Ride" labels.
- **Item 3 group ride anchor main flow — clean (after D-086).** Wizard captures any day in `state.groupRideDay`, writes to `preferredDays.quality_bike`, persists via `create-goal-and-materialize-plan/index.ts:1405-1431` to `freshCombinedPrefs.bike_quality_day`. Reconciler reads it. Optimizer uses it as a placement preference. Verified: any day the athlete picks, the engine respects.
- **Item 4 brick session structure — clean** vs CYCLING-PROTOCOL.md §4.3 / §5.7. `brick()` (`session-factory.ts:2008-2069`): bike leg = caller-sized to long-ride floor; race-spec closing block 20-45 min at 25% of bike total (`Math.min(45, Math.max(20, Math.round(bikeMin * 0.25)))`); race-pace IF target documented in description (0.78-0.82 for 70.3 / 0.62-0.68 for full IM); `steps_preset` emits `[bike_endurance_*Z2, bike_race_pace_*Z3]` on structured race-spec; AM/PM timing on bike/run; `['brick','bike'/'run', phase]` tags. The 20-min closing floor for <120 min bricks is reasonable degradation that preserves 75% base.
- **Verification:** NO_CACHE=1 matrix 486/486 pass post-batch (unchanged from end of overnight batch). compute-workout-analysis HTTP 200. May 23 ride end-to-end remains healthy.
- **Files:** `supabase/functions/compute-workout-analysis/index.ts` (D-085), `supabase/functions/generate-combined-plan/reconcile-athlete-state-week-optimizer.ts` (D-086), `supabase/functions/generate-combined-plan/index.ts` (D-087).

### Cycling-attach + linked-workout cascade D-074..D-084 (2026-05-26)
- **Story**: started as "May 23 Strava ride shows unattached in UI despite bidirectional plan link." Eleven D-entries; each layer's fix uncovered the next. Class of bug: silent PostgREST 42703 errors + unhandled exceptions caught by try/catch + caches preserving stale outputs.
- **D-074** — `plans.start_date` column doesn't exist; INSERT attempt reverted. Hoisted `planStartDate` local in `generate-combined-plan/index.ts` kept (single source of truth for `plan_contract_v1.start_date` + `plan_config.user_selected_start_date`). Separately: `activate-plan/index.ts:540` baseRow gains `environment: mapped === 'swim' ? 'pool' : null` so the swim-only `DEFAULT 'pool'` column stops surfacing on ride/run/strength planned rows.
- **D-075** — `analyze-cycling-workout/index.ts:1534-1539` planned_workouts SELECT listed `workout_type` and `workout_name` (columns don't exist; correct names are `type` and `name`). Silent 42703 → `plannedWorkout: null` for EVERY linked cycling ride → intervals=[], performance null-fields, `_hasLinkedPlan=false`, cycling LLM ran UNPLANNED MODE for every linked ride. Mirror run-side pattern: capture `error` + check it.
- **D-076** — "unplanned route" hallucination at the cycling LLM. No literal source — pure prompt synthesis from `is_unplanned: true` + a "climbing route" example in the SO-WHAT rule. HARD BAN rule appended after UNPLANNED MODE + "climbing route" → "climbing day" in the example. Mirrors D-040 Fix B / D-043 hard-ban pattern from the run side.
- **D-077** — UI: `TrainingBaselines.tsx` cycling FTP "Edit to override" was a non-interactive `<span>`. Added `ftpInputRef` + `focusFtpInput()` helper that focuses + selects the input on tap. Separately: `docs/FTP-COLD-START-SPEC.md` saved for future wizard FTP seeding work (W/kg-tier × bodyweight × 0.90 discount, stored as `learned_fitness.ride_ftp_estimated` with `confidence: 'low'`, source `'wizard_estimated'`).
- **D-078** — `recompute-workout` now passes `force_regenerate_ai_summary: true`; both analyzers gate the "preserve previous ai_summary on LLM failure" fallback on `!forceRegenerate`. ingest-activity / compute-facts paths unchanged so transient sync-time LLM hiccups still preserve good narrative; user-triggered recompute forces fresh.
- **D-079** — Cycling analyzer writes `workout_analysis.recomputed_at = new Date().toISOString()` — parity with `analyze-running-workout/index.ts:2817`. Closes the cache-bust gap: `workout-detail`'s `isSessionDetailStale` at line 112-116 reads `recomputed_at`; without it, cached `session_detail_v1` for cycling could persist past analyzer reruns until 24h timeout.
- **D-080** — Debug log in `workout-detail/index.ts:~598` for ledger-match resolution. Surfaced the `ledger_day_planned_count: 0` data point that pinpointed D-081. Block removed in the D-081 commit.
- **D-081** — `workout-detail/index.ts` planned_workouts SELECT at lines 360, 453, 492 listed three phantom columns: `swim_unit`, `baselines_template`, `baselines`. Same silent-42703 class as D-075. Affected EVERY sport — `plannedRows: []` → ledger saw zero planned → `softMatch` Pass 4 emitted `unplanned session` for every actual workout → `build.ts:650` `is_unplanned: !match?.planned_id` = `!null` = `true` for every linked workout. Downstream consumers in `session-detail/build.ts:1000-1007` reading those fields with optional chaining had been getting `undefined` for as long as the SELECT shape existed — swim baseline reads silently broken across the board. Removed phantom columns; reads degrade gracefully (already were).
- **D-082** — Diagnostic instrumentation: `callLLM` accepts optional `debug` mutable object populated with `has_api_key, http_status, response_chars, error_message, stop_reason` etc. `generateCyclingAISummaryV1` accepts `debug` and adds per-attempt diagnostics + validator outcomes. `analyze-cycling-workout` writes the captured diagnostics to `workout_analysis.ai_summary_debug` for REST-side visibility. Found D-083 in 30 seconds. Instrumentation kept in for now; removable in future cleanup.
- **D-083** — `_shared/cycling-v1/ai-summary.ts:426` referenced `isUnplanned` from `generateCyclingAISummaryV1`'s outer scope, but `isUnplanned` was only defined inside `toDisplayPacket`'s scope. Every cycling LLM call since D-046 shipped (2026-05-25) threw `ReferenceError: isUnplanned is not defined`. The analyzer's try/catch swallowed the exception, set ai_summary to null, preservation fallback re-served pre-D-046 narrative on every recompute. The LLM was never actually being contacted. Two-line fix at the top of `generateCyclingAISummaryV1`: `const isUnplanned = unplannedGate?.isUnplanned === true;`.
- **D-084** — Two Performance-tab cosmetic issues made visible by D-075/D-081/D-083 (now reaching the UI with real data):
  1. `_shared/cycling-v1/build.ts:14` `normalizePlanIntent('ride')` returned the literal `'unknown'` for discipline-only types ('bike'/'ride'/'cycling') → `planIntent || fallbackClassifyIntent(...)` short-circuited because `'unknown'` is truthy → classifier never ran → POWER row read "unknown effort". Fix: return null for discipline-only types + defense-in-depth `'unknown'`-aware guard at line 215.
  2. `AdherenceChips.tsx` Duration chip secondary line was a +/- delta (`+3:00`) from plan. The adherence % above it already conveys "how close to plan"; switched the secondary to absolute completed duration via new `fmtDurAbs(s)` helper.
- **Verification**: May 23 ride (id `7679f3a8`) end-to-end. Bidirectional plan link healthy; workout_analysis.performance populated (execution_score 99, power_adherence 100); ai_summary fresh (`2026-05-26T21:12:57`) with plan-aware narrative ("appropriate for a build-week ride", "Ironman 70.3 block"); classified_type `'threshold'` (zone-time-based, not IF average); Duration chip will show `2:03:00`. No "unplanned route", no backward "X days post-Marathon" anchor.
- **Footgun (cycling)**: any new SELECT against `planned_workouts` MUST capture and check `error` (mirror run-side `analyze-running-workout/index.ts:455-466`). Service-role queries with phantom columns return null data silently; downstream null-or-undefined handling masks the failure. Two of today's bugs were this exact class.
- **Footgun (LLM)**: any reference to a variable defined inside a nested function's scope from the outer scope WILL throw `ReferenceError` and be swallowed by the analyzer's try/catch. The D-082 instrumentation block in `_shared/llm.ts` + `_shared/cycling-v1/ai-summary.ts` + `analyze-cycling-workout` is the existing diagnostic surface for this class — KEEP IT until the next dormant exception lands.
- **Files**: `_shared/cycling-v1/build.ts`, `_shared/cycling-v1/ai-summary.ts`, `_shared/llm.ts`, `_shared/session-detail/build.ts` (D-080 read), `analyze-cycling-workout/index.ts`, `analyze-running-workout/index.ts` (D-078 parity), `recompute-workout/index.ts`, `workout-detail/index.ts`, `activate-plan/index.ts` (D-074 environment), `generate-combined-plan/index.ts` (D-074 planStartDate hoist), `src/components/TrainingBaselines.tsx` (D-077 UI), `src/components/AdherenceChips.tsx` (D-084 UI), `docs/FTP-COLD-START-SPEC.md` (new).

### Plan-gen overnight batch D-064..D-069 — 486/486 matrix pass (2026-05-26)
- **Spec / decisions:** D-064 (swim on rest_day), D-065 (Z3 jargon strip), D-066 (strength on rest_day + balancer), D-067 (test-harness peak-vs-median), D-068 (WoW TSS ramp distance-aware), D-069 (first_race sweet-spot in base). Full per-fix narrative in `scripts/plan-test-output/overnight-report.md`.
- **Root pattern (D-064 + D-066):** any session placed by the optimizer on a `rest_days` member day was silently dropped by the week-builder's `!slot?.isRest` defensive guard, with no relocation. Two layers of placement (swim/strength) and the weekly load balancer all needed rest_days awareness — fix is `restDaySet` hoisted to the top of `deriveOptimalWeek` and consulted in all four strength candidate orders + masters_swim anchor + swim preference loop + `BalancerContext.restDays`.
- **Verification:** 5 new regression tests in `_shared/week-optimizer.anchor-contract.test.ts` (35/35 green). 486-combo plan-generation matrix at 100% pass. Per-fix matrix progression in the overnight report.
- **Files:** `_shared/week-optimizer.ts` (D-064 / D-066), `generate-combined-plan/session-factory.ts` (D-065 / D-069 sweetSpotRun), `generate-combined-plan/week-builder.ts` (D-069 wiring), `generate-combined-plan/validate-training-floors.ts` + `generate-combined-plan/index.ts` (D-068 distance-aware ceilings), `scripts/plan-generation-matrix.mjs` (D-067 + NO_CACHE env).
- **Footgun:** D-067's harness fix is critical — without `NO_CACHE=1`, subsequent matrix runs read the pre-fix cached JSON sidecars from disk and report unchanged numbers. The 60s matrix runtime is cheap; always pass `NO_CACHE=1` when verifying an engine fix.

### Performance screen polish D-039 through D-042 (2026-05-24)
- CV outlier clipping: pace >1800 sec/mi excluded before CV computation
- Route naming: times_run only, no auto-generated name
- Arc forward-bias: build_read + unstructured_read hard-ban backward anchor;
  temporal lede must reference next race when active plan exists
- Decoupling surface: split by is_mixed_effort — steady raw gets drift number;
  mixed raw gets inconclusive description only
- HR-aware TREND: direction label neutralized when HR drops with pace
- Segment label: "Steady" for long_run/easy_run single-segment via workout_type
- Phase-aware TREND pool: pre-race points excluded when days_since_race < 60;
  trend_pool_crosses_race_boundary flag suppresses fitness claim on fallback
- run_easy_hr_trend wired: aerobic_direction + aerobic_efficiency_trend_pct
  surface in signals block; AEROBIC EFFICIENCY TREND prompt rule live
- Verify: recompute any linked easy run — should see "aerobic base responding"
  framing when athlete_snapshot.run_easy_hr_trend is populated

### Mixed-effort / unplanned run decoupling + HR signals (D-037, 2026-05-23)
- calculateEfficiency runs for unplanned interval sessions and all mixed/fartlek
  sessions (forMixedEffort flag, basis forced to 'raw')
- vs_similar HR fields (hr_delta_bpm, drift_delta_bpm, trend direction) reach
  LLM for mixed-effort sessions; pace fields stay nulled
- is_first_post_race_run flag + POST-RACE COMPARISON prompt rule suppresses
  fatigue/regression framing on literal first run back (runs_since_last_race<=1,
  days<=60)
- Planned interval sessions: decoupling null (intentional)

### Pool composition + detection widening (D-038, 2026-05-24)
- detectWorkoutType executed-pace fallback: unplanned fartleks with role='lap'
  now route correctly via _varGate threading
- 15% pace-proximity pool filter with 3-hit fallback; pool_intensity_filter
  diagnostic field on every session
- pool_pace_context.intensity_match always-on; POOL INTENSITY CONTEXT prompt
  rule prevents pace-gap from being misread as fatigue
- Verify: recompute any unplanned fartlek session — workoutType should be
  'fartlek', decoupling.basis 'raw', pool_pace_context present

### Run aerobic decoupling computed on grade-adjusted pace (D-036)
- **Spec:** `docs/RUN-HR-DRIFT-SPEC.md`. Decision: D-036.
- **Files:** `_shared/gap.ts` (new `enrichSamplesWithGAP` — idempotent via `raw_pace_s_per_mi` marker; returns `{ samples, basis: 'gap' \| 'raw' }`), `analyze-running-workout/index.ts` (GAP enrichment lifted to top after sensor extraction; `effectiveSensorData` passed to both `calculatePrescribedRangeAdherenceGranular` and `analyzeHeartRate`; post-fact-packet override of `derived.cardiac_decoupling_pct` with sample-level value plus `decoupling_basis` and `decoupling_assessment`), `analyze-running-workout/lib/adherence/granular-pace.ts` (inline enrichment replaced with shared helper call; idempotent so the outer lift doesn't double-correct), `analyze-running-workout/lib/heart-rate/efficiency.ts` (`decoupling.basis` field detected from `raw_pace_s_per_mi` marker presence), `analyze-running-workout/lib/heart-rate/types.ts` + `index.ts` (HRSummaryMetrics gains `decouplingBasis` + `decouplingAssessment`; all five summary builders updated), `_shared/fact-packet/types.ts` (`derived.decoupling_basis` + `decoupling_assessment`), `_shared/fact-packet/ai-summary.ts` (`signals.decoupling_basis` + `signals.decoupling_assessment` in display packet — only surfaced when `cardiac_decoupling` present, defense-in-depth against orphan flags; new AEROBIC DECOUPLING (RUN) prompt rule with two branches in `COACHING_SYSTEM_PROMPT`), `_shared/session-detail/types.ts` (`classification.decoupling: { pct, basis, assessment } \| null`), `_shared/session-detail/build.ts` (surfaces from `wa.heart_rate_summary`).
- **Behavior:** GAP enrichment runs once at the top of the run analyzer. The HR analyzer's `calculateEfficiency` reads grade-adjusted pace samples and produces a decoupling number that reflects real cardiovascular efficiency drift, not terrain. Sample-level decoupling (warmup-skipped, 20-min minimum, sample-granularity) replaces segment-level value for runs; segment-level path stays for cycling. When elevation data is absent, `enrichSamplesWithGAP` no-ops, decoupling is computed on raw pace, basis is reported as `'raw'`, and the LLM prompt explicitly tells the narrative to treat the number as inconclusive (don't claim fitness from a terrain-contaminated signal — same discipline as D-035 unplanned-workout fix). Cycling unchanged.
- **Verification:** 7 new Deno tests in `_shared/session-detail/decoupling.test.ts` (idempotency, basis detection, empty input, surface gating on `cardiac_decoupling` present, raw-basis carries through). All 407 `_shared` tests green; 19 cycling analyzer tests green; 0 regressions. Run analyzer has no direct test file (logged at Q-coverage from D-034). Deployed `analyze-running-workout` + `workout-detail` + `recompute-workout` + `bulk-reanalyze-workouts` + `ingest-activity` 2026-05-23.
- **Footgun:** `enrichSamplesWithGAP` must stay idempotent — two consumers now share its output via the lift; remove the `raw_pace_s_per_mi` marker check and pace gets corrected twice. Never synthesize a fitness claim from `basis === 'raw'` — the two prompt branches exist for a reason; folding them together reintroduces the contaminated-input bug across flat-shoes / no-GPS / indoor runs. `workout_type` is a descriptive label; the 3/5/8% thresholds are fixed bands in `efficiency.ts`, do not key them off the label. Cycling decoupling stays on its own path — the override in `analyze-running-workout` is run-only; do not unify across sports without re-evaluating cycling's NP-vs-pace concept.

### Unlinked workouts return null adherence + UNPLANNED MODE narrative (D-035)
- **Spec:** `docs/UNLINKED-WORKOUT-INTERPRETATION-SPEC.md`. Decision: D-035.
- **Files:** `analyze-running-workout/index.ts` (fake-target synthesis at `:504-538` deleted; null-override after `performance` finalization at `:~1460`; `unplannedGate` threaded into `generateAISummaryV1`), `analyze-cycling-workout/index.ts` (`performance` is null-fields object when `!plannedWorkout`; `unplannedGate` threaded into `generateCyclingAISummaryV1`; `power_variability` still computes for the D-034 variance gate), `analyze-swim-workout/index.ts` (`100` default replaced with `null`; hardcoded `duration_adherence: 100` killed; linked swims compute real ratio-based `duration_adherence`), `_shared/session-detail/types.ts` (`classification.is_unplanned: boolean`), `_shared/session-detail/build.ts` (surfaces `is_unplanned = !match?.planned_id`), `_shared/fact-packet/build.ts` (`execution.assessed_against = 'actual'` when `!plannedWorkout` — defense-in-depth for client `AdherenceChips.tsx:60` guard), `_shared/fact-packet/ai-summary.ts` (new `UnplannedGateOptions` + 7th param on `generateAISummaryV1`; `toDisplayFormatV1` drops `signals.execution` + `signals.interval_execution` when unplanned; `buildUserMessage` emits "UNPLANNED SESSION" top-line; new UNPLANNED MODE rule in `COACHING_SYSTEM_PROMPT` with terrain-aware variance reading), `_shared/cycling-v1/ai-summary.ts` (symmetric: `is_unplanned` surfaced on display packet, cross_workout preserved per user direction, UNPLANNED MODE rule added).
- **Behavior:** Adherence requires a prescription. Without a linked plan, all three analyzers return `null` for `execution_adherence`, `pace_adherence`, `duration_adherence`, `completed_steps`, `total_steps`. The client (`AdherenceChips.tsx`) already null-safe — chips disappear cleanly. The LLM input shape swaps: prescribed-range signals dropped; "UNPLANNED SESSION" top-line emitted; UNPLANNED MODE prompt rule fires and tells the LLM to interpret on the workout's own terms (HR-to-pace efficiency, terrain-aware variance reading via GAP, conditions, route history). Same-category history (vs_similar for run; cross_workout for cycling) is preserved when unplanned — it's legitimate history, not prescription. The duration-derived `workout_type` label stays as a soft hint; the prompt rule states explicitly it's descriptive only, never a target.
- **Verification:** 9 new Deno tests in `_shared/session-detail/unplanned-workout.test.ts` (toDisplayFormatV1 gate; buildUserMessage top-line + skip; vs_similar preserved; linked-workout regression). All 400 `_shared` tests green; 19 cycling analyzer tests green; 0 regressions. Deployed `analyze-running-workout` + `analyze-cycling-workout` + `analyze-swim-workout` + `workout-detail` + `recompute-workout` + `bulk-reanalyze-workouts` + `ingest-activity` 2026-05-23.
- **Footgun:** adherence requires a prescription — if a future feature needs an "adherence number" for unlinked workouts, introduce a different metric (efficiency/consistency score), don't resurrect synthesized targets. `workout_type` is a descriptive label, never a target — any code path that reads it and sets a pace_range / power_range / adherence baseline reintroduces this bug. The UNPLANNED MODE prompt rule is load-bearing; if anti-jargon retry or any other guard mutates the prompt, preserve this rule. `assessed_against = 'actual'` now has two trigger conditions (intentional deviation OR no plan); the client guard at `AdherenceChips.tsx:60` treats them the same — if you ever split the cases, make sure both still hide chips. Swim's linked-swim `duration_adherence` formula matches the run analyzer's (`granular-pace.ts:471-482`); if you change one, change the other.

### `is_mixed_effort` variance gate + Bug A segment-label hygiene (D-034)
- **Spec:** `docs/PERF-INTERVAL-INTERPRETATION-SPEC.md` v2. Decision: D-034.
- **Files:** `supabase/functions/analyze-running-workout/index.ts` (`buildRowsFromBreakdown` ~:4040, `_varGate` hoisted ~:2040 before `generateAISummaryV1`, `'Overall session'` literal removed at the three fallback sites :4014/4209/4277), `analyze-cycling-workout/index.ts` (`_varGateRide` hoisted ~:2340), `_shared/session-detail/build.ts` (`humanizePlannedSegmentLabel` rewrite at :41-65 — defense-in-depth `'Overall session' → 'Overall'` + synthesize `Interval N`/`Recovery N` from numbering; variance surfacing in `classification` + `pacing` blocks), `_shared/session-detail/types.ts` (extended `classification` + `pacing`), `_shared/fact-packet/queries.ts` (pool filter excludes `is_mixed_effort` rows from easy queries; new `getOverallGapSecPerMi`/`resolvePaceForComparison`/`isMixedEffortRow` exports; GAP-aware basis selection in vs_similar + trend_points), `_shared/fact-packet/build.ts` (threads `currentAvgGapSecPerMi`, persists `pace_basis`), `_shared/fact-packet/ai-summary.ts` (new `VarianceGateOptions` param; drops `vs_similar` and adds `interval_summary` when mixed-effort; MIXED-EFFORT MODE prompt rule), `_shared/cycling-v1/ai-summary.ts` (cycling symmetric: `CyclingVarianceGateOptions`, drops `cross_workout` and adds `interval_summary`).
- **Behavior:** Server computes one deterministic boolean per workout — `workout_analysis.session_state_v1.glance.is_mixed_effort` — from any of: `interval_execution.total_steps ≥ 2`, plan-intent classified as interval-like, `detectWorkoutTypeFromIntervals` returns non-easy (unlinked), or pace CV ≥ 8% on a basis-trusted series. Run CV basis is `gap` whenever `granular-pace.ts:hasUsableElevation` is true; raw CV is trusted only on `flat` terrain. Cycling uses VI ≥ 1.05 OR power CV ≥ 12% OR plan-intent intervals. When mixed-effort: vs_similar pool filter excludes the row from steady comparisons; LLM input shape swaps (steady comparison → per-interval execution); plan intent never overwritten (override flag set instead). Cross-workout pace comparisons (vs_similar, trend_points) prefer GAP when both current and historical have it; raw fallback; never mix bases. `session_detail_v1.classification.{is_mixed_effort, variance_signal, classified_type_variance_override}` + `pacing.{coefficient_of_variation_basis, variability_index, power_cv_pct}` are the server-side surface — client renders, never derives. Bug A: analyzer's `buildSessionIntervalRows` no longer collapses unlinked sessions with ≥2 measured breakdown intervals to a single "Overall" row; the literal `'Overall session'` is no longer emitted at any of three sites (now `null`); display-layer `humanizePlannedSegmentLabel` has a defense-in-depth guard so stale rows render correctly without backfill.
- **Verification:** 9 new Deno tests in `_shared/session-detail/variance-gate.test.ts` (humanize labels — Overall guard + Interval N / Recovery N synthesis + Warmup/Cooldown + pass-through; `isMixedEffortRow` reader; `resolvePaceForComparison` GAP-when-both vs raw-fallback; `getOverallGapSecPerMi`). All 391 `_shared` tests green; 19 cycling analyzer tests green; 0 regressions. Run analyzer has no direct test file (logged as a coverage-gap follow-up in POLISH-PUNCH-LIST.md). Deployed `analyze-running-workout` + `analyze-cycling-workout` + `workout-detail` + `recompute-workout` + `bulk-reanalyze-workouts` + `ingest-activity` 2026-05-23.
- **Footgun:** plan intent is sacred — a linked-plan `'easy'` that trips the gate keeps `classified_type:'easy'` and sets `classified_type_variance_override: true`. Pool filters key off `is_mixed_effort` + the override flag, NOT classified_type. Never mix GAP and raw within a single vs_similar comparison set — averaging grade-adjusted with raw is apples + pomegranates. Raw CV on non-flat terrain is silently skipped by design (terrain confounder; missed detection > false flag). No client-side CV/variance math anywhere — if a future UI needs more granularity, extend the server contract first.

### §6.1 cycling/running asymmetry — heavy Lower placement
- **Spec:** `docs/STRENGTH-PROTOCOL.md` §6.1 (4f106a78), §6.1.5 consolidation gate widening (b189e7ca), phase-aware adjacency messaging (7715ff5d).
- **Files:** `supabase/functions/_shared/schedule-session-constraints.ts`, `_shared/week-optimizer.ts`, `_shared/plan-generation-trade-offs.ts`.
- **Behavior:** Heavy Lower (Strength Build 78-85%, Maintenance + Power 70-75%, Rebuild 72-80%) is never 24h-adjacent to Quality Run or Long Run; 48h gap minimum from Long Run on both sides; permitted 24h-adjacent to bike sessions per Wilson 2012 (running ES≈0.94 vs cycling ES≈0.32). Hypertrophy / Deload sub-maximal Lower has relaxed adjacency.
- **Verification:** §6.1 conformance contract W-004..W-008 in STRENGTH-PROTOCOL.md §3.8. Test coverage in `supabase/functions/generate-combined-plan/same-day-pairing.test.ts`. Verified manually against multiple plan exports during the §6.1 pass.
- **Open question:** is the heavy-Lower classification protocol-gated to performance, or load-magnitude-gated? See Q-003.

### §7.3 post-race recovery week SKIP
- **Spec:** `docs/STRENGTH-PROTOCOL.md` §7.3.
- **File:** `supabase/functions/generate-combined-plan/week-builder.ts:~1569` — `if (phase === 'recovery') strFreq = 0`.
- **Behavior:** Both hybrid REDUCE-default and durability SKIP-default mid-block 3:1 deloads keep their parent phase (`base` / `build` / `race_specific`) and emit reduced strength sessions. The dedicated post-race recovery week (`phase === 'recovery'` from `phase-structure.ts:289-309 insertRecoveryBlock`) is the only path that triggers the unconditional SKIP. Protocol-agnostic — works for hybrid AND durability.
- **Verification:** Plan #59 W14 went from emitting Hypertrophy Deload Lower to SKIPPED post-fix (commit d42c1079). W4/W8/W12 mid-block deloads still emit reduced sessions as designed.

### Option B endurance-hours deduction
- **Spec:** `docs/SESSION-FREQUENCY-DEFAULTS.md` §2.1.
- **File:** `src/lib/session-frequency-defaults.ts:228-236` (`strengthCountFromIntent`) + line 282-285 (deduction applied before tier lookup).
- **Behavior:** Tier lookup uses endurance-adjusted hours (`declared - strength_count × 0.75hr`); §7 strength count keeps reading declared hours. Hybrid 11hr athlete re-tiers from `10-12` to `8-10` (Plan #59 regression fix, commit cf68cf43).
- **Verification:** 7 deno tests in `supabase/functions/_shared/session-frequency-defaults.test.ts` covering hybrid 11hr/7d, hybrid 11.5hr/7d boundary, hybrid 10hr/7d boundary, hybrid 9.99hr/7d (no §7 discontinuity), endurance-only 11hr/7d (no regression), support 11hr/7d, performance 14hr/7d. All passing.
- **Constant rationale:** see D-001.

### `useStrengthOrderingPreference` hook + `orderDayWorkoutsByTimingThenDiscipline` helper
- **Files:** `src/lib/use-strength-ordering-preference.ts` (hook + module-level cache + in-flight dedupe), `src/lib/pairing-timing.ts` (`orderDayWorkoutsByTimingThenDiscipline` — the single shared sort helper, relocated here from AllPlansInterface 2026-05-18, `2ba6c68b`; takes an optional `getWorkout` accessor for wrapper items), `src/components/AllPlansInterface.tsx` (weekly view ~:2372 + markdown export ~:1783 call sites), `src/components/TodaysEffort.tsx` (top cards ~:704), `src/components/WorkoutCalendar.tsx` (weekly calendar cells, ~:963, via `(e)=>e._src`).
- **Behavior:** One fetch path for `goals.training_prefs.strength_ordering_preference` (per planId). **FOUR** consumers — TodaysEffort, AllPlansInterface weekly view, AllPlansInterface markdown export, WorkoutCalendar cells. AllPlansInterface + WorkoutCalendar share `orderDayWorkoutsByTimingThenDiscipline` (timing then discipline-rank); TodaysEffort uses a timing-only stable sort (no discipline tiebreaker) — equivalent for the run+lower timed case, differs only for untimed stacks (e.g. swim+upper = Q-001 cosmetic). WorkoutCalendar resolves ONE week-dominant planId (single-plan weeks are the common case; first-found approximation, same as TodaysEffort per-day).
- **Verification:** consolidation e41e7781 + 3770ad41 (2026-05-13); WorkoutCalendar wired as the 4th consumer 2026-05-18 (`2ba6c68b`) — it was previously **unwired** (raw insertion order; Thursday Strength-above-Run / Monday Strength-above-Swim). `src/lib/pairing-timing.test.ts` (5 — incl. strength_first pref-honored + accessor guard); manual for the React render sites.
- **Footgun:** any NEW day-stacked render surface MUST call `orderDayWorkoutsByTimingThenDiscipline` from `@/lib/pairing-timing` (do not re-copy a sort; WorkoutCalendar was exactly that miss). The hook's module-level cache is **never invalidated** (page-load lifetime) — latent stale risk only if a regenerate-in-place keeps the same planId while the value flips mid-session (not currently exercised; relies on regeneration minting a new planId).
- **Decisions:** see D-003, D-004, D-005, D-006 (D-006/Q-001: swim+upper has no AM/PM ordering — discipline-rank "swim above upper" is the intended cosmetic; not pref-driven, do not "fix"). **Q-012:** easy-run + Lower on deload Thursdays renders Strength-before-Run *by design* (recovery-flush; `decideOrdering` ignores `strength_ordering_preference` for easy partners — mirrors server `week-builder.ts:1997-2012`). The 2026-05-18 read-only diagnosis confirmed `2ba6c68b` is correct, not regressing — do not "fix".

### `swim_experience='learning'` → soft -1 in `inferTrainingFitnessLevel`
- **File:** `supabase/functions/_shared/infer-training-fitness.ts:~178` (`if (swimExp === 'learning') score -= 1`).
- **Behavior:** Wizard-collected `training_prefs.swim_experience` is consulted by the training_fitness inference. Soft signal (-1), not hard clamp — bounded by score thresholds, won't over-clamp a strong cyclist/runner who's new to swim. Cascades into swim volume bands and per-session rep caps via existing consumers of `training_fitness`.
- **Verification:** 2 new deno tests in `supabase/functions/_shared/infer-training-fitness.test.ts` (commit 0fd17ad9). 6 of 7 tests pass; the 1 failure is pre-existing on main, unrelated — see Q-006 / D-002.
- **Decision:** see D-002.

### Cycling Performance tab at running parity
- **Files:** `_shared/session-detail/build.ts`, `_shared/cycling-v1/*`, `analyze-cycling-workout`, `workout-detail` (+ client `SessionNarrative.tsx`). Commits `25167b90`→`a739961f` (2026-05-14→16).
- **Behavior:** Cycling INSIGHTS (arc-aware LLM), mode-aware + classified_type-filtered TREND with dual pace+HR line, EFFICIENCY, CLIMBING/VAM, POWER ZONES, PACING, TERRAIN (+temp), vs-similar, and a dist/dur/temp stat line — at parity with running's Performance tab.
- **Verification:** recompute a ride → reload Performance tab; confirm rows render and `classified_type` reflects the VI gate. Pure helpers unit-tested (`classify-intent`, `ride-physiology`, `cycling-trend`, `segments`, `analysis-mode`); suite 628 pass / 1 pre-existing-unrelated fail.
- **Decision:** see D-011..D-014; design `docs/CYCLING-ANALYSIS-DESIGN.md`.

### Intent-aware cycling analysis (Build Order #1–#7, #9)
- **Files/data:** NP-TSS `computed.analysis.power.tss`; CTL/ATL/TSB `workout_analysis.fitness_v1` + `athlete_snapshot.{ctl,atl,tsb}` + `ArcContext.cycling_fitness`; HR-at-power/decoupling + VAM in `computed.analysis.{efficiency,climbing}`; segment ingestion `cycling_segment_history`.
- **Behavior:** per-ride TSS, PMC fitness/fatigue/form, aerobic efficiency + climbing-rate metrics, Strava+Garmin-climb segment history. Both migrations applied via SQL editor; all table access guarded.
- **Verification:** `docs/SESSION-CONTEXT.md` §6 checklist.
- **Decision:** D-011..D-014.

### Cycling fact-packet IF/VI sourced from canonical `computed.analysis.power.*`
- **Files:** `analyze-cycling-workout/index.ts:~1790` (NP resolver now prefers `computed.analysis.power.normalized_power`; new canonical VI/IF extraction passed as overrides), `_shared/cycling-v1/build.ts` (`buildCyclingFactPacketV1` optional `variabilityIndexOverride`/`intensityFactorOverride` — finite & positive overrides win for `facts`, the classifier gate, and `executed_intensity`, else per-metric recompute). Commits `6941a236` (fix) + `fae293e7` (verify/backfill script), 2026-05-17.
- **Behavior:** the packet previously recomputed IF (NP/FTP) and VI (NP/avg) from NP/avg resolved via `computed.overall.*` — which `compute-workout-summary` never writes at the overall level (power is written only per-interval/segment), so it fell through to provider/device power and disagreed with the analyzer. IF/VI now come from `computed.analysis.power.{intensity_factor,variability_index}` (the source `compute-facts:1124` trusts); the classifier's VI≥1.10 ∧ IF≥0.85 gate and downstream TSS/executed_intensity reason over the analyzer's full-series numbers.
- **Verification:** `scripts/verify-cycling-vi-if-fix.mjs` selects rides by the actual fact-packet-vs-canonical divergence and replays the recompute chain. Verified run 2026-05-17: 8 affected rides (120 d), all converged to exact match; `60304656` vo2→tempo and `4375a709` endurance_long→threshold reclassified on the corrected numbers. `_shared/cycling-v1/build.test.ts` (4 tests) + full cycling-v1 suite green (83 pass).
- **Decision:** see D-015.

### Cycling VI-gate elevation density from total `workouts.elevation_gain`
- **Files:** `_shared/cycling-v1/build.ts` (`elevationGainPerMi` now uses `elevationGainM`; climb_ascent_m fallback only), `analyze-cycling-workout/index.ts` (`elevation_gain` added to workout SELECT + passed through). Commit `bdf2cde2`, 2026-05-17.
- **Behavior:** the classifier's ≥40 ft/mi elevation-density gate sources ascent from total ride elevation gain, not grade≥3% climb-segment ascent (which under-reported on rolling terrain and straddled the gate wrong). Supersedes D-011's elevation-source tradeoff.
- **Verification:** May-10 ride `60304656` recomputed post-deploy → reclassified `tempo` → `climbing` (325 m total → 46.5 ft/mi; was 249 m climb-seg → 35.6 ft/mi); VI/IF stayed canonical (fpVI 1.13 = capVI). `build.test.ts` +3 (60304656 fix / climb-seg fallback / total-gain precedence); cycling-v1 suite green (86 pass).
- **Decision:** see D-016 (supersedes D-011 elevation-source only).

### Cycling Performance-tab display & narrative correctness (2026-05-17 — arc close-out)
- **Files/commits:** sport-aware TREND legend `91ea2078`; TREND ≥5-or-text gate `6bf574d4`; POWER-ZONES all-zones + duration-anchored total `80b4c285`/`8e83e5df`; PR attribution + Efforts-scoped language `a0ca4158`; narrative trend-series match + deterministic Arc-lede guard `36a7e792`/`dcaa9f08`/`da7dbce8`; INSIGHTS plain-language + deterministic jargon guard `98c04e2f`/`d6da072c`/`d02abfe4`. Touch `_shared/cycling-v1/ai-summary.ts` + `cross-workout-{types,queries}.ts`, `_shared/session-detail/build.ts`, `src/components/SessionNarrative.tsx`, `analyze-cycling-workout`.
- **Behavior:** TREND legend says "power" for rides; the chart needs ≥5 same-type rides (3–4 → text), cycling-only (running unchanged); POWER-ZONES shows every zone with the total accounting for full ride duration (un-binned coasting → "+Xm other"); the narrative cites the SAME trend series the TREND row shows, never leads with PR/recovery/taper framing, and is plain-language (no IF/VI/EF/HR-decoupling/ACWR/TSB labels-or-numbers; "so what" context; 3–4 sentences). Enforced by a 3-guard stack on the ai_summary — `validateNoNewNumbers` + `ledeOpensWithArcFrame` + `summaryHasJargon` — sharing ONE combined corrective retry (plus `set_on_current_ride` gating "set this ride"; Efforts-scoped language).
- **Verification:** `scripts/verify-cycling-vi-if-fix.mjs --all` (180 d / 30 rides) re-run after each change — final 30/30, 0 failed, **0/30 Arc-lede**, **jargon 0/30**, ≤4 sentences 30/30, IF/VI 26/26 consistent, banned-language 0/30, the 1 PR claim correctly attributed (`60304656`/`54e8fd86`). cycling-v1 suite 96 pass; session-detail 24 pass.
- **Footguns (don't re-litigate):** `docs/SESSION-CONTEXT.md` §7 — narrative trend MUST mirror `pickCyclingTrendSeries` (don't revert to always-np_trend); the lede + jargon guards are deterministic by design (don't delete them / don't "fix" the lede in the shared `arc-narrative-ai-appendix.ts` — would change running); the 3-guard stack shares ONE retry budget (don't split per-guard or cut the attempt count — degrades the others); `achievements_v1` PRs are prior-ride; `computed.overall.*` has no overall power; `climb_ascent_m` ≠ total gain.
- **Decision:** D-015, D-016 (no new D-NNN — display/narrative fixes were single-sane-implementation).

### Plan-gen strength provenance split (Bugs 1&2 / #131)
- **Files/commits:** `reconcile-athlete-state-week-optimizer.ts:276` (stop engine→`strength_preferred_days`), `create-goal-and-materialize-plan/index.ts:~904` (strip engine `preferred_days.strength`, persist `trainingPrefs.strength_optimizer_slots`), `src/lib/format-wizard-prefs-export.ts` ("Strength (scheduled by app):" path). Commit `71611501`, 2026-05-17. D-017.
- **Behavior:** `strength_preferred_days` / `preferred_days.strength` carry ONLY a genuine wizard pin; engine-chosen strength placement lives ONLY in `strength_optimizer_slots` and exports as "scheduled by app", never as an athlete preference. The optimizer is no longer fed phantom strength prefs → no bogus "preferred day rejected" strength trade-offs (root-fixed at source, not composer-suppressed).
- **Verification:** export test +2 (engine→scheduled-by-app; genuine pin still surfaces) flipped from the leak-encoding test; suites green — format-wizard-prefs-export 6, plan-generation-trade-offs 30, week-optimizer.anchor-contract 25, generate-combined-plan 158, all 0 failed; client build clean. End-to-end (live multi-sport regen) **observed 2026-05-18** — the co-equal-2× regen surfaced no bogus strength "preferred rejected" trade-offs and no engine-strength-as-pin leak; D-017 holds end-to-end.
- **Footgun (don't re-litigate):** never re-introduce an engine→`strength_preferred_days` or engine→`preferred_days.strength` write (reconcile:276 *was* exactly that bug). Engine strength = `strength_optimizer_slots`. `freshCombinedPrefs` is re-derived from `goal.training_prefs` (NOT the gcp response) — the `:904` strip is what keeps it wizard-only; don't "thread the gcp response" thinking it matters.
- **Decision:** D-017.

### Bug 1 Piece B — trade-off consolidation duplicate (Slices 1 & 2)
- **Files/commits:** Slice 1 `60338100` (jargon strip `plan-generation-trade-offs.ts:~195`; delete optimizer consolidation push `week-optimizer.ts:1237`). Slice 2 `1fff344b` 2026-05-18 (delete the two surviving sibling pushes — `week-optimizer.ts:1604-1606` live co-equal-2× branch + `:1756-1758` dead twin; `stacking`, the `if (stacking)` guard, PM-timing `place(...)`, and the pin-divergence push all preserved). D-018.
- **Behavior:** the same-day quality-run + lower-body-strength consolidation trade-off has ONE owner — the builder's realized-grid `collectQualityRunLowerBodyTradeOffs` (`generate-combined-plan/week-builder.ts:~2109`). The optimizer no longer emits its canonical-pattern copy (which fired before the builder could split the day via `enforceHardEasy(grid, allowConsolidatedHardException=false)`, so it could name a day that never realizes). Athlete-visible "Schedule adjustments" shows the line once, day-accurate.
- **Verification:** deno 55/0 (`plan-generation-trade-offs` + `week-optimizer.anchor-contract`); the `04i` reproducer shows the pin-divergence line kept, the consolidation push gone, `thursday=quality_run+lower_body_strength` placement intact; the `§6.5` inline-fixture test green unchanged (no assertion flip needed). Live multi-sport co-equal-2× regen **observed 2026-05-18** — Line 1 absent, builder Line 3 present.
- **Footgun (don't re-litigate):** never re-add a QR+lower consolidation `trade_offs.push` in `week-optimizer.ts` "for parity" — the builder collector is the sole owner (the `:1237`/`:1604`/`:1756` comments say so). Any future optimizer↔builder trade-off de-dup REQUIRES the builder-coverage gate (prove B ⊇ optimizer-emit cases) first. `arc-setup-chat`'s LLM context intentionally loses the line for co-equal-2× weeks (no realized grid there) — accepted, not a regression. The inner dead `if (stacking)` block in the non-co-equal branch (~`:1755`) remains by design — see Q-013.
- **Decision:** D-018.

### Swim protocol arc (Phases 0–3 / SWIM-PROTOCOL §4.1 ramp + §6.2 pools + §6.3 selection)
- **Files/commits:** P0 `95c9b13f` (`SWIM-PROTOCOL.md §4.1` ratification — dormant `phaseProgress`/`lerp` curve documented). P1 `c1c94cec` (`generate-combined-plan/swim-protocol-volumes.ts:resolveSwimSlotYardsWithBudget` clamp → band-as-envelope lerp; `week-builder.ts` wired `swimWeekInPhase = weekInPhaseForTimeline(...)` per ADR-0002; `_shared/swim-program-templates.ts` exports `lerp`/`phaseProgress`/`roundYards`/ramp constants; `swim-volume-ramp.test.ts` regression lock). P2 `ef91c2ee` (`src/lib/plan-tokens/swim-drill-tokens.ts:SWIM_DRILL_POOLS` — `singlearm`/`616` added to base, `scull`/`scullfront`/`zipper` to build, `singlearm` to peak, `fingertipdrag` to taper). S3a `e723d246` (§6.3 ↔ §5 hierarchy ratified + in-code citations + Q-015/Q-016 deferrals). S3b `95b94aba` (`SWIM_DRILL_STROKE_PHASE` map per §6.1 column 2 + Path A distinct-phase pairing). S3d `f53bbf34` (`FOUNDATION_DRILL_SUFFIXES` + `RACE_SPECIFIC_DRILL_SUFFIXES` + `tierBias`; threaded `athleteFitness` through `speedSwim`/`thresholdSwim`/`easySwim`/`cssAerobicSwim`/`enduranceSwim` + dispatcher). Plus Q-014 `ff9600e9` (server-side `swim_equipment_suggested` redundant channel documented as intentional). D-044 item 6 `ca1e6cd0` (Q-015 threading: `prevWeekDrillTokens` opt → 7 swim creators → `pickSwimDrillInset` filter at `swim-drill-tokens.ts:521-538`). D-045 (this commit) (`generate-combined-plan/drill-token-harvest.ts` + `.test.ts`; fixes the `index.ts:286-301` wrong-shape walk that had silently emptied the Set every week — Q-015 RESOLVED end-to-end).
- **Behavior:** swim yards now ramp START→PEAK across base/build/race-specific (band IS the envelope; lerp by `phaseProgress(weekInPhase, rampWeeks)`); taper/recovery skip ramp. Drill picker honors §5 per-type counts (Path A = 2-3 for §5.1 Technique Aerobic; Path B = 1 for §5.2/§5.3/§5.4/§5.7/§5.10); §6.3 "rotates" means temporal variation across sessions, not multiple drills within one session. Path A enforces distinct §6.1 stroke phases across the 2-3 chosen drills (permissive fallback when pool diversity is the constraint). Beginner athletes bias toward §6.2 base primaries (catchup/fingertipdrag/singlearm/616); advanced athletes bias toward sighting in peak phase. Drill tokens for §6.2 missing primaries (singlearm/616/scull/scullfront/zipper) wired into the right pools; downstream per-step equipment surfacing (Garmin `SWIM_BUOY`, Form "Pull buoy") works via `inferEquipFromDrillName`'s `/scull/` branch.
- **Verification:** full `swim-drill-tokens.test.ts` + `generate-combined-plan/` deno suite **238/0** (covers `swim-volume-ramp.test.ts` § 4.1 regression lock, `swimDrillStrokePhase` §6.1 mapping, every-DRILL_EQUIPMENT_MAP-suffix-categorized coverage gate, Path A distinct-phase regression, beginner bias, advanced bias, intermediate no-regression lock). `generate-triathlon-plan/` + `materialize-plan/` **12/0** (no collateral on the legacy / token-resolver paths). Pre-existing `_shared/infer-training-fitness.test.ts:52` failure surfaced and filed (Known broken — unrelated; provably pre-existing via stash-out).
- **Footgun (don't re-litigate):** swim within-phase ramp MUST be driven by `weekInPhaseForTimeline(phaseBlocks, weekNum, block)`, NEVER `weekNum - block.startWeek + 1` (which is always 1 per ADR-0002 — silently flattens). Band-as-envelope (Option A) is locked over rewrite-floor/ceil (Option B); floor/ceiling functions are deliberately untouched. §5 per-type drill counts are AUTHORITATIVE; §6.3's "2-3 drills" is the GLOBAL DEFAULT — do NOT "fix" Path B back to 2-3 drills per session (Slice 3a in-code citations prevent this re-litigation). scull/scullfront are pull-buoy-REQUIRED in DRILL_EQUIPMENT_MAP (coaching-conservative; diverges from §6.1 column 4 "None" by design). Tri-generator's 6 `pickSwimDrillInset` call sites are intentionally NOT threaded for `athleteFitness` (legacy path; optional param preserves intermediate-equivalent behavior). Server-side `computed.swim_equipment_suggested` is incomplete on non-pull_focused sessions with pull-buoy drills — REDUNDANT channel, do not "fix" (Q-014). 3c (recent-pick memory) shipped and verified end-to-end — Q-015 RESOLVED 2026-05-25 (D-044 item 6 + D-045 harvest fix); 3e (§2 ratio scaling) still deferred — Q-016. Harvest helper at `generate-combined-plan/drill-token-harvest.ts` is the ONE place that walks `buildWeek`'s output for drill tokens — any future change to `buildWeek`'s return shape must keep `week.sessions[].steps_preset` populated or update this helper (the test file is the regression sentinel).
- **Decision:** D-020.

### Race-week protocol (Phases 1–4 / RACE-WEEK-PROTOCOL §8)
- **Files/commits:** P1 `4a63f44e` (`RaceAnchor.priority`, `PhaseBlock`/`GeneratedWeek.race_week`, priority-driven A/B + chronology guard — `phase-structure.ts`). P2 `9c393119` (`science.ts:raceDaySessionSpec` distance-aware race day; §8.4 hard-fail `validator.ts:findMissingRaceDaySessions` + `index.ts`; Issue-2 `classify-error.ts`). P3 `7221b8d5` (`phase-structure.ts` backward allocation — A-taper full `taperWeeks` never compressed, rebuild floored ≥1, Decision-A `[race-week §8.2/§8.5]` throw). P4 `f7580ec5` (Gap 6 activation-swim `opts.isRaceWeek`) / `3076ba72` (Gap 9 b/c/d soft `validator` guards) / `0b54318d` (Gap 8 T6 realized B=13/A=17 E2E) / `95bd017e` (Gap 7 doc-drift). D-019.
- **Behavior:** A-race = priority-driven (NOT calendar-order), gets the full distance-driven taper (never compressed), preceded by ≥1 rebuild after post-B recovery; B-race = raced-through (no multi-week protected taper; 70.3-B taper already 1wk). Race day = one distance-aware `type:'race'` session, always materialized (hard-fail if missing); activation-swim substitution scoped to the actual race week only. Infeasible B→A spacing hard-fails with an actionable `[race-week §8.2/§8.5]` message (Issue-2 → `race_week_infeasible`/422, wrapper-preserved).
- **Verification:** full `generate-combined-plan` deno suite **189/0** (`race-week-phase{1,2,3,4}.test.ts`, `rebuild-phase`, `classify-error`; Flag-C empty). T6 deterministically locks the realized two-70.3 (B=13/A=17, full 2wk A-taper) end-to-end. Live multi-sport regen user-verified 2026-05-18.
- **Footgun (don't re-litigate):** A/B is priority-driven — do NOT revert to calendar-order; the chronology guard intentionally HARD-FAILS priority/chronology disagreement. A-taper is inviolable — rebuild/recovery/base absorb tight windows, never the A-taper (Decision A = hard-fail, not silent compression). Gap-9 b/c/d are SOFT (advisory `console.warn`); only Gap-9a (race-day-present) is hard-fail. `bikeOpeners` over-broad `phase==='taper'` gate (`week-builder.ts:1298`) is the SAME class as the fixed Gap-6 swim sub but deliberately OUT of §8.6 (POLISH backlog). Synthetic test fixture is B=14/A=18; the *realized* contract is B=13/A=17 — don't conflate.
- **Decision:** D-019.

### §6.1 heavy-Lower classifier is blanket / load-agnostic (verified 2026-05-20 — Q-003)
- **Verified:** `_shared/week-optimizer.ts:464-467` defines `isHigh(k)` as `k === 'long_ride' || k === 'long_run' || k === 'quality_bike' || k === 'quality_run' || k === 'lower_body_strength'`. The classifier is **unconditional** on `lower_body_strength` — it reads neither `strength_protocol` nor `repProfile` nor load magnitude. Every Lower placement gets the full §4.7 24h-pre / 48h-post adjacency protection.
- **Behavior:** durability MS phase Lower gets identical adjacency rules to Strength Build / M+P / Rebuild phase Lower. The implementation is **more conservative** than `STRENGTH-PROTOCOL.md §6.1`'s load-magnitude framing technically allows — the spec lets sub-maximal Hypertrophy / Deload Lower get relaxed adjacency, but the code applies blanket protection. Safe-conservative posture; zero risk of under-protecting heavy Lower.
- **Footgun (don't re-litigate):** the Q-003 premise that "may scope to protocol names" was incorrect — the classifier is intent-blind and load-blind by design. If a future need arises (real Deload-week placement complaints), the spec's load-magnitude relaxation is the natural extension. No observed defect today. See Q-003 (resolved).

### Full IM §3.7 race-spec strength scaling (verified 2026-05-20 — Q-004)
- **Verified:** `generate-combined-plan/week-builder.ts:108-128` `strFreqForPhase` has explicit Full IM branching at line 116 (`isFullIm = d === 'ironman' || d === 'full' || d.includes('iron')`). Build phase + Full IM + `weeklyHours >= 18` → 1× strength (line 120); race-specific phase + Full IM → 1× strength (line 124). `slotsPlanned = slotsOrdered.slice(0, 1)` at `:1632` takes the first slot (Upper by session_index convention). Full IM race-spec realizes "1× upper-only" per §3.7.
- **Behavior:** Full IM athletes in race-spec get 1× strength (down from 70.3's 2×); Build at 18+hr/wk also drops to 1×. The frequency enforcement is the load-bearing piece (over-prescribing volume was the original concern). Shipped in the v2.1 close-out (`cf5867fa`, 2026-05-12).
- **Partial verification residual:** §3.7 also prescribes the single race-spec session at maintenance load (halved power volume, no depth jumps). Maintenance `repProfile` is present in `triathlon_performance.ts:501/549/1408` — framework Full IM race-spec would consume. Exhaustive trace of `triathlonStrength()` → race_specific × Full IM → maintenance repProfile selection was outside the 30-min Q-004 budget; flagged as asserted-good-by-framework. No observed defect.
- **Footgun (don't re-litigate):** the Full IM check uses substring match (`d.includes('iron')`) so legacy `"ironman"` plus modern `"full"` both resolve correctly. Don't change the substring without auditing fixture goal-distance strings. See Q-004 (resolved).

### Swim arc §10 — fitness-tier session-type selection (Plan #78 Issue 1 closure, 2026-05-21 — D-025)
- **Files/commits:** P0 spec `656dc039` (`docs/SWIM-PROTOCOL.md` new §10 with §10.1-§10.8 subsections; existing §10/§11 renumbered to §11/§12; no cross-references to old §10/§11 elsewhere in the codebase). P1 engine `6ad97ee2` — `_shared/swim-program-templates.ts` adds `RACE_70_3_SLOT_META_BEGINNER` + `FOCUS_70_3_SLOT_META_BEGINNER` parallel constants, `raceTemplatesFromYardsBeginner` / `focusTemplatesFromYardsBeginner` emitters, exported `raceTwoSwimRotationSlotMetaForBeginner(planWeek)`; dispatch in `getSwimSlotTemplates` is a `const isBeginner = athleteFitness === 'beginner'` ternary at four entry points (focus taper, focus non-taper, race taper, race non-taper rotation). 9 new pin tests in `swim-slot-templates.test.ts` (§10.6 required suite + §10.8 distance-agnostic + taper substitution lock).
- **Behavior:** for `swim_fitness === 'beginner'` athletes (the population D-024 produces from `swim_experience='learning'`), the substitution map `threshold → css_aerobic`, `race_specific_aerobic → technique_aerobic`, `speed → technique_aerobic` applies at template selection. Realized race-intent rotation: Week 1 → `[css_aerobic, technique_aerobic]` (was `[threshold, race_specific_aerobic]`); Week 2 → `[css_aerobic, pull_focused]`; Week 3 → `[technique_aerobic, technique_aerobic]`; Week 4 → `[css_aerobic, technique_aerobic]`. Focus-intent: `[css_aerobic, technique_aerobic, recovery]` with slot 1 pull/kick alternation preserved unchanged. Distance-agnostic — same substitution across sprint / olympic / 70.3 / full IM (§10.8). Intermediate / advanced athletes pass through unchanged: the four entry points keep using `RACE_70_3_SLOT_META` / `FOCUS_70_3_SLOT_META` / `raceTwoSwimRotationSlotMeta` / `raceTemplatesFromYards` / `focusTemplatesFromYards`. The Plan #78 Issue 1 closure composes with D-024's type axis fix and D-022's volume axis cap — three layers, end-to-end coverage of the high-CTL learner population.
- **Verification:** `swim-slot-templates.test.ts` → **18/0** (9 prior + 9 new — 5 §10.6 required pins, 1 distance-agnostic, 1 full beginner rotation cycle, 1 focus-intent pull/kick preservation, 1 taper substitution). Full sweep `generate-combined-plan/` + `_shared/` → **598/0** (was 589; +9 new; zero regressions). Existing rotation tests for intermediate/advanced (lines 27-118 in swim-slot-templates.test.ts) all green, locking the §10.6 anti-regression contract. Deployed `generate-combined-plan` 2026-05-21.
- **Footgun (don't re-litigate):**
  - **§10.6 anti-regression is load-bearing.** `RACE_70_3_SLOT_META` / `FOCUS_70_3_SLOT_META` / `raceTwoSwimRotationSlotMeta` / `raceTemplatesFromYards` / `focusTemplatesFromYards` MUST stay bit-identical for intermediate/advanced. 3 of the 9 new pin tests are pure no-regression locks (intermediate Week 1 race-intent, advanced Week 1 race-intent, intermediate focus-intent). Don't delete them; don't "consolidate" the parallel constants by merging into the existing meta with conditional fields.
  - **Dispatch parameter name is `athleteFitness`** (the `getSwimSlotTemplates` opt) — upstream `week-builder.ts:1216` passes `swimFitness` (D-024) which resolves to `'beginner'` for the Q-006 population. Don't rename to `swimFitness` in the helper signature; the opt name is part of the public API.
  - **Three-layer closure for Plan #78 / Plan #60 W6:** D-022 (Ticket-B per-session cap) + D-024 (`swim_fitness` clamp from `swim_experience`) + D-025 (session-type substitution). All three required to fully close. D-022 alone misses the high-CTL learner population (caps gated on `fitness === 'beginner'` which the population never reaches). D-022+D-024 caps the volume but still emits threshold sessions at the capped volume. D-022+D-024+D-025 closes both axes.
  - **Slot 1 pull/kick alternation runs for beginners too** — both types §10.2-allowed. The existing `if ((ph === 'build' || ph === 'race_specific') && slots[1])` block at `swim-program-templates.ts:436-466` mutates `slots[1]` regardless of which template emitter produced the base slot. Don't gate that alternation on `!isBeginner`.
  - **Full IM advanced endurance carve-out** at slot 2 (`swim-program-templates.ts:467-481`) gates on `athleteFitness === 'advanced'`. Beginners never hit it; slot 2 stays `recovery` per §10.4. Don't extend the carve-out condition.
  - **Plan #78 Issue 1 (this) ≠ Issue 2 (D-024).** Type axis vs. volume axis. Don't conflate in future refactors.
- **Decision:** D-025 (this slice's close-out; one D-NNN covers the spec + engine cohesively, mirroring the swim arc D-020 / run arc D-023 patterns).

### `mergeCombinedSchedulePrefs` preserves `swim_experience` (Bundle A, 2026-05-21 — re-enables D-002 + D-024 + D-025)
- **Files/commits:** `370097c1` — `_shared/combined-schedule-prefs.ts` (`CombinedSchedulePrefs.swim_experience` type field + merge logic mirroring the `swim_intent` pattern at `:376-378` + `:459`: defensive snake/camel alias, lowercase normalization, value-narrowing typeguard); `_shared/combined-schedule-prefs.test.ts` NEW (9 pin tests including explicit Plan #78 end-to-end chain). No D-NNN — one-line preservation fix; the design decisions are documented in D-002 / D-024 / D-025.
- **Behavior:** the merge function silently dropped `swim_experience` from its output pre-fix. The field was in the input `training_prefs` but absent from the merged `freshCombinedPrefs` object that the create-goal pipeline reads downstream. Two consumers broke as a result:
  1. **`inferTrainingFitnessLevel`** (D-002 soft `-1` signal, shipped `0fd17ad9` 2026-05-13 — **silent since day one**). High-CTL athletes who declared `swim_experience='learning'` never got the documented `score -= 1` nudge. The wizard-experience signal had been ignored at the inference layer for 8 days.
  2. **`deriveSwimFitness`** (D-024 hard clamp, shipped `8d1315af` earlier this session — **silent on arrival**). The clamp received `undefined` and fell through to `return trainingFitness` → `swim_fitness = 'intermediate'` → D-025's beginner substitution gate at `getSwimSlotTemplates` (`athleteFitness === 'beginner'`) never matched. The three-layer Q-006 closure (D-022 + D-024 + D-025) was structurally complete but the data pipeline never delivered the right signal.
  The duplicate athlete_state read at `create-goal-and-materialize-plan/index.ts:1719-1725` (which bypasses `freshCombinedPrefs` entirely and reads directly from `newGoal.training_prefs`) is what kept `athleteState.swim_experience` correctly populated downstream — so `learnerSwimExperience(athleteState.swim_experience)` checks in `week-builder.ts` (e.g. heavy-aerobic-swim-off-quality-run scheduling) kept working. The bypass masked the merge defect for everyone else.
- **Verification:** `combined-schedule-prefs.test.ts` → **9/0**. Full sweep `_shared/` + `generate-combined-plan/` → **607/0** (598 + 9 new). Plan #78 end-to-end pin proves the chain: `mergeCombinedSchedulePrefs({swim_experience:'learning'}).swim_experience === 'learning'` → `deriveSwimFitness('intermediate', merged.swim_experience) === 'beginner'`. Deployed `create-goal-and-materialize-plan` 2026-05-21. `generate-combined-plan` not redeployed (consumes `swim_fitness` via athlete_state payload — no engine code change required at the engine side).
- **Footgun (don't re-litigate):**
  - **Any field that lives in `training_prefs` AND is consumed downstream via `freshCombinedPrefs.<field>` MUST be in the `mergeCombinedSchedulePrefs` preservation list** (lines `:445-469`-ish). Silent drops are invisible at compile time and at the deploy log. The 9 pin tests in `combined-schedule-prefs.test.ts` lock the contract for `swim_experience`; future fields should add equivalent pins.
  - **The duplicate `swim_experience` read at `:1719-1725`** (workaround that bypasses the merge) is **load-bearing** — `athleteState.swim_experience` is consumed by `learnerSwimExperience` checks in `week-builder.ts:1504` and `shouldMaintainTwoSwimsInRecovery` at `:1197`. Don't refactor to "consolidate" the read through `freshCombinedPrefs.swim_experience` thinking it's redundant; both reads serve different purposes.
  - **D-002, D-024, D-025 are now all simultaneously functional** for the Plan #78 / Plan #60 W6 population. The three-layer closure (volume cap + swim_fitness clamp + type substitution) composes end-to-end on next regenerate.
- **Decision:** no new D-NNN (one-line preservation fix; design decisions documented in D-002 / D-024 / D-025). Q-006 RESOLVED entry in `OPEN-QUESTIONS.md` updated to note the wiring bug.

### Phase 0 Arc channel — dynamic Arc data threaded into `generate-combined-plan` (2026-05-22 — D-032)
- **Files/commits:** Spec `30e88008` (`docs/PHASE-0-ARC-CHANNEL-SPEC.md`); implementation `ad4102f8`.
  - `generate-combined-plan/types.ts` — new `ArcChannelPayload` interface (conservative 4-field subset: `latest_snapshot`, `cycling_fitness`, `swim_training_from_workouts`, `longitudinal_signals`). `CombinedPlanRequest` gains optional `arc?: ArcChannelPayload`. Imports `AthleteSnapshot` + `SwimTrainingFromWorkouts` from `_shared/arc-context.ts` and `LongitudinalSignals` from `_shared/longitudinal-signals.ts`.
  - `generate-combined-plan/index.ts` — destructures `arc` from the request body; threads into `buildWeek` options at the existing call site.
  - `generate-combined-plan/week-builder.ts` — `buildWeek` options shape gains `arc?: ArcChannelPayload`. Docstring documents the engine read pattern (`arc?.field?.subfield` optional chain; fall back to baselines when undefined). NO consumer reads `options.arc` in Phase 0.
  - `create-goal-and-materialize-plan/index.ts` — wrapper populates `arc` at the existing `invokeFunction('generate-combined-plan', { ... })` call site (~line 1567). Built manually from the existing `arcForCombined` (no `arcForCombined` pass-through — explicit 4-field object enforces curation).
  - `generate-combined-plan/arc-channel.test.ts` (NEW) — 7 tests: 5 byte-identical hash assertions across fixtures mirroring existing e2e shapes (Plan #78, 70.3 base_first advanced, Full IM, D-031 spike reproducer, Sprint short plan) + 2 hash-function sanity checks.
- **Behavior:** `generate-combined-plan` accepts an optional `arc` field on its request body. The wrapper channels Arc's dynamic 4-field subset into the engine. **No engine code path reads `options.arc` in Phase 0** — behavior-neutral by contract. Plans generated with `arc: undefined` and `arc: populated` produce byte-identical `sessions_by_week` output (SHA-256 hash verified across 5 fixtures). Phase 1-4 consumers add their reads with explicit hash-test updates as each phase ships. Legacy callers that omit `arc` get exactly today's behavior (the existing semantics) — verified by full-suite green.
- **Verification:** `arc-channel.test.ts` → 7/0 (5 fixture byte-identical assertions × 1 mode-pair each + 2 sanity). Full sweep `generate-combined-plan/` + `_shared/` + `materialize-plan/` + `src/lib/` → **931/0** (was 924/0 → +7 new). Every pre-existing test unchanged. Deployed `generate-combined-plan` + `create-goal-and-materialize-plan` 2026-05-22.
- **Footgun (don't re-litigate):**
  - **`arc?: ArcChannelPayload` stays optional.** Default = undefined. Don't change to empty-object default; the explicit-undefined pattern is what keeps consumers honest about back-compat.
  - **Curated 4-field subset stays curated.** Adding a field requires documenting which phase consumes it + what for. Conservative-4 is the starting point; phases extend deliberately. `recent_completed_events`, `arc_narrative_context`, `five_k_nudge` deliberately excluded — they get added when their first consumer ships.
  - **Engine read pattern: `arc?.field?.subfield` (optional chain throughout).** Phase 1+ consumers fall back to baselines when undefined. Never `arc.latest_snapshot.run_*` without `?.`.
  - **Hash test stays load-bearing.** Phase 1+ tests adding consumers MUST update `arc-channel.test.ts` to reflect that `arc: populated` and `arc: undefined` now produce different outputs FOR THE CONSUMED FIELD. The Phase 0 hash test is the contract; consumers are explicit additions, not silent regressions.
  - **No re-fetch inside the engine.** `generate-combined-plan` never calls `getArcContext()` directly. Wrapper fetches; engine consumes from request body. Protects engine's pure-function-of-inputs property + preview-mode contract.
  - **Wrapper builds the `arc` object explicitly with 4 fields.** Don't pass `arcForCombined` whole — that would smuggle non-curated fields. The explicit construction at create-goal:~1567 is the curation enforcement point.
- **Decision:** D-032 (Phase 0 of the feedback-loop closure work order — `docs/FEEDBACK-LOOP-WORKORDER.md`. Behavior-neutral foundation. Phases 1-4 unblocked — next candidate: Phase 1 run pace per user direction).

### Phase 1 run pace feedback loop — `resolveRunEasyPace` reconciler under streak + median + ACWR gates (2026-05-22 — D-033)
- **Files/commits:** Spec `b8f1e626` / `d87be8ef` (`docs/PHASE-1-RUN-PACE-SPEC.md`); Path B amendment + implementation in this commit.
  - `generate-combined-plan/types.ts` — new `RunObservedFitness` interface; `ArcChannelPayload.run_observed_fitness: RunObservedFitness | null` added (Phase 1 extension of D-032's curated 4-field subset).
  - `generate-combined-plan/science.ts` — new `ResolvedRunEasyPace` type + pure `resolveRunEasyPace(baseline, observed)` reconciler. Pure function — no DB / env / side effects. 8-case decision tree per spec §5.3.
  - `generate-combined-plan/index.ts` — request handler calls `resolveRunEasyPace` after validation; when `source !== 'baseline'`, overrides `state.learned_fitness.run_easy_pace_sec_per_km.value` in-place (downstream code via shallow spreads shares the reference). Best-effort try/catch — reconciler exceptions never block plan generation.
  - `create-goal-and-materialize-plan/index.ts` — new `buildRunObservedFitness(supabase, user_id)` helper aggregates last 4 weeks of `athlete_snapshot` (selects `run_easy_pace_at_hr`, `run_long_run_duration`, `run_interval_adherence`, `acwr`); 3-of-4 minimum sample gate. Populated at the existing `invokeFunction('generate-combined-plan', { arc: { ... } })` call site (~line 1567).
  - `generate-combined-plan/arc-channel.test.ts` — Phase 0 byte-identical contract narrowed to `run_observed_fitness === null` explicitly; doc comment explaining the reconciler runs in `index.ts` (not `buildWeek`), so the buildWeek-level hash test is unaffected.
  - `generate-combined-plan/run-pace-feedback.test.ts` (NEW) — 22 tests: 10 spec §6 scenarios (6.1–6.10 incl. LOAD-BEARING ACWR-gate triad 6.7/6.8/6.9 + LOAD-BEARING §6.10 Path B regression pin) + 12 unit tests (boundary cases, sample-count gating, ACWR null partial-data tolerance, source-enum exhaustiveness, anti-volatility PR-week pin).
- **Behavior:** the engine's `state.learned_fitness.run_easy_pace_sec_per_km.value` can be transiently displaced at request-handler entry when observed easy pace (4-week median of `snapshot.run_easy_pace_at_hr`) sustained-diverges from baseline. Override is in-memory only — never persisted to `user_baselines`; each plan regen re-evaluates against current observed data. **Engagement requires three independent gates to clear:** (1) streak — ≥2 consecutive worsening weeks at >+4% above baseline, or ≥4 consecutive improving weeks at >-4% below; (2) median — 4-week median outside the ±4% band in the matching direction; (3) ACWR ≤ 1.3 over the worsening window (worsening path only; improving has no ACWR gate). The Path B amendment locked gates (1) and (2) to be independent and BOTH required — streak-alone engagement under moderate ACWR was demonstrated to be too aggressive (2-week noise at ACWR 1.1–1.2 could displace baseline). Output `source` enum has 5 values: `baseline` / `reconciled_worse` / `reconciled_better` / `observed_no_baseline` / `baseline_acwr_gated`. Per-distance scoping: ONLY `learned_fitness.run_easy_pace_sec_per_km` is reconciled — `performance_numbers.fiveK_pace` is untouched (athlete-controlled race-pace baseline).
- **Verification:** `run-pace-feedback.test.ts` → **22/0** (10 spec §6.1–§6.10 + 12 unit). Full sweep `generate-combined-plan/` + `_shared/` → **781/0** (was 780, +1 e2e scenario file with 22 tests but Phase 0 hash test count unchanged). The §6.10 LOAD-BEARING regression pin (`[380, 378, 360, 358]` weekly + `[1.10, 1.15, 1.05, 1.00]` ACWR → `baseline` required) is locked with an explicit assertion message naming both regression failure modes (`reconciled_worse` = Path B regressed; `baseline_acwr_gated` = gate ordering regressed). Deployed `generate-combined-plan` + `create-goal-and-materialize-plan` 2026-05-22.
- **Footgun (don't re-litigate):**
  - **Engagement requires BOTH streak AND median (Path B locked).** Don't simplify to streak-alone engagement under any ACWR rationale — the §6.10 regression pin catches this. Locked invariant per spec §8 + DECISIONS-LOG D-033.
  - **ACWR gate fires AFTER streak + median both fire.** If either of the first two gates fails, result is `'baseline'`, NOT `'baseline_acwr_gated'`. The latter is reserved for the case where pace evidence is unambiguous (streak + median both cross) but workload evidence suggests fatigue (ACWR > 1.3). Don't fold the ACWR check into the median-or-streak short-circuit.
  - **Improving path has NO ACWR gate by design.** A faster-pace signal during a hard block is unambiguous: fitness improving despite high load. Don't add an ACWR gate to the improving path "for symmetry."
  - **The override is in-memory only.** Don't add a write to `user_baselines` to "persist" the reconciled value. Each regen re-evaluates against current observed data; that's the recovery mechanism if the override turns out to be wrong.
  - **The reconciler is pure.** No DB reads, no env reads, no side effects. 22 pin tests rely on this. Don't add caching, logging that mutates state, or DB calls.
  - **`performance_numbers.fiveK_pace` is NEVER touched by the reconciler.** Race-pace prescriptions, interval-pace anchors (5K-pace strides, 1600m repeats at threshold) all derive from `performance_numbers`, not `learned_fitness`. Don't extend the reconciler to other run-pace fields without re-litigating the anti-cross-pollination rule (spec §4.6).
  - **`run_observed_fitness === null` from the wrapper means insufficient data** (fewer than 3 of 4 weeks with `run_easy_pace_at_hr` samples). Engine treats it as a no-op. Don't infer "observed is null because reconciler hasn't run yet."
  - **The asymmetric ratchet ratio is 2× (worsening 2 weeks / improving 4 weeks).** Don't make it symmetric ("for fairness") — fast worsening engagement is the safety-favored bias for a training plan. Spec §4.3 + §8 lock the asymmetry direction (worsening faster than improving) as non-negotiable.
  - **`run_efficiency` field is `null` on the payload** because `compute-snapshot` doesn't aggregate it at week granularity. Reserved for future extension; reconciler doesn't depend on it. Don't infer "efficiency_index null = data missing"; it's just not populated yet.
  - **Workorder pauses after Phase 1 per user direction.** Phases 2 (strength) / 3 (cycling Arc-to-plan) / 4 (swim aggregation) remain queued — D-034 / D-035 / D-036 — but don't proactively roll into them. See `docs/FEEDBACK-LOOP-WORKORDER.md`.
- **Decision:** D-033 (Phase 1 of the feedback-loop closure work order. First feedback loop closed end-to-end. Phase 2-4 deferred per user direction.)

### Rebuild-mode lerp throttle + peak-of-base floor (2026-05-22 — D-031, amends D-026/D-028/D-029)
- **Files/commits:** `0badf064`.
  - `generate-combined-plan/science.ts` — `longRunMilesForWeek`, `longRideHoursForWeek`, `brickRunMilesForWeek` gain optional `loadThrottle` parameter (default 1.0). When `loadThrottle < 1.0`, lerp output is multiplied by throttle and FLOORED at the distance-aware peak-of-base value (`longRunFloorMiles(distance, 'base')` / `longRideFloorHours(distance, 'base')` / `brickRunTargetMiles(distance, 'base')`). Distance-aware: 70.3 → 10mi/2.25h/2.5mi; full IM → 13.5mi/4.5h/5.0mi; Olympic → 5.5mi/1.25h/1.5mi; Sprint → 3.0mi/0.75h/1.5mi.
  - `_shared/swim-program-templates.ts: getSwimSlotTemplates` — gains `loadThrottle` in opts. Per-anchor floor = `BASE_VS_BUILD_YARD_SCALE × build_START_YDS` per anchor (intent-aware: focus 70.3 → `[1760, 1600, 1440]`, race 70.3 → `[1760, 1600]`). Applied AFTER `protocolMidVolumeMultiplier`.
  - `generate-combined-plan/week-builder.ts` — derives `loadThrottle = (phase ∈ {base, build, race_specific}) ? Math.min(1.0, block.tssMultiplier) : 1.0`. Threaded into all four lerp call sites: `longRunMilesForWeek` (long-run), `longRideHoursForWeek` (long-ride), `brickRunMilesForWeek` (brick-run; uses `brickPhaseForSession` for its own throttle gate), `getSwimSlotTemplates` (swim yards). **REMOVED** the previous redundant `Math.max(longRunFloorMiles(distance, phase), Math.round(longRunMiles * 0.86))` line — that line's peak-of-current-phase floor was the convergence-failure root cause for the 25.5% spike. TSS-share cap in the same `physiologicalFloorRebuild` block stays (separate invariant).
  - `generate-combined-plan/validate-training-floors.ts` — `effectiveLongRunFloorMiles` + `effectiveLongRideFloorHours` gain `loadThrottle` (sixth param, optional). Both soft (`evaluateLongDayVolumeFloors`) and hard (`enforceLongDayFloors`) call sites compute `throttleSoft`/`throttleHard` from `block.tssMultiplier` per week and thread through. Without this parity, validator would re-bump throttled long sessions back up and defeat the rebuild loop.
- **Behavior:** the rebuild loop (`tightenPhaseBlocksForFloorRebuild` × 0.87 per pass, up to 12 passes + 1 deep) now has traction on the canonical lerps. Plans that previously failed with `WEEK_OVER_WEEK_TSS_RAMP` after rebuild-exhausted now converge — the throttled lerp shrinks alongside the budget, floored at peak-of-base for durability anchor preservation. The pre-fix failure mode (canonical lerps emitted spec values regardless of `block.tssMultiplier`, so the long sessions stayed constant while other sessions shrank → WoW step couldn't close) is closed. Normal generation behavior is unchanged (default `loadThrottle = 1.0` for every legacy caller; D-026 / D-028 / D-029 lerp values exactly as before).
- **Verification:**
  - `lerp-rebuild-throttle.test.ts` (21/0) — unit pins: back-compat (throttle=1.0 default unchanged); throttle 0.5 / 0.87 / 0.30 / 0 across distances (floor binds when lerp dips below); distance-keyed for full IM (13.5mi / 4.5h), Olympic (5.5mi), Sprint (3.0mi); validator parity (`effectiveLongRunFloorMiles` matches `longRunMilesForWeek` output); ADR-0002 anti-regression (lerp still varies with `weekInPhase` above floor); non-ramp phases (rebuild/taper/recovery) delegate to floor function — throttle inert.
  - `d031-convergence-e2e.test.ts` (4/0) **— load-bearing**: headline test asserts BOTH halves of the contract:
    1. Reproducer fixture (Olympic 13-wk close, CTL=65, 11hr/wk, advanced race_peak) genuinely fails initial validation with `WEEK_OVER_WEEK_TSS_RAMP: Week 8: 22.9%` — test refuses to silently pass on a non-load-bearing fixture (if the fixture stops reproducing the spike, this assertion fires).
    2. Rebuild loop converges (`ok=true`) AND engaged (`passes > 0`) — converges in 1 pass for this fixture.
  - WoW guardrail test asserts every same-phase delta < 20% post-rebuild. Two durability-anchor tests assert throttle-protected weeks honor the peak-of-base floor AND inspect at least one throttled week (refuse to silently pass when no week is actually throttle-protected).
  - Full sweep `generate-combined-plan/` + `_shared/` + `materialize-plan/` → **796/0** (was 771 → +25 new). Zero regressions. Deployed `generate-combined-plan` 2026-05-22.
- **Footgun (don't re-litigate):**
  - **`loadThrottle` default = 1.0.** Don't change the default to 0.87 or "auto-detect rebuild mode" — explicit-opt-in keeps the canonical-lerp contract clear for normal generation. Throttle is a per-week derived value from `block.tssMultiplier`, threaded by week-builder + validator only.
  - **Floor source MUST be peak-of-BASE, not peak-of-current-phase.** Peak-of-current-phase was the convergence-failure root cause (the removed line). Peak-of-base is the smallest "real" long session per the spec. Distance-aware via `*FloorMiles/Hours(distance, 'base')` / per-anchor swim base-peak.
  - **Throttle only fires for base/build/race_specific phases.** Taper / recovery / rebuild keep their phase-multiplier logic unchanged. If you add a new phase type, decide explicitly whether it joins the throttle path.
  - **Validator parity is load-bearing.** Both soft and hard validators thread the throttle. New validator code paths using the effective-floor helpers must pass `loadThrottle` or they'll re-bump throttled sessions and break the rebuild loop.
  - **The e2e fixture is the canary** — Olympic 13-wk close (CTL=65, 11hr/wk advanced race_peak) genuinely reproduces the pre-fix WoW spike. The headline test asserts both halves; refuses to silently pass if either half stops holding. Don't change the fixture without re-verifying.
  - **The removed redundant line MUST stay removed.** `Math.max(longRunFloorMiles(distance, phase), Math.round(longRunMiles * 0.86))` at the long-run rebuild site would re-introduce the convergence failure. The TSS-share cap below it stays — that's a separate share-of-budget invariant.
  - **Issue 2 of the same investigation** — delete-goal idempotent on 404 — shipped at `5ccfdcdc`. Backend architectural follow-up (atomic goal rollback via wizard-inserted-goal list) filed but unscheduled.
- **Decision:** D-031 (amends D-026 + D-028 + D-029; the canonical-lerp contracts hold for normal generation; rebuild mode is the explicit, bounded, documented exception).

### Swim CSS-kill + per-step drill equipment (2026-05-22 — D-030, companion to D-029)
- **Files/commits:** Spec `3833024f`; Step 2 `22642fa4`; Steps 3+4 `9d178ca9`.
  - `docs/SWIM-PROTOCOL.md` — new §0.5 "Athlete-facing effort vocabulary" with canonical session-type → tier mapping table + anti-regression rule listing every athlete-facing surface that must stay free of internal swim jargon.
  - `supabase/functions/generate-combined-plan/session-factory.ts` — `cssAerobicSwim` session name "Moderate Aerobic Swim — N yd" (was "CSS Aerobic Swim"); description "moderate effort — sustainable and conversational" (was "comfortable CSS pace") + "high-volume aerobic blocks" (was "high-volume CSS blocks"); zone string "Z3 moderate aerobic" (was "Z3 CSS aerobic"); `swimCssFallbackCue` text rewritten to mention "100yd pace baseline" (was "CSS pace yet").
  - `supabase/functions/_shared/plan-generation-trade-offs.ts:269` — `no_swim_threshold_pace` template rewritten; points at "100yd pace" baseline field + next regenerate (was "CSS pace targets" + "200yd time trial").
  - `src/components/ArcSetupWizard.tsx` — §7.5 learning-swimmer hint rewritten ("No swim pace baseline?" → 100yd pace; was "No CSS test?" → CSS pace).
  - `src/utils/swimPlanTokens.ts` — aerobic-css display prefix `CSS Nxdist` → `Nxdist` (renderer prefixes "Aerobic " separately).
  - `src/utils/formGogglesSwimScript.ts` — narrator regex fallback drops "moderate CSS pace" / "moderate aerobic" branches; intensity-preference path (D-029 Slice 1) handles modern steps; CSS regex now → 'moderate'.
  - `supabase/functions/materialize-plan/index.ts`:
    - `swimTokenIntensity(token, sessionTags?)` refactored — session-tag-aware. Step-kind rules (WU/CD/drill → easy) always win. Token-keyed rules for `swim_aerobic_css_*` → moderate and `swim_threshold_*` → hard unchanged. Plain aerobic / pull / kick tokens now route through session-tag inspection per §0.5 mapping. 1-arg back-compat preserved.
    - Step.label renamed to the tier word in all four work branches (`'css'` / `'threshold'` / `'aerobic'` / `'pull'` / `'kick'` → `swimIntensity`). Closes the leak from any consumer reading label without checking intensity.
    - New `drillLabelWithGear` inline helper at drill push sites — appends "(fins)" / "(fins, snorkel)" etc. to the drill step label when athlete owns the §6.6 recommended gear. Lowercase inside parens. Wired into both `swim_drill_*` and `swim_drills_*` token branches.
    - Imports extended with `swimDrillEquipmentFromTokens`, `swimGearLabelForDisplay`, `swimGearNormalized`.
  - `supabase/functions/send-workout-to-garmin/index.ts` — unchanged (already prefers `st.intensity` from D-029 Slice 1; label-rename below is the back-compat path).
- **Behavior:** athlete-facing copy uses {easy, moderate, hard} as the only effort vocabulary. Watch face / Form Goggles per-step labels render `moderate`, `hard`, `easy` — never `css`, `threshold`, `aerobic`. Session names + descriptions + zone strings + fallback cue + trade-off message + wizard hint all free of "CSS" / "Critical Swim Speed". Drill steps surface owned recommended gear in the label: `Drill — Fingertip Drag (fins)` when athlete owns fins; bare `Drill — Fingertip Drag` otherwise. Required equipment still flows through the separate `step.equipment` field. Internal identifiers (`css_aerobic` discriminator, `cssAerobicSwim` function name, `swim_aerobic_css_*` token grammar, `'css_aerobic'` tags, `swim_threshold_pace` field, helper function names) all unchanged.
- **Verification:** 4 new pin-test files:
  - `swim-css-kill-e2e.test.ts` (13/0) — end-to-end pin on session name + description + zone string + fallback cue + trade-off message + every swim session function (cssAerobicSwim / thresholdSwim / speedSwim / easySwim Technique Aerobic / pullFocusedSwim / recoveryEasySwim) must all be free of internal swim jargon per §0.5 anti-regression rule.
  - `swim-css-kill.test.ts` (13/0) — `swimTokenIntensity` session-tag-aware mapping for every session-type × token-kind combination; step-kind rules win for WU/CD/drill; back-compat for 1-arg calls.
  - `swim-drill-equipment-label.test.ts` (10/0) — `drillLabelWithGear` behavior for fingertipdrag / fist / catchup / zipper / sighting; owned vs not-owned ownership filtering; token-suffix robustness with `_r15` + `_fins` decorations; Garmin-friendly lowercase format.
  - Updated existing pins (`swim-css-fallback.test.ts`, `swim-race-specific-ow.test.ts`) — `FALLBACK_FRAGMENT` updated to new text; trade-off template asserts "100yd pace" + anti-regression "must NOT carry 'CSS'".
  - Full sweep generate-combined-plan/ + _shared/ + materialize-plan/ + src/lib/ → **899/0** (was 735 → +164 across this arc + D-029 unification). Zero regressions. Deployed generate-combined-plan, materialize-plan, send-workout-to-garmin 2026-05-22.
- **Footgun (don't re-litigate):**
  - **Internal identifiers stay in code.** `css_aerobic` discriminator, `cssAerobicSwim` function name, `swim_aerobic_css_*` token grammar, `'css_aerobic'` tags, `swim_threshold_pace` field — all unchanged. The strip applies to athlete-facing STRINGS, not engineering identifiers. Token parsers key on `swim_aerobic_css_` literal — renaming the function or token would break expansion.
  - **Wizard prompts at "100yd pace baseline", not "CSS test".** Athletes enter the 100yd pace via TrainingBaselines `performanceNumbers.swimPace100`; the internal `swim_threshold_pace` derives from this at materialize time via `swimSecPer100YdFromArcSwimInputs`. The engineering chain stays; the wizard surface vocabulary doesn't reference it.
  - **`swimTokenIntensity` session-tag rules apply BELOW the token-keyed rules.** `swim_threshold_*` → hard regardless of session tags; `swim_aerobic_css_*` → moderate regardless. This is intentional precedence (token wins). Race-Specific Aerobic substitution → label "moderate" not "hard" — `swim_aerobic_css_*` token rule fires before the `race_specific_swim` session-tag rule. Documented + pin-tested. If you want race_specific to override, flip the rule order in the helper.
  - **Step.label is now the tier word for swim work steps.** Constrained set: {`easy`, `moderate`, `hard`}. Any downstream code doing label-based session-type detection breaks — none currently (verified via grep), but document the contract change. Drill steps keep the drill-name format with optional gear parenthetical.
  - **Drill-label equipment hint surfaces ONLY recommended gear** (§6.6 `recommended` field). Optional gear stays in the session-level Pool gear line.
  - **Tier-gated 616 → fins beginner-only NOT in drill-label hint.** The helper is called tier-agnostically. Tier context lives in the session-level `recommended:fins` tag from D-029 Slice 3 — two surfaces, two purposes. If a future audit wants the drill label tier-aware, the helper needs an `athleteFitness` arg + row context (not on row directly today).
  - **§0.5 anti-regression rule is the authoritative surface inventory.** Any new athlete-facing surface (new trade-off, new chip, new export channel, new wizard prompt) MUST audit for internal swim jargon before shipping. Pin tests in `swim-css-kill-e2e.test.ts` are the canary.
- **Decision:** D-030 (companion to D-029; closes the CSS-vocabulary axis that D-029 left parked).

### Swim arc — research-backed §5.2 / §6 / §8 revision (2026-05-22 — D-029)
- **Files/commits:** Spec `c1492b15`; Slice 1 `92af2072` (tier rest + effort-tier export propagation); Slice 2 `fc517e12` (§5.2.1 within-phase lerp); Slice 3 `130de4b2` (fins/paddles split + drill-equipment map + sculling hard-gate). Five fixes shipped, one deferred (Q-020 ankle band).
  - `generate-combined-plan/session-factory.ts` — new helpers `cssRestSecByTier`, `cssRestSecByPhaseWeek`, `swimSessionRecommendedGear`; `cssAerobicSwim` gains `weekInPhase` + `rampWeeks` options + emits tier-aware rest in description and token (`swim_aerobic_css_*x100yd_r{20|25|10|...}`); `easySwim` (Technique Aerobic path) emits `recommended:fins` for beginners when owned; `appendPoolGearLine` extended with `sessionRecommended` + `athleteFitness`.
  - `generate-combined-plan/week-builder.ts` — `swimFromTplOpts` populates `weekInPhase` + `rampWeeks` from existing `swimWeekInPhase` and `rampWeeksForPhase(phase)`.
  - `src/lib/plan-tokens/swim-drill-tokens.ts` — `DrillEquipment` gains optional `recommended` field; `DRILL_EQUIPMENT_MAP` updated for fingertipdrag / fist / 616 per §6.6; `SWIM_DRILL_RECOMMENDED_FINS_BEGINNER_ONLY` set for tier-gated dispatch; `swimDrillEquipmentFromTokens` accepts optional `athleteFitness` + token-suffix regex strip fix; `SwimSessionGearLineOpts` extended with `sessionRecommended` + `athleteFitness`; `buildSwimGearLine` renders three sections (Required / Recommended / Optional) with dedupe priority; `pickSwimDrillInset` hard-bans sculling from beginner inset via post-filter on `phaseDrillCandidates`.
  - `materialize-plan/index.ts` — new `swimTokenIntensity` exported helper maps token-kind to `easy` / `moderate` / `hard`; each swim step (drill / work / WU / CD) carries `intensity` field; `inferSwimEquipmentPack` recognizes `recommended:*` tags and merges into chip-surface optional bundle.
  - `send-workout-to-garmin/index.ts` — swim `effortLabel` prefers `st.intensity` (athlete sees "moderate" / "hard" / "easy" on the watch face, not internal session-type tags); falls back to legacy `label` for steps without the new field.
  - `src/utils/formGogglesSwimScript.ts` — narrator prefers `st.intensity` over label-regex; falls back for pre-arc materialized plans.
  - `docs/SWIM-PROTOCOL.md` — §5.2 tier-rest table, NEW §5.2.1 within-phase lerp endpoints, §6.1 tier-gate column, §6.4 ankle-band coaching note, NEW §6.6 drill-equipment map, §8.1 ankle-band candidate enum note, §8.4 fins/paddles split revision.
- **Behavior:** swim CSS Aerobic 100yd rest is now tier-adjusted AND lerps across the phase ramp. End-to-end: beginner 70.3 base wk 1 → 25s rest, wk 6 → 20s rest, build → 20s flat. Intermediate/advanced base wk 1 → 15s, wk 6 → 12s, build → 12→10s, race-spec → 10s flat. Beginner Technique Aerobic + CSS Aerobic surface `Recommended: Fins` in Pool gear line when athlete owns fins (paddles still suppressed). Sculling never appears in the beginner inset regardless of phase / pool diversity. Per-step Garmin export labels each swim work step with the athlete-facing effort tier (`moderate` / `hard` / `easy`) instead of internal session-type tags; Form Goggles narrator does the same. Race-Specific Aerobic substitution (`raceSupport=true`) keeps its inline 15s string — Slice 1 scope decision; Slice 2 lerp does NOT route through that branch.
- **Verification:** `swim-css-rest-tiers.test.ts` (9/0) — tier rest START + token grammar + Slice 1 scope guard. `swim-css-rest-lerp.test.ts` (20/0) — endpoint math for every tier × phase × wip combination; Slice 1 fallback; raceSupport scope guard; ADR-0002 anti-regression (wip=1 ≠ wip=N); continuity at lerp START. `swim-intensity.test.ts` (9/0) — `swimTokenIntensity` mapping for every token kind; case-insensitive; defensive default. `swim-fins-paddles-split.test.ts` (18/0) — beginner/intermediate/advanced × technique/CSS/pull-focused/race-spec carve-out permutations; §5.5 paddles-suppressed anti-regression; sculling hard-gate across 12 plan weeks × 7 salt values; §6.6 drill-equipment map per drill; §6.6+§8.4 dedupe in gear line. Full sweep `generate-combined-plan/` + `_shared/` + `materialize-plan/` → **735/0** (was 697 → +38 new). Zero regressions. Deployed `generate-combined-plan`, `materialize-plan`, `send-workout-to-garmin` 2026-05-22.
- **Footgun (don't re-litigate):**
  - **`weekInPhase` AND `rampWeeks` must both be threaded for the §5.2.1 lerp to engage.** Either omitted → falls back to Slice 1 tier helper (START of phase). Test fixtures constructing isolated cssAerobicSwim calls without these get the Slice 1 value. Pin tests assert the back-compat path.
  - **Race-spec (`raceSupport=true`) bypasses both Slice 1 tier helper AND Slice 2 lerp** — inline 15s string is the source of truth. Slice 1 scope-guard test pins this.
  - **`recommended:*` is a new tag class.** Three-channel taxonomy now: `req:` / `recommended:` / `optional:`. New gear-surfacing rules must touch all four consumers (description prose, chip surface, render, Garmin/Form Goggles labels) — list in the D-029 footgun.
  - **Sculling hard-gate is post-filter at `pickSwimDrillInset`, not at the phase pool.** Beginner-only. Intermediate / advanced still see sculling.
  - **6-3-6 tier-gated fins** lives in the `SWIM_DRILL_RECOMMENDED_FINS_BEGINNER_ONLY` set — single source of truth. Add new tier-gated drills to that set; don't sprinkle conditionals inline.
  - **`swimDrillEquipmentFromTokens` token-suffix regex** now strips trailing `_r\d+` and `_(fins|board|buoy|snorkel)` markers — pre-fix it didn't, and lookup failed for production tokens like `swim_drills_3x100yd_fingertipdrag_r15`. Don't revert.
  - **Per-step `intensity` falls back to 'easy'** for unrecognized tokens. New swim token kinds need explicit mapping in `swimTokenIntensity`.
  - **Q-020 ankle band is deferred — engine surface blocked on wizard scope decision.** §6.4 prose references the coaching value but the engine doesn't surface it. Don't "fix" the missing engine surface without resolving the wizard scope question first.
  - **CSS terminology strip is separately parked** per user direction — different decision than this arc. Don't bundle.
- **Decision:** D-029.

### Cycling arc Phase 1 — within-phase volume ramp + validator parity (bundled, 2026-05-21 — D-028)
- **Files/commits:** `61faf828` — `science.ts` (new `LONG_RIDE_RAMP_ENDPOINTS` table base 0.65→0.75 / build 0.75→0.85 / race_specific 0.85→1.00 per CYCLING-PROTOCOL §4.5 LOCKED 2026-05-21 + new `longRideHoursForWeek(distance, phase, weekInPhase, rampWeeks)` lerp helper mirroring `longRunMilesForWeek`; peak target reuses `expectedBikeDurationHours` so 70.3 race_specific lands at 3.0hr and full IM at 6.0hr; 0.25hr rounding); `session-factory.ts:groupRideQualityBikeSession` (signature gains `weekInPhase: number`; sweet spot + threshold rep ramps `clamp(2, 4, 2 + floor((wip-1)/2))` per §10.4 — slow ramp, longer plateau at 3 reps; VO2max rep ramp `clamp(3, 6, 3 + (wip-1))` per §5.6); `week-builder.ts:912-940` (long-ride hours derivation now routes through `longRideHoursForWeek` for base/build/race_specific non-recovery weeks, existing TSS caps applied AFTER via `Math.min`; rebuild/taper/recovery keep legacy TSS-derived path; `bikeWeekInPhase` aliases `runWeekInPhase` — calendar week is sport-agnostic; single `groupRideQualityBikeSession` call site at `:1414` threads `bikeWeekInPhase`); `validate-training-floors.ts:effectiveLongRideFloorHours` (gains optional `weekInPhase?: number, rampWeeks?: number`; routes spec through `longRideHoursForWeek` when both provided, else fallback to `longRideFloorHours` peak-of-phase; both call sites — soft eval + hard enforcer — thread the same `wipSoft`/`rwSoft` / `wipHard`/`rwHard` already computed for the D-027 run-side path; reuses the existing `weekInPhaseInline` mirror introduced for D-027); `bike-volume-ramp.test.ts` (new, 15 tests — lerp endpoints across 3 phases × 4 distances, rep ramps, ADR-0002 anti-regression wip=1 vs wip=4 must differ, end-to-end through `buildWeek` realizing 70.3 base wip=1 as 2.0hr, validator within-phase-aware floor, end-to-end enforcer doesn't peak-bump, backward-compat for legacy 3-arg `effectiveLongRideFloorHours` callers). D-028 in `DECISIONS-LOG.md`.
- **Behavior:** cycling session generation now ramps within each phase instead of landing flat at peak-of-phase from week 1. The realized progression per CYCLING-PROTOCOL §4.5 LOCKED — 70.3 long ride base **2.0 → 2.0 → 2.0 → 2.25 → 2.25 → 2.25hr**, build **2.25 → 2.25 → 2.5 → 2.5hr**, RS **2.5 → 2.75 → 2.75 → 3.0hr**; full IM scaled proportionally (peak 6.0hr) — is now what the engine emits, subject to existing TSS-budget caps. Rep ramps: sweet spot 2→3→4 across base, threshold 2→3→4 across build, VO2max 3→4→5→6 across race-specific. Pre-fix, all four were structurally flat — `groupRideQualityBikeSession` had no `weekInPhase` parameter, and long-ride hours were derived from `bikeTotalMin × constant phase share` which has no within-phase variation. CYCLING-PROTOCOL Phase 0 spec (commit `42b2d2c3`, 2026-05-21) documented these ramps; D-028 ships the implementation. Bundled — no Plan-#78-equivalent production observation drove the run-side split, so cycling ships in one commit / one D-NNN / one deploy.
- **Verification:** `bike-volume-ramp.test.ts` → **15/0** (all new). Full sweep `generate-combined-plan/` → **285/0** (was 270; +15 new); `_shared/` → **382/0**; `src/lib/` → **128/0**. Zero regressions across the suite. The pre-fix Plan #1 base-wk1 log line `longRideHours: 2.25` (peak-of-base flat) is now `longRideHours: 2` (lerp output, also confirmed in test fixture engine logs at base wip=1 for 70.3). Deployed `generate-combined-plan` 2026-05-21.
- **Footgun (don't re-litigate):**
  - **`weekInPhase` AND `rampWeeks` are both required to engage validator within-phase-aware behavior.** Mirror of D-027's footgun on the run side. Test fixtures constructing isolated `GeneratedWeek` objects without `phaseBlocks` see legacy 3-arg behavior — locked by the no-regression test in `bike-volume-ramp.test.ts`.
  - **`bikeWeekInPhase` aliases `runWeekInPhase` in `week-builder.ts`** — calendar week is sport-agnostic. Same `weekInPhaseForTimeline(phaseBlocks, weekNum, block)` output for both sports. If a future change introduces sport-specific phase boundaries, this aliasing breaks; the alias is a deliberate "if they ever diverge, split here" marker.
  - **TSS caps apply on TOP of the lerp, not under it.** Existing race-week 1.0hr ceiling, `hasTri` cap, return-from-recovery 0.85× compress, and recovery-rebuild-wk1 caps remain — they `Math.min` against the lerp output. Don't "fix" the caps to be floor-only.
  - **VO2 5-min duration is hardcoded in the function body.** Per CYCLING-PROTOCOL §5.6, only `reps` ramps; duration locked at 5 min for the race-specific VO2max template. Name + token templates both encode the literal `5`.
  - **Phase 2+ outstanding** — race-specific brick-bike race-pace closing block (§4.4), `bikeOpeners` race-week-only gating (§9.1 footgun: currently fires every taper week, not just race week), `limiter_sport='bike'` intensity dial (Phase 4, deferred). Phase 1 closes the volume axis only.
  - **Cycling parity for D-027 now landed.** The "Cycling parity is pending" entry in D-027's footgun list is closed by D-028 — `effectiveLongRideFloorHours` is now within-phase-aware on the same shape as `effectiveLongRunFloorMiles`.
- **Decision:** D-028 (cycling arc Phase 1 bundle; mirrors the run-side Phase 1 + Bundle C combo into a single slice; CYCLING-PROTOCOL §4.5 / §5.6 / §10.4 LOCKED endpoints + rep formulas are now what the engine emits, subject to TSS-budget caps).

### Validator long-run floor is within-phase-aware (Bundle C, 2026-05-21 — D-027, completes D-026)
- **Files/commits:** `d1fd0745` — `validate-training-floors.ts` (`effectiveLongRunFloorMiles` signature gains optional `weekInPhase`/`rampWeeks`; routes spec through `longRunMilesForWeek` when both provided, else fallback to `longRunFloorMiles` peak-of-phase; `EvaluateLongDayFloorsOpts` + `EnforceLongDayFloorsOpts` types gain optional `phaseBlocks`; both validator loops compute weekInPhase per-week and thread through; inlined `weekInPhaseInline` mirror of `week-builder.ts: weekInPhaseForTimeline` to avoid the circular import); `index.ts` (passes `phaseBlocks: blocks` into both opts objects; rebuild loop refreshes `longDayFloorOpts.phaseBlocks` after each `tightenPhaseBlocksForFloorRebuild` reassignment at `:260` + `:267`); `run-volume-ramp.test.ts` (+2 new D-027 pins — end-to-end Plan #78 chain through `buildWeek → enforceLongDayFloors` and legacy-fallback no-regression). D-027 in `DECISIONS-LOG.md`.
- **Behavior:** the post-build `enforceLongDayFloors` and the soft `evaluateLongDayVolumeFloors` now follow the lerp's within-phase ramp instead of bumping the realized output up to peak-of-phase. End-to-end Plan #78 / similar high-budget athlete shape (no recent run history) realizes the §4.5 LOCKED contract: **base 8.5 → 8.5 → 9 → 9 → 9.5 → 10mi; build 10 → 10 → 10.5 → 11mi; RS 11 → 11.5 → 12.5 → 13mi**. Pre-fix, Bundle B made the week-builder canonical but the validator immediately bumped 8.5mi → 10mi because `longRunFloorMiles('70.3', 'base') = 10` — the second `Math.max` floor at the second layer. History-aware path (`recentLongestRunMi × 0.5`) is **unchanged**; experienced athletes who log 10mi+ long runs still get bumped up to floor regardless of within-phase index. The protective semantics for the high-recent population are preserved.
- **Verification:** `run-volume-ramp.test.ts` → **5/0** (3 prior + 2 new D-027 pins: end-to-end Plan #78 chain locks 8.5mi survives `enforceLongDayFloors`; legacy-fallback test locks peak-of-phase 10mi when `phaseBlocks` is omitted — the 15+ existing hardcoded-floor tests in `long-day-volume-floors.test.ts` rely on this backward-compat path). Full sweep `generate-combined-plan/` + `_shared/` → **610/0** (was 608; +2 new). Zero regressions. Deployed `generate-combined-plan` 2026-05-21.
- **Footgun (don't re-litigate):**
  - **`weekInPhase` AND `rampWeeks` are both required to engage the within-phase-aware path.** Passing one without the other falls back to peak-of-phase. The optional-parameter design is deliberate — fixtures constructing isolated `GeneratedWeek` objects get the legacy behavior.
  - **`weekInPhaseInline` is a mirror of `week-builder.ts:weekInPhaseForTimeline`.** Any change to the canonical function MUST be mirrored here (circular-import constraint — `week-builder.ts` imports from `validate-training-floors.ts`). Docstring captures the duplication.
  - **`longDayFloorOpts.phaseBlocks` MUST be refreshed after every `tightenPhaseBlocksForFloorRebuild`.** `blocks` is reassigned by the rebuild call; the opts field becomes stale otherwise. Current code refreshes at `:260` (normal pass) + `:267` (deep pass). Adding new rebuild call sites must follow the same pattern.
  - **Cycling parity is pending** — `effectiveLongRideFloorHours` still uses peak-of-phase. When CYCLING-PROTOCOL Phase 1 ships `longRideHoursForWeek`, apply the same fix-shape there.
  - **Five-layer Plan #78 closure now fully end-to-end:** D-022 (Ticket-B cap) + D-024 (swim_fitness clamp) + D-025 (type substitution) on the swim axes; **D-026 (week-builder canonical) + D-027 (validator within-phase-aware)** on the run axis. All five required for the §4.5 LOCKED progression to actually deliver in production.
- **Decision:** D-027 (companion to D-026; completes the canonical-not-floor contract from week-builder through the validator).

### `longRunMilesForWeek` canonical-not-floor fix (Bundle B, 2026-05-21 — D-026)
- **Files/commits:** `0b983f07` — `generate-combined-plan/week-builder.ts:863-866` (Math.max floor replaced with direct canonical assignment; extended comment documenting the contract change + footgun); `generate-combined-plan/run-volume-ramp.test.ts` (+1 new canonical-contract pin test asserting EXACT 8.5mi at base wip=1 for the Plan #78 athlete shape). D-026 in `DECISIONS-LOG.md`.
- **Behavior:** the lerp output from `longRunMilesForWeek(distance, phase, weekInPhase, rampWeeks)` is now the canonical long-run mileage for base / build / race_specific phases. The TSS-derived `longRunMiles` from `:832` no longer participates in long-run selection for ramping phases. The realized progression per RUN-PROTOCOL §4.5 — 70.3 base **8.5 → 8.5 → 9 → 9 → 9.5 → 10mi**, build **10 → 10 → 10.5 → 11mi**, RS **11 → 11.5 → 12.5 → 13mi** — is now what the engine emits. Pre-fix, high-budget athletes (Plan #78 demographics: CTL 60, 11hr/wk, intermediate, performance/race_peak) saw flat **10mi every base week, 11mi every build week, 13mi every RS week** because the TSS-derived value naturally hit the lerp's PEAK and `Math.max` rendered the lower lerp values invisible. The §4.5 LOCKED progression was dormant in production.
- **Verification:** `run-volume-ramp.test.ts` → **3/0** (2 prior + 1 new canonical pin). The new test fails pre-fix regardless of fixture TSS — locks the canonical-vs-floor distinction. Full sweep `generate-combined-plan/` + `_shared/` → **608/0** (was 607; +1 new). Zero regressions on existing rebuild / taper / recovery / floor-validator tests. Eval-script run post-deploy confirms 70.3 endpoints match §4.5 exactly. Deployed `generate-combined-plan` 2026-05-21.
- **Footgun (don't re-litigate):**
  - **Never re-introduce a `Math.max` or floor-style clamp** between `longRunMilesForWeek` and `longRunMiles` at `week-builder.ts:~864`. The lerp IS canonical. Future overrides belong at the §4.5 endpoints themselves (spec amendment + D-NNN), not the assignment site.
  - **Two long-run functions, two semantics:** `longRunMilesForWeek` (PRESCRIBED mileage, `week-builder.ts`) ≠ `longRunFloorMiles` (WARNING THRESHOLDS, `validate-training-floors.ts`). They must move in lockstep at the **source** (D-023 two-peak-source rule) but do NOT interchange at the consumer. Conflating collapses both contracts.
  - **Rebuild / taper / recovery preserve their explicit `Math.min` clamps** at `:868-882`. `longRunMilesForWeek` internally delegates to `longRunFloorMiles` for those phases, then the external clamps cap further. Don't remove either layer.
  - **Brick-run was already lerp-canonical** via `brickRunMilesForWeek` at `:1012`. Don't apply this fix again there; it never had the floor pattern.
  - **High-budget athletes lose ~1-2mi early-phase long-run volume** on next regenerate. By design — protocol > budget for long-run prescription. The "saved" budget flows to other sessions via per-session sizing logic.
  - **The Phase 1 wiring's comment at `:860-862`** ("Lerps START → PEAK") was the spec intent from day one; D-026 makes the code match the comment.
- **Decision:** D-026 (supersedes the `Math.max` semantics introduced inadvertently in Phase 1 wiring `60c23de2`; ratifies the canonical-from-lerp contract the comment always documented).

### Q-006 closure — `swim_fitness` tier override (2026-05-21 fix — D-024)
- **Files/commits:** `8d1315af` — `_shared/infer-training-fitness.ts` (`deriveSwimFitness` pure helper: `learning` → `beginner`, `strong` → `advanced`, `steady` / unset → inherit; case-insensitive); `generate-combined-plan/types.ts` (`AthleteState.swim_fitness` optional field); `create-goal-and-materialize-plan/index.ts:1681-1687` (populates `swim_fitness` alongside `training_fitness`); `generate-combined-plan/week-builder.ts:1169` (`swimFitness = athleteState.swim_fitness ?? trainFitness`) swapped at **exactly five** swim-specific call sites: `:1216` `getSwimSlotTemplates(athleteFitness)`, `:1260` `resolveSwimSlotYardsWithBudget({fitness})`, `:1302` `applyOverdistanceIfApplicable({athleteFitness})`, `:1313` `swimFromTplOpts.athleteFitness`, `:1319` `enduranceOverdistanceNote` Full IM gate. 4 `deriveSwimFitness` pin tests in `_shared/infer-training-fitness.test.ts` + 3 Q-006 composition tests in `swim-protocol-volumes.test.ts`. D-024 in `DECISIONS-LOG.md`; Q-006 → RESOLVED in `OPEN-QUESTIONS.md`.
- **Behavior:** the Ticket-B per-session learner cap (D-022) now reaches the population D-022 named in its footgun ("high-CTL learner resolving to intermediate"). Plan #60 W6 / Plan #78 athletes (`swim_experience='learning'` + `training_fitness='intermediate'` via high CTL) derive `swim_fitness='beginner'` and hit the cap: 70.3 build threshold ceiling **2000yd** (was 3200 silently), aerobic ceiling **2500yd** (was 3200). Symmetric strong-side: a beginner-global athlete who declares `swim_experience='strong'` derives `swim_fitness='advanced'` and unlocks the full bmax + Full IM OD note eligibility. Steady / unset swimmers inherit `training_fitness` unchanged. Non-swim consumers (CTL fallback, weekly-hours bucket, loading pattern, run band selection, strength protocol, bike templates) read `training_fitness` unchanged — zero downstream behavior shift for non-swim paths.
- **Verification:** `_shared/infer-training-fitness.test.ts` → 373/0 (+4 new). `generate-combined-plan/swim-protocol-volumes.test.ts` → 245/0 (+3 new Q-006 composition: Plan #78 chain, strong-swimmer unlock, steady inherits + ceiling parity). Full sweep `generate-combined-plan/` + `_shared/` → **589/0**. Deployed BOTH `create-goal-and-materialize-plan` (populates field) AND `generate-combined-plan` (consumes field) 2026-05-21.
- **Footgun (don't re-litigate):**
  - **5-site threading is exact.** `shouldMaintainTwoSwimsInRecovery` (`week-builder.ts:1197`) is **NOT** in the swap list — it already takes `swim_experience` directly + has internal learner-awareness. Don't "complete" the threading by swapping it; that's redundant and dilutes the carefully-scoped surface.
  - **`?? trainFitness` fallback is load-bearing.** Legacy `athleteState` payloads or external test fixtures that don't populate `swim_fitness` keep working at the intermediate tier. Don't remove the `??`.
  - **The soft `-1` signal in `inferTrainingFitnessLevel` STAYS** (D-002). D-024 supplements it — the soft signal handles borderline athletes where the global tier SHOULD also nudge (low-CTL learner); the hard clamp handles explicit-signal cases where global tier should NOT change but swim should.
  - **Issue 1 NOT closed by D-024.** Plan #78's "learning swimmer gets `[threshold, race_specific_aerobic]` Week-1 rotation" is a SEPARATE swim arc slice (spec-first per `docs/SWIM-PROTOCOL.md §X`, then beginner rotation variant). D-024 closes the cap path (Issue 2) only. `raceTwoSwimRotationSlotMeta` is a pure function of `planWeek % 4`; making it fitness-aware is its own slice.
  - **Symmetric strong-side clamp lifts ceilings by design.** An explicitly-declared strong swimmer at global beginner tier now unlocks `advanced` bands + OD windows. This is intentional symmetry, not a regression.
- **Decision:** D-024 (supersedes D-002's "swim experience as soft signal only" stance — the soft signal stays, this is the additive explicit-signal hard clamp the original Q-006 entry named as the "proper closure").

### Run protocol arc Phases 0-3 (closed 2026-05-21 — D-023)
- **Files/commits:** P0 spec `50921629` (`docs/RUN-PROTOCOL.md` 415-line companion to SWIM-PROTOCOL.md). P1 fix `60c23de2` — `science.ts` additive exports `longRunMilesForWeek` / `brickRunMilesForWeek` / `racePacePeakMiles` / `rampWeeksForPhase` + `LONG_RUN_RAMP_ENDPOINTS` / `BRICK_RUN_RAMP_ENDPOINTS` tables; `session-factory.ts vo2Run` signature gains optional `weekInPhase` (N=3→6 reps); `week-builder.ts` 6 sites threaded via hoisted `runWeekInPhase = weekInPhaseForTimeline(...)`; `run-volume-ramp.test.ts` NEW (2 regression tests). P0 spec patch bundled in P1: §4.5 taper/recovery multipliers corrected to engine reality (taper 0.45, recovery 0.40; draft had 0.40/0.55). P2 strides ship (this session, 2026-05-21) — `session-factory.ts addStridesToEasyRun` helper (pure; +5min wall-clock; intensity stays EASY; TSS re-derived via `estimateSessionTSS`/`weightedTSS`; appends `strides_${reps}x${sec}s` token already resolved by `materialize-plan/index.ts:1222-1257`); `week-builder.ts shouldInjectStridesOnEasyRun` predicate computed once per week as `stridesOnEasyRun`, applied at the three easy-run emission sites (taper Wed `:~1407`, run-only Thursday `:~1546`, mid-week Friday `:~1595`); `run-strides-injection.test.ts` NEW (7 tests — 6 buildWeek end-to-end + 1 pure predicate). P3 ship (this session, 2026-05-21) — 70.3 long-run peak 11 → 13mi in BOTH `science.ts longRunPeakTarget` (ramp source) and `longRunFloorMiles` (validator/legacy source); half / half_marathon aliases moved together; default fallback also 13.0. Obsolete `validate-training-floors.ts:394` `≤25 min … transition stimulus` comment replaced (the brick run is 5.5mi / ~55min per §5.7; this function is bike-leg-only because it measures *long-ride* volume, not because the run is short); companion `:306` "11 mi for 70.3" → "13 mi". 15 `long-day-volume-floors.test.ts` assertions + comments updated, `rebuild-phase.test.ts:205` rebuild floor 9.5 → 11.0, `run-volume-ramp.test.ts` `later` selector bumped to wip ≥ 3 (new endpoint band rounds wip 1+2 both to 8.5mi).
- **Behavior:** within-phase ramps fire for base/build/race_specific via band-as-envelope lerp + `weekInPhaseForTimeline` (recovery-non-resetting in-phase index, NEVER `weekInBlock` ≡ 1 per ADR-0002). Realized 70.3 long-run progression: base 8.5 → 10mi (wk 1-6), build 10 → 11mi (wk 1-4), RS 11 → 13mi (wk 1-4). Strides inject on the easy run for **performance-intent** athletes in build / race_specific / taper (plus late base, wip ≥ 4 per §4.1's "optional" allowance); never in race week (§5.8 over §9.1 — see Q-017), recovery, or rebuild weeks. Comeback / completion / first_race intents do not get strides per §2 protocol table. `limiter_sport='run'` is deliberately NOT consulted — Phase 4 of the future arc adds an additional dose on top of this base. Base intervals 4 → 6 reps (was flat 4); build VO2max 3 → 6 × 3min (was hardcoded 5×3min); RS race-pace 3 → 6mi (was flat 4); brick run RS 4.5 → 5.5mi (was flat 5.5).
- **Verification:** `generate-combined-plan/` deno suite **213/0** (Phase 2: 206→213, 7 new strides tests; Phase 3: same 213 after updating the 15 long-day + 1 rebuild + 1 run-volume-ramp tests for the new floor values). `_shared/` **369/0**. Eval-script verified the 70.3 ramp endpoints (`base 10, build 11, RS 13`). Deployed `generate-combined-plan` 2026-05-21 (single deploy bundling Phases 2 + 3; `create-goal-and-materialize-plan` picks up via HTTP payload — no separate deploy).
- **Footguns (don't re-litigate):**
  - **ADR-0002 anti-regression:** within-phase index MUST be `weekInPhaseForTimeline(phaseBlocks, weekNum, block)`, NEVER `weekInBlock` (≡ 1; silently flattens the ramp). Same anti-regression rule the swim arc ratified at §4.1.
  - **Two peak-source rule:** `longRunPeakTarget` (ramp) and `longRunFloorMiles` (validator) must move in lockstep — both at 13.0 for 70.3 now. Phase 1 deliberately preserved the legacy values; Phase 3 lifted both together. The `brickRunTargetMiles` legacy function is similarly preserved unchanged for validator + rebuild-test backward compat.
  - **Strides gate:** `shouldInjectStridesOnEasyRun` is the SOLE owner of the gate; do not inline the conditions at sites. `training_intent === 'performance'` is the AND-gate; race-week / recovery / rebuild are HARD veto. `limiter_sport='run'` is intentionally not in the gate (Phase 4 scope).
  - **Strides token already wired:** the engine emits `strides_NxYs` — materialize-plan / Garmin / analyzer already handled this token pre-Phase-2; no downstream changes needed. Future strider-rate / pace-target enrichments must NOT bypass `addStridesToEasyRun`.
  - **Brick-run comment correction:** the function `maxLongRideMinutes` is bike-leg-only because it measures long-ride volume, NOT because the brick's run is short. Per §5.7 the 70.3 race-spec brick run is 5.5mi / ~55min — a meaningful run stimulus. Do not re-introduce a "≤ 25 min transition" framing.
  - **§5.8 vs §9.1 conflict:** race-week strides are gated OFF (Q-017). If a future session re-reads §9.1 ("optional race-day priming") as a green light for stride emission in race week, check Q-017 for the resolution + rationale before editing.
  - **§4.5 realized-progression table drift:** the spec table interior weeks differ from engine output by ±0.5mi at a few weeks due to half-mile rounding granularity (Q-018). Endpoints (8.5 → 10, 10 → 11, 11 → 13) match exactly. Don't tune the engine to match the table — update the table next time it's edited.
- **Pending:** Phase 4 (`limiter_sport='run'` intensity dial) **deferred to its own arc** — separate architectural-decision blocker per `TICKET-B-WIRING-AUDIT.md` Field 2 §7 (additive vs. replacing the current +7% TSS allocation in `science.ts:295-330 getBaseDistribution`).
- **Decisions:** D-023 (arc-wide); Phase 1 spec-correction sub-decision (taper 0.40→0.45 / recovery 0.55→0.40) recorded inline in `60c23de2` and rolled into D-023. Open questions: Q-017 (§5.8/§9.1), Q-018 (§4.5 table drift).

### `swim-protocol-volumes` Ticket B learner per-session cap (2026-05-20 fix — D-022)
- **Files/commits:** `95e712cc` — `generate-combined-plan/swim-protocol-volumes.ts` (`learnerSessionCap()` helper + restructured `getProtocolCeiling()` to apply the cap as `Math.min` AFTER the base ceiling switch). 10 Ticket B pin tests added to `swim-protocol-volumes.test.ts`. D-022 in `DECISIONS-LOG.md`.
- **Behavior:** `fitness === 'beginner' && distance ∈ {'70.3', 'full'}` athletes get per-session yardage capped at **2500yd aerobic** (`css_aerobic` / `race_specific_aerobic` / `technique_aerobic` / `endurance` / `kick_focused` / `pull_focused`) and **2000yd threshold** (`threshold` / `speed`). `easy` unchanged (existing `raceYd × 0.5` cap is already lower). Endurance OD window's 4600yd is gated for beginners — they never hit OD volume regardless of phase. Sprint/olympic/intermediate/advanced pass through unchanged. Phase-by-phase impact: 4-17% reduction for 70.3 beginner aerobic; 22-38% for full beginner aerobic.
- **Verification:** `deno test swim-protocol-volumes.test.ts` → 20/0 (10 prior + 10 new pin tests). `deno test generate-combined-plan/` → 204/0 (194 prior + 10 new). Deployed `generate-combined-plan`.
- **Footgun (don't re-litigate):** the cap is a `Math.min` OVERLAY — it cannot raise the ceiling, only lower it. Do NOT change `learnerSessionCap` to set the ceiling unconditionally; the band table is the canonical bmax source, the cap is a clamp on top. Endurance OD window is intentionally gated for beginners — do NOT re-elevate beginners into the OD path (protocol prescribes lower per-session yards for learners). **Plan #60 W6 documented athlete is NOT closed by this fix** — that athlete (high-CTL learner, `swim_experience='learning'`) resolves to `intermediate` per Q-006, not `beginner`. The Q-006 structural fix (separate `swim_fitness` tier override) is the proper closure for that population; the Ticket B cap closes athletes who DO resolve to beginner. Olympic distance excluded per documented scope (band 2600-2800, marginally over 2500) — extend if Olympic athlete complaints surface.

### `scaledWeeklyTSS` endurance-hours-based budget (2026-05-20 fix — D-021, Q-005 resolved)
- **Files/commits:** `2a9deab5` — `src/lib/session-frequency-defaults.ts` (`SessionFrequencyDefaults.endurance_hours` field exposed from existing `:284` local), `generate-combined-plan/week-builder.ts:677` (`scaledWeeklyTSS` consumes `endurance_hours ?? weekly_hours_available`), `generate-combined-plan/index.ts:481` (mirrored swap for `plan_contract_v1.weekly_tss_target`). 3 Q-005 pin tests added to `_shared/session-frequency-defaults.test.ts`. D-021 in `DECISIONS-LOG.md`.
- **Behavior:** TSS budget now scales on endurance hours (post-§2.1 strength-load deduction) instead of declared hours. Hybrid 11hr/7d × 2× perf-strength athlete: budgets on 9.5hr endurance → `hourFactor` 1.1 → 0.95 → ~15-20% TSS budget reduction at tier boundary (build week: ~700-800 → ~550-650). Plan #60 W6 predicted to land ~11h vs prior 11h55m. Endurance-only athletes: zero change (`endurance_hours === hours_per_week`). Production plans frozen; behavior is opt-in via next regenerate.
- **Verification:** `deno test _shared/session-frequency-defaults.test.ts` → 71/0 (68 prior + 3 new). `deno test generate-combined-plan/` → 194/0 (no collateral; no fixture asserts on concrete TSS values). `deno test _shared/` → 369/0. Deployed `generate-combined-plan` (`create-goal-and-materialize-plan` picks up via HTTP payload).
- **Footgun (don't re-litigate):** do NOT conflate `endurance_hours` with `hours_per_week`. Declared hours stay the user-facing wizard value + canonical workout-time budget; endurance hours are the TSS-budgeting axis ONLY. The §2.1 deduction at `session-frequency-defaults.ts:284` is the SINGLE SOURCE OF TRUTH — do not duplicate the formula. Any new TSS-scaled budget must use `endurance_hours` with the same `??` fallback for the reconciler short-circuit path (no-`long_run_day` case). See D-021, Q-005 (resolved).

### `inferTrainingFitnessLevel` swims90 null-arc penalty (2026-05-20 fix)
- **Files/commits:** fix `3b228dc8` (`_shared/infer-training-fitness.ts:157-165` — both `>= 14` and `<= 1` swims90 branches gated inside `if (swim != null)` with a `Number.isFinite` guard; null arc = "unknown", not "zero sessions"). Originally filed as Known Broken in `7751541d` (2026-05-19, surfaced during swim Phase 1 `c1c94cec` gate sweep — pre-existing, confirmed via stash-out).
- **Behavior:** athletes with `arc.swim_training_from_workouts === null` (wizard-only signups, mid-onboarding) no longer get a spurious -1 score penalty from the prior `?? 0` coalesce. The penalty correctly fires only when the arc row exists AND records ≤1 observed sessions. High-CTL athletes (≥58) with no swim arc now resolve to `'advanced'` instead of `'intermediate'`. Downstream: `create-goal-and-materialize-plan/index.ts:1506-1543` CTL seed (40→65 when `recentLoads.length === 0`), weekly-hours fallback (10→14 when wizard didn't supply), `athlete_state.training_fitness` payload → `week-builder.ts:1095` swim band selection (intermediate→advanced templates).
- **Verification:** `deno test supabase/functions/_shared/` → **366/0** (was 365/1 with the documented Known Broken red; +2 new pin-tests in `infer-training-fitness.test.ts` lock the null-vs-observed-zero distinction). Deployed `create-goal-and-materialize-plan` (single direct importer per `_shared/infer-training-fitness.ts` consumer audit).
- **Footgun (don't re-litigate):** do NOT re-collapse the null-vs-observed-zero distinction. The two new pin-tests ("null arc → no spurious -1 penalty" and "observed swim_training_from_workouts with zero sessions DOES penalize") are the regression lock — future refactors must preserve both. The low-CTL→beginner test fixture (`:62`) was tightened CTL 18→15 because the prior CTL was depending on the bogus penalty to compose `ctl_le_22` (-1) + `swims90` (-1) = -2; the new fixture hits `ctl_le_16` (-2) directly. Production athletes shift `intermediate → advanced` on next regenerate (organic, no in-place rewrite).

---

### Autonomous batch D-043 through D-045 (2026-05-25)
- CV outlier clipping: pace >1800 sec/mi excluded before CV computation
- Route naming: times_run only surfaced to LLM
- Arc forward-bias: unstructured_read hard-bans backward temporal anchor when
  active plan + next goal within 6 months
- Swim equipment duplicate: suppressed client-side
- Beginner swim rotation: swim_fitness='beginner' routes correctly
- bikeOpeners: scoped to race week only
- Drill repeat-pick memory: end-to-end via drill-token-harvest.ts helper;
  harvest reads week.sessions[] directly; cross-week repeat prevention live
- computeVarianceGate: extracted pure function, 14 pin tests, coverage gap closed
- Verify: regenerate any triathlon plan — drill families should rotate
  week-over-week with no consecutive cross-week repeats

---

### D-046 — Q-026 unplanned backward-anchor suppression (2026-05-25)
- **Files/commits:** `97111a01`. `_shared/arc-narrative-ai-appendix.ts:arcUnplannedBackwardAnchorAddon` — new exported helper; wired into running ai-summary at `_shared/fact-packet/ai-summary.ts:~1175` and cycling ai-summary at `_shared/cycling-v1/ai-summary.ts:~400`.
- **Behavior:** when `is_unplanned=true` AND narrative mode is NOT recovery_read / race_debrief (those modes require comeback framing per D-019), prompt now hard-bans backward race anchors with the same forbidden-pattern enumeration shape as `backwardAnchorHardBan`. Closes the "X days post-marathon" leak that escaped the linked-session forward-bias rules (D-039 / D-040).
- **Footgun:** the recovery_read / race_debrief override is mode-keyed, not flag-keyed — those addons REQUIRE the comeback frame regardless of is_unplanned. Don't extend the ban to those modes.
- **Decision:** D-046.

### D-047 — Q-024 hr_delta_bpm null root-cause + symmetric HR resolution (2026-05-25)
- **Files/commits:** `244c22c4`. Exported `getOverallAvgHr` from `_shared/fact-packet/queries.ts:161`; build.ts:~284 now uses the same three-stage fallback chain (overall.avg_hr → overall.avg_heart_rate → row.avg_heart_rate). 7 pin tests in `queries.test.ts`.
- **Behavior:** when current workout has HR populated under the alt key (`overall.avg_heart_rate`) or row-level (`workouts.avg_heart_rate`) but not under `overall.avg_hr`, `currentAvgHr` now resolves correctly. Closes the asymmetric-resolution bug where pool rows resolved through `getOverallAvgHr` but the current workout dropped to null, producing `hr_delta_bpm: null` despite `sample_size >= 3`. Sensor-data fallback stays at the tail as defense-in-depth.
- **Footgun:** any future avg-HR lookup MUST use `getOverallAvgHr` (not inline `coerceNumber(overall?.avg_hr)`) to preserve the symmetric resolution. The 7 pin tests lock the three-key fallback contract.
- **Decision:** D-047.

### D-048 — POLISH §1 base-skip + rebuild-skip trade-offs (2026-05-25)
- **Files/commits:** `ef0f6bcb`. `_shared/plan-generation-trade-offs.ts` adds two templates (`base_phase_skipped_short_plan`, `rebuild_skipped_tight_window`). `generate-combined-plan/phase-structure.ts:buildPhaseTimeline` return shape extended with `phaseStructureTradeOffs: PlanGenerationTradeOff[]`; threaded into `buildSingleEventBlocks` + the overlapping/tight non-priority-A branches. `generate-combined-plan/index.ts` merges into persistedTradeOffs (copied first to avoid mutating caller input). 6 pin tests in `phase-structure-tradeoffs.test.ts`.
- **Behavior:** 9-week 70.3 plan (or any plan where backward-from-race packing leaves baseStart >= buildStart) now surfaces a "base phase skipped" trade-off instead of silent skip. Two-A-priority sequential paths with tight inter-race windows surface a "rebuild skipped" trade-off instead of silent skip. Priority-A-tri-with-B branch unchanged (still hard-fails via the §8.5 windowWks < aTaperWks + 1 guard).
- **Footgun:** `persistedTradeOffs` is now a LOCAL COPY; mutating it does NOT propagate back to the request body. Any future fan-out that needs the caller's array should re-derive or accept the copied list.
- **Decision:** D-048. POLISH §1 flipped 95% → 100%.

### D-049 — CYCLING-PROTOCOL §4.3 race-spec brick closing block (2026-05-25)
- **Files/commits:** `228492ea`. `generate-combined-plan/session-factory.ts:brick` (~:1734) — race_specific bricks ≥ 60 min now emit a 2-block `steps_preset`: `bike_endurance_${baseMin}min_Z2` + `bike_race_pace_${closingMin}min_Z3`. Closing block ≈ 25% of total (clamped 20-45 min). 5 pin tests in `brick-race-spec.test.ts`.
- **Behavior:** athlete description references the race-IF target band (0.78-0.82 IF for 70.3 / 0.62-0.68 IF for full IM) so they know what "race power" means. Shorter race-specific bricks (< 60 min — early-season / olympic) keep the single-zone Z3 tag (too short to slice a meaningful closing block). Base / build bricks unchanged (single-block Z2 throughout).
- **Footgun:** the `useStructuredRS` gate is `isRS && bikeMin >= 60` — don't relax it without re-tuning. The 20-45 min closing-block clamp via `Math.min(45, Math.max(20, Math.round(bikeMin * 0.25)))` is the §4.3 spec window; closing block sizes outside that range violate the protocol.
- **Decision:** D-049. POLISH §3 Cycling P2 + P3 both closed.

### D-050 — Pace-at-HR percentile-classifier trend (Q-025 closed, 2026-05-25)
- **Files/commits:** `e95b3c94` (Piece 1: pace_at_hr on trend points — queries.ts + build.ts), `6dc3ab5a` (Piece 2: classifier helper `pace-at-hr-direction.ts` + fact-packet/build.ts wiring), `36cec623` (Piece 3: session_detail_v1 surface), `f041619b` (Piece 4: client SessionNarrative.tsx render), `1f11555e` (Piece 5: 18 pin tests + responsiveness fix). Spec: `docs/PACE-AT-HR-TREND-SPEC.md`.
- **Behavior:** running TREND row now uses pace-at-HR (sec/mi per 100bpm = `pace * 100 / avg_hr`) as the primary signal when ≥6 points have non-null pace_at_hr AND the server's percentile classifier returns a usable direction. Pool-uniform basis (GAP when useGapForTrend, raw otherwise). GAP-coverage filter at the classifier: when ≥60% of pace_at_hr-valid points are gap-basis, restrict to those for the slope computation. Direction labels are athlete-facing: "getting more efficient" / "holding steady" / "worth watching". Red color ONLY on declining (the Q-025 misleading-red-label class of bug is closed end-to-end).
- **Classifier (locked):** session signal = MEAN OF THE LAST K=3 PAIR-SLOPES, classified against p33 / p67 of the full pair-slope distribution within the window. Switched from whole-window LR slope (which was structurally biased toward 'stable' — LR ≈ mean(pair_slopes) → always in middle third) to the recent-K mean (responsive to current trend; K=3 smooths single-session noise). Stable-bias preserved for degenerate distributions (p33 === p67 — uniform deltas → no NEW direction signal). Verified behavior across 14 fixture shapes; 18 pin tests green.
- **Footgun:** the percentile classifier produces ~33/33/33 distribution on noisy data BY CONSTRUCTION — improving/declining fire when recent-K mean is unusually extreme vs internal volatility, not when there's a monotone trend. Long monotone trends correctly read as 'stable' because they're not a NEW direction relative to typical. Don't re-introduce a "monotone → improving" assumption — the classifier intentionally suppresses it. The K=3 window is locked; multi-user calibration may warrant tuning later, but single-athlete tuning would over-fit.
- **Decision:** D-050. Q-025 RESOLVED.

---

### D-061 — training_intent wired into combined-plan recovery + quality (2026-05-26)
- **Files/commits:** `a5762100`. New `loadingPatternForIntent()` helper in `phase-structure.ts`; `applyLoadingPattern()` + `blockWeekMultiplier()` extended with '1:1' pattern; LoadingPattern type widened; run-quality emission sites in `week-builder.ts` gain intent-aware base-rep cap + build-VO2 gating; 13 pin tests in `training-intent-differentiation.test.ts`.
- **Behavior:** closes the WIZARD-AUDIT G4 differentiation gap. `performance` → '3:1' (every 4th week recovery); `completion` → '2:1' (every 3rd); `first_race` / `comeback` → '1:1' (every 2nd). first_race/comeback also get 80% base-interval rep cap. In race_peak path (defensive): completion blocks VO2 entirely (downgrade to tempo); first_race blocks VO2 until weekInPhase ≥ 4.
- **Footgun:** intent-driven loading pattern OVERRIDES athlete's pinned `loading_pattern` field. A first_race athlete who pinned `loading_pattern: '3:1'` still gets '1:1' because intent semantic dominates. If a future athlete-pin override mechanism is needed, it must be explicit (per-goal opt-out flag) — don't pin-respect at the loadingPatternForIntent call site without a deliberate decision.
- **Decision:** D-061. Out of scope: swim + strength intent wiring (Ticket B follow-ups).

### D-062 — Cycling dashboard rows plain-language (Q-010 partial) (2026-05-26)
- **Files/commits:** `c2c32517`. Two row label/value translations in `_shared/session-detail/build.ts`: POWER ("IF 0.85" → "85% of threshold"); EFFICIENCY ("EF · % HR decoupling" → "Watts per heartbeat · HR drift").
- **Behavior:** dashboard rows now align with the jargon-banned INSIGHTS prose. Numeric values + gate logic unchanged.
- **Footgun:** the cycling LLM prompt closing-clause hedge half of Q-010 is NOT shipped — the SESSION-CONTEXT §7 3-guard-stack footgun warns the three jargon/lede/numeric-drift guards interact via a shared retry; modifying any single guard or trimming the attempt budget silently degrades the others. Without a concrete reproducer for the hedge pattern, the prompt was untouched.
- **Decision:** D-062. Q-010 stays partially open for the closing-clause hedge softening.

---

### Autonomous batch D-051 through D-060 (2026-05-25)
- D-051: Race-Specific Aerobic ramp 1500→2500yd (SWIM-PROTOCOL §5.4); was 2000→2400
- D-052: four new swim session types (Time Trial / Open Water Skills / Mixed-Fartlek / Race-Pace Sustained) with phase-aware rotation injection
- D-053: §0.5 athlete-vocab pass — stripped Z-codes / "threshold" / "CSS pace" from athlete-facing swim copy
- D-054: WIZARD-AUDIT.md written (no UI changes); 7 global findings + per-step audit
- D-055: training_intent copy softening + Q-027 days_since semantic clarification + Q-024 / Q-026 / POLISH §1 / Cycling §4.3 closures
- D-056: wizard polish batch (vocab pass + A/B/C tooltips + Hybrid rename + hours note + ordering note + building indicator + "Not sure" defaults + long-day same-day warning)
- D-057: Q-016 experience-tier drill ratio (Path A only, locked 30/20/10 vs §2's 75/30/10 to avoid double-counting)
- D-058: Q-020 ankle band added to gear chips + normalization + beginner pull_focused `optional:ankle_band` tag
- D-059: Q-022 segment_progress_metrics writer chain fixed (column names + error-surfacing)
- D-060: DB column rename run_easy_hr_trend → run_easy_pace_at_hr_trend (migration + 5-file consumer coordination)
- **Footgun:** D-060 deploy required migration apply BEFORE function deploy (atomic ALTER TABLE RENAME COLUMN; brief 42703 window for in-flight old code). For future cosmetic renames on low-write tables, this pattern is acceptable; for high-write tables, use add-column → backfill → drop two-step migration instead.
- **Decisions:** D-051 through D-060 in DECISIONS-LOG.

---

## Known broken (filed, not blocking)

Behaviors that are demonstrably wrong but intentionally deferred. Don't propose fixes unless you have new information — the deferral was a scoping call, and the list below documents the cost so the next implementer can pick up cleanly.

### `limiter_sport` intensity-side handling not implemented
- **Symptom:** spec at `docs/SESSION-FREQUENCY-DEFAULTS.md §4` says "Run limiter is handled through intensity, not frequency. Adding run sessions increases injury risk disproportionately. The engine addresses a run limiter by making existing run sessions more productive (longer long run, higher-quality intervals, strides on easy days) rather than adding a 4th session." Implementation today: frequency side is correctly a no-op for run limiter; intensity side has zero implementation. The +7% TSS allocation bump in `science.ts:268-278 getBaseDistribution()` is a percentage shift across all phases, not a per-session intensity boost.
- **Files (where wiring would land):** `supabase/functions/generate-combined-plan/science.ts` (extend `brickRunTargetMiles()` and `longRunFloorMiles()` to accept `limiterSport` — note: the cycling mirror is `longRideFloorHours`; do not confuse), `session-factory.ts` (interval modulation), `week-builder.ts` (stride logic).
- **Predicted effect:** ~+65-70 TSS/week for run-limiter athlete (long run +15-20%, quality run +1 tempo interval, strides on 1-2 easy runs).
- **Why deferred:** multi-file medium-risk change; needs an architectural decision on whether the +7% TSS allocation stays additive with the new intensity dial or gets replaced by it. Documented in `docs/TICKET-B-WIRING-AUDIT.md` Field 2.

### "Run — Tempo" vs "Run Intervals 4×1000m" label divergence
- **Symptom:** Same workout renders with two different titles across surfaces. Today's Efforts uses the workout's stored `name` directly. `AllPlansInterface.tsx:881-885` and `PlannedWorkoutSummary.tsx:34-66` both use regex against `description`/`tags` but with slightly different heuristics. Compounded by Monday-May-18 swim title case ("Swim — Drills" vs "Race-Specific Aerobic Swim") which suggests the surfaces also read different upstream data shapes.
- **Files:** `src/components/PlannedWorkoutSummary.tsx:34-66`, `src/components/AllPlansInterface.tsx:881-885`, `src/components/TodaysEffort.tsx` (uses `workout.name` directly).
- **Fix shape:** consolidate the title-derivation into a single shared utility — same architectural pattern as the `useStrengthOrderingPreference` consolidation. Solving at the data layer (one canonical session name per workout, derived once at materialize time) is cleaner than patching label-by-label downstream.
- **Why deferred:** predates the universal fixes; cosmetic, not protocol-violating; queued behind higher-signal work.

### Cycling TREND dashed HR line never draws (historical `avg_hr` null)
- **Symptom:** cycling TREND shows the power line + current-ride "· {bpm}" label but no dashed HR line.
- **File:** `analyze-cycling-workout/index.ts:~2108` reads `r.computed.overall.avg_hr` (frequently null); SELECT at `:2077` omits the reliable `workouts.avg_heart_rate` column. → all historical TREND points `avg_hr: null` → `SessionNarrative.tsx` `TrendSparkline` `hasHr (≥3)` gate fails.
- **Fix shape:** add `avg_heart_rate` to the SELECT; resolve `computed.overall.avg_hr ?? workout_analysis.fact_packet_v1.facts.avg_hr ?? r.avg_heart_rate`. Same projection/field-source footgun class as `cead4e9e`/`41d1582d`/`f9efb893`.
- **RESOLVED (2026-05-17, `4177c05c`):** added `avg_heart_rate` to the loop SELECT; `hrH` resolves `computed.overall.avg_hr ?? fact_packet_v1.facts.avg_hr ?? r.avg_heart_rate` (each candidate guarded). Wide backfill verified 26/26 rides-with-a-trend now have ≥3 HR points → dashed line draws. Kept here as the was-broken record (Known-broken doubles as the fix log, per the pwr20 precedent). Q-007 closed.

### Type-filtered `pwr20_trend_v1` won't populate from a single recompute
- **Symptom:** `pwr20_trend_v1` null on a reclassified ride despite `computed.power_curve['20min']` existing.
- **Cause:** the series filters historical rides by their **stored** `classified_type`; post-VI-gate, a single recompute re-derives only the current ride — historical rides keep stale stored types until re-analyzed. Not a code defect.
- **Fix shape:** historical re-analysis backfill across recent rides. See Q-008 / SESSION-CONTEXT open item #2.
- **RESOLVED (2026-05-17):** one-off script, run wide. `scripts/verify-cycling-vi-if-fix.mjs --all` (`fae293e7` + `--all` `83d07fdb`) replays the full recompute chain via service role. Wide run: 180 d / 30 rides, 0 failed, 26/26 cap-present consistent; 16 historical `null → type`; post-backfill every ride in-window has a stored type and recovery/threshold/climbing/endurance/tempo each ≥3 (pwr20-eligible). No longer broken; kept here as the data-caveat record (a fresh single recompute still only re-derives one ride — re-run `--all` after future classifier-input changes). See Q-008.

### #8 race-course segment matching — blocked on GPX dependency
- **Symptom:** no race-course-relevant tagging on segment history.
- **Cause:** needs course-segment geometry from race-course GPX (Data-Dependency ❌); not in the #6 unblock decisions. Forward hook `cycling_segment_history.race_course_relevant` is in place.
- **Why deferred:** documented blocker; product decision owed (GPS-track matcher vs Strava-only). See Q-009 / `docs/CYCLING-ANALYSIS-DESIGN.md`.

---

## Questioned (worth verifying)

Believed-working but never explicitly verified. Listed here so the next session can pick up the verification cheaply, not so anyone re-implements.

_All previously-listed entries verified 2026-05-20 and moved to Solid (Q-003 §6.1 scoping; Q-004 Full IM §3.7). Section currently empty — append here when a future session ends with an unverified claim._

---

## When to update this doc

Append to **Solid** when a fix ships and is verified.
Append to **Known broken** when a bug surfaces and is intentionally deferred.
Append to **Questioned** when a session ends with an unverified claim.
Move items between sections as their state changes — promotion (Questioned → Solid) requires a verification method documented inline.
