# Engine State

A current snapshot of what's load-bearing, what's known broken, and what's believed-working but unverified. Read this BEFORE proposing changes — most "obvious" bugs were either already fixed (don't re-litigate), already filed (don't re-discover), or intentionally left in place (don't "fix").

Last updated: 2026-05-21.

---

## Solid (don't re-litigate)

Verified-working architecture and fixes. If you think one of these is broken, the bug is probably elsewhere — read the verification method before changing anything.

### §6.1 cycling/running asymmetry — heavy Lower placement
- **Spec:** `docs/STRENGTH-PROTOCOL.md` §6.1 (4f106a78), §6.1.5 consolidation gate widening (b189e7ca), phase-aware adjacency messaging (7715ff5d).
- **Files:** `supabase/functions/_shared/schedule-session-constraints.ts`, `_shared/week-optimizer.ts`, `_shared/plan-generation-trade-offs.ts`.
- **Behavior:** Heavy Lower (Strength Build 78-85%, Maintenance + Power 70-75%, Rebuild 72-80%) is never 24h-adjacent to Quality Run or Long Run; 48h gap minimum from Long Run on both sides; permitted 24h-adjacent to bike sessions per Wilson 2012 (running ES≈0.94 vs cycling ES≈0.32). Hypertrophy / Deload sub-maximal Lower has relaxed adjacency.
- **Verification:** §6.1 conformance contract W-004..W-008 in STRENGTH-PROTOCOL.md §3.8. Test coverage in `supabase/functions/generate-combined-plan/same-day-pairing.test.ts`. Verified manually against multiple plan exports during the §6.1 pass.
- **Open question:** is the heavy-Lower classification protocol-gated to performance, or load-magnitude-gated? See Q-003.

### §7.3 post-race recovery week SKIP
- **Spec:** `docs/STRENGTH-PROTOCOL.md` §7.3.
- **File:** `supabase/functions/generate-combined-plan/week-builder.ts:~1569` — `if (phase === 'recovery') strFreq = 0`.
- **Behavior:** Both hybrid REDUCE-default and durability SKIP-default mid-block 3:1 deloads keep their parent phase (`base` / `build` / `race_specific`) and emit reduced strength sessions. The dedicated post-race recovery week (`phase === 'recovery'` from `phase-structure.ts:289-309 insertRecoveryBlock`) is the only path that triggers the unconditional SKIP. Protocol-agnostic — works for hybrid AND durability.
- **Verification:** Plan #59 W14 went from emitting Hypertrophy Deload Lower to SKIPPED post-fix (commit d42c1079). W4/W8/W12 mid-block deloads still emit reduced sessions as designed.

### Option B endurance-hours deduction
- **Spec:** `docs/SESSION-FREQUENCY-DEFAULTS.md` §2.1.
- **File:** `src/lib/session-frequency-defaults.ts:228-236` (`strengthCountFromIntent`) + line 282-285 (deduction applied before tier lookup).
- **Behavior:** Tier lookup uses endurance-adjusted hours (`declared - strength_count × 0.75hr`); §7 strength count keeps reading declared hours. Hybrid 11hr athlete re-tiers from `10-12` to `8-10` (Plan #59 regression fix, commit cf68cf43).
- **Verification:** 7 deno tests in `supabase/functions/_shared/session-frequency-defaults.test.ts` covering hybrid 11hr/7d, hybrid 11.5hr/7d boundary, hybrid 10hr/7d boundary, hybrid 9.99hr/7d (no §7 discontinuity), endurance-only 11hr/7d (no regression), support 11hr/7d, performance 14hr/7d. All passing.
- **Constant rationale:** see D-001.

### `useStrengthOrderingPreference` hook + `orderDayWorkoutsByTimingThenDiscipline` helper
- **Files:** `src/lib/use-strength-ordering-preference.ts` (hook + module-level cache + in-flight dedupe), `src/lib/pairing-timing.ts` (`orderDayWorkoutsByTimingThenDiscipline` — the single shared sort helper, relocated here from AllPlansInterface 2026-05-18, `2ba6c68b`; takes an optional `getWorkout` accessor for wrapper items), `src/components/AllPlansInterface.tsx` (weekly view ~:2372 + markdown export ~:1783 call sites), `src/components/TodaysEffort.tsx` (top cards ~:704), `src/components/WorkoutCalendar.tsx` (weekly calendar cells, ~:963, via `(e)=>e._src`).
- **Behavior:** One fetch path for `goals.training_prefs.strength_ordering_preference` (per planId). **FOUR** consumers — TodaysEffort, AllPlansInterface weekly view, AllPlansInterface markdown export, WorkoutCalendar cells. AllPlansInterface + WorkoutCalendar share `orderDayWorkoutsByTimingThenDiscipline` (timing then discipline-rank); TodaysEffort uses a timing-only stable sort (no discipline tiebreaker) — equivalent for the run+lower timed case, differs only for untimed stacks (e.g. swim+upper = Q-001 cosmetic). WorkoutCalendar resolves ONE week-dominant planId (single-plan weeks are the common case; first-found approximation, same as TodaysEffort per-day).
- **Verification:** consolidation e41e7781 + 3770ad41 (2026-05-13); WorkoutCalendar wired as the 4th consumer 2026-05-18 (`2ba6c68b`) — it was previously **unwired** (raw insertion order; Thursday Strength-above-Run / Monday Strength-above-Swim). `src/lib/pairing-timing.test.ts` (5 — incl. strength_first pref-honored + accessor guard); manual for the React render sites.
- **Footgun:** any NEW day-stacked render surface MUST call `orderDayWorkoutsByTimingThenDiscipline` from `@/lib/pairing-timing` (do not re-copy a sort; WorkoutCalendar was exactly that miss). The hook's module-level cache is **never invalidated** (page-load lifetime) — latent stale risk only if a regenerate-in-place keeps the same planId while the value flips mid-session (not currently exercised; relies on regeneration minting a new planId).
- **Decisions:** see D-003, D-004, D-005, D-006 (D-006/Q-001: swim+upper has no AM/PM ordering — discipline-rank "swim above upper" is the intended cosmetic; not pref-driven, do not "fix"). **Q-012:** easy-run + Lower on deload Thursdays renders Strength-before-Run *by design* (recovery-flush; `decideOrdering` ignores `strength_ordering_preference` for easy partners — mirrors server `week-builder.ts:1997-2012`). The 2026-05-18 read-only diagnosis confirmed `2ba6c68b` is correct, not regressing — do not "fix".

### `swim_experience='learning'` → soft -1 in `inferTrainingFitnessLevel`
- **File:** `supabase/functions/_shared/infer-training-fitness.ts:~178` (`if (swimExp === 'learning') score -= 1`).
- **Behavior:** Wizard-collected `training_prefs.swim_experience` is consulted by the training_fitness inference. Soft signal (-1), not hard clamp — bounded by score thresholds, won't over-clamp a strong cyclist/runner who's new to swim. Cascades into swim volume bands and per-session rep caps via existing consumers of `training_fitness`.
- **Verification:** 2 new deno tests in `supabase/functions/_shared/infer-training-fitness.test.ts` (commit 0fd17ad9). 6 of 7 tests pass; the 1 failure is pre-existing on main, unrelated — see Q-006 / D-002.
- **Decision:** see D-002.

### Cycling Performance tab at running parity
- **Files:** `_shared/session-detail/build.ts`, `_shared/cycling-v1/*`, `analyze-cycling-workout`, `workout-detail` (+ client `SessionNarrative.tsx`). Commits `25167b90`→`a739961f` (2026-05-14→16).
- **Behavior:** Cycling INSIGHTS (arc-aware LLM), mode-aware + classified_type-filtered TREND with dual pace+HR line, EFFICIENCY, CLIMBING/VAM, POWER ZONES, PACING, TERRAIN (+temp), vs-similar, and a dist/dur/temp stat line — at parity with running's Performance tab.
- **Verification:** recompute a ride → reload Performance tab; confirm rows render and `classified_type` reflects the VI gate. Pure helpers unit-tested (`classify-intent`, `ride-physiology`, `cycling-trend`, `segments`, `analysis-mode`); suite 628 pass / 1 pre-existing-unrelated fail.
- **Decision:** see D-011..D-014; design `docs/CYCLING-ANALYSIS-DESIGN.md`.

### Intent-aware cycling analysis (Build Order #1–#7, #9)
- **Files/data:** NP-TSS `computed.analysis.power.tss`; CTL/ATL/TSB `workout_analysis.fitness_v1` + `athlete_snapshot.{ctl,atl,tsb}` + `ArcContext.cycling_fitness`; HR-at-power/decoupling + VAM in `computed.analysis.{efficiency,climbing}`; segment ingestion `cycling_segment_history`.
- **Behavior:** per-ride TSS, PMC fitness/fatigue/form, aerobic efficiency + climbing-rate metrics, Strava+Garmin-climb segment history. Both migrations applied via SQL editor; all table access guarded.
- **Verification:** `docs/SESSION-CONTEXT.md` §6 checklist.
- **Decision:** D-011..D-014.

### Cycling fact-packet IF/VI sourced from canonical `computed.analysis.power.*`
- **Files:** `analyze-cycling-workout/index.ts:~1790` (NP resolver now prefers `computed.analysis.power.normalized_power`; new canonical VI/IF extraction passed as overrides), `_shared/cycling-v1/build.ts` (`buildCyclingFactPacketV1` optional `variabilityIndexOverride`/`intensityFactorOverride` — finite & positive overrides win for `facts`, the classifier gate, and `executed_intensity`, else per-metric recompute). Commits `6941a236` (fix) + `fae293e7` (verify/backfill script), 2026-05-17.
- **Behavior:** the packet previously recomputed IF (NP/FTP) and VI (NP/avg) from NP/avg resolved via `computed.overall.*` — which `compute-workout-summary` never writes at the overall level (power is written only per-interval/segment), so it fell through to provider/device power and disagreed with the analyzer. IF/VI now come from `computed.analysis.power.{intensity_factor,variability_index}` (the source `compute-facts:1124` trusts); the classifier's VI≥1.10 ∧ IF≥0.85 gate and downstream TSS/executed_intensity reason over the analyzer's full-series numbers.
- **Verification:** `scripts/verify-cycling-vi-if-fix.mjs` selects rides by the actual fact-packet-vs-canonical divergence and replays the recompute chain. Verified run 2026-05-17: 8 affected rides (120 d), all converged to exact match; `60304656` vo2→tempo and `4375a709` endurance_long→threshold reclassified on the corrected numbers. `_shared/cycling-v1/build.test.ts` (4 tests) + full cycling-v1 suite green (83 pass).
- **Decision:** see D-015.

### Cycling VI-gate elevation density from total `workouts.elevation_gain`
- **Files:** `_shared/cycling-v1/build.ts` (`elevationGainPerMi` now uses `elevationGainM`; climb_ascent_m fallback only), `analyze-cycling-workout/index.ts` (`elevation_gain` added to workout SELECT + passed through). Commit `bdf2cde2`, 2026-05-17.
- **Behavior:** the classifier's ≥40 ft/mi elevation-density gate sources ascent from total ride elevation gain, not grade≥3% climb-segment ascent (which under-reported on rolling terrain and straddled the gate wrong). Supersedes D-011's elevation-source tradeoff.
- **Verification:** May-10 ride `60304656` recomputed post-deploy → reclassified `tempo` → `climbing` (325 m total → 46.5 ft/mi; was 249 m climb-seg → 35.6 ft/mi); VI/IF stayed canonical (fpVI 1.13 = capVI). `build.test.ts` +3 (60304656 fix / climb-seg fallback / total-gain precedence); cycling-v1 suite green (86 pass).
- **Decision:** see D-016 (supersedes D-011 elevation-source only).

### Cycling Performance-tab display & narrative correctness (2026-05-17 — arc close-out)
- **Files/commits:** sport-aware TREND legend `91ea2078`; TREND ≥5-or-text gate `6bf574d4`; POWER-ZONES all-zones + duration-anchored total `80b4c285`/`8e83e5df`; PR attribution + Efforts-scoped language `a0ca4158`; narrative trend-series match + deterministic Arc-lede guard `36a7e792`/`dcaa9f08`/`da7dbce8`; INSIGHTS plain-language + deterministic jargon guard `98c04e2f`/`d6da072c`/`d02abfe4`. Touch `_shared/cycling-v1/ai-summary.ts` + `cross-workout-{types,queries}.ts`, `_shared/session-detail/build.ts`, `src/components/SessionNarrative.tsx`, `analyze-cycling-workout`.
- **Behavior:** TREND legend says "power" for rides; the chart needs ≥5 same-type rides (3–4 → text), cycling-only (running unchanged); POWER-ZONES shows every zone with the total accounting for full ride duration (un-binned coasting → "+Xm other"); the narrative cites the SAME trend series the TREND row shows, never leads with PR/recovery/taper framing, and is plain-language (no IF/VI/EF/HR-decoupling/ACWR/TSB labels-or-numbers; "so what" context; 3–4 sentences). Enforced by a 3-guard stack on the ai_summary — `validateNoNewNumbers` + `ledeOpensWithArcFrame` + `summaryHasJargon` — sharing ONE combined corrective retry (plus `set_on_current_ride` gating "set this ride"; Efforts-scoped language).
- **Verification:** `scripts/verify-cycling-vi-if-fix.mjs --all` (180 d / 30 rides) re-run after each change — final 30/30, 0 failed, **0/30 Arc-lede**, **jargon 0/30**, ≤4 sentences 30/30, IF/VI 26/26 consistent, banned-language 0/30, the 1 PR claim correctly attributed (`60304656`/`54e8fd86`). cycling-v1 suite 96 pass; session-detail 24 pass.
- **Footguns (don't re-litigate):** `docs/SESSION-CONTEXT.md` §7 — narrative trend MUST mirror `pickCyclingTrendSeries` (don't revert to always-np_trend); the lede + jargon guards are deterministic by design (don't delete them / don't "fix" the lede in the shared `arc-narrative-ai-appendix.ts` — would change running); the 3-guard stack shares ONE retry budget (don't split per-guard or cut the attempt count — degrades the others); `achievements_v1` PRs are prior-ride; `computed.overall.*` has no overall power; `climb_ascent_m` ≠ total gain.
- **Decision:** D-015, D-016 (no new D-NNN — display/narrative fixes were single-sane-implementation).

### Plan-gen strength provenance split (Bugs 1&2 / #131)
- **Files/commits:** `reconcile-athlete-state-week-optimizer.ts:276` (stop engine→`strength_preferred_days`), `create-goal-and-materialize-plan/index.ts:~904` (strip engine `preferred_days.strength`, persist `trainingPrefs.strength_optimizer_slots`), `src/lib/format-wizard-prefs-export.ts` ("Strength (scheduled by app):" path). Commit `71611501`, 2026-05-17. D-017.
- **Behavior:** `strength_preferred_days` / `preferred_days.strength` carry ONLY a genuine wizard pin; engine-chosen strength placement lives ONLY in `strength_optimizer_slots` and exports as "scheduled by app", never as an athlete preference. The optimizer is no longer fed phantom strength prefs → no bogus "preferred day rejected" strength trade-offs (root-fixed at source, not composer-suppressed).
- **Verification:** export test +2 (engine→scheduled-by-app; genuine pin still surfaces) flipped from the leak-encoding test; suites green — format-wizard-prefs-export 6, plan-generation-trade-offs 30, week-optimizer.anchor-contract 25, generate-combined-plan 158, all 0 failed; client build clean. End-to-end (live multi-sport regen) **observed 2026-05-18** — the co-equal-2× regen surfaced no bogus strength "preferred rejected" trade-offs and no engine-strength-as-pin leak; D-017 holds end-to-end.
- **Footgun (don't re-litigate):** never re-introduce an engine→`strength_preferred_days` or engine→`preferred_days.strength` write (reconcile:276 *was* exactly that bug). Engine strength = `strength_optimizer_slots`. `freshCombinedPrefs` is re-derived from `goal.training_prefs` (NOT the gcp response) — the `:904` strip is what keeps it wizard-only; don't "thread the gcp response" thinking it matters.
- **Decision:** D-017.

### Bug 1 Piece B — trade-off consolidation duplicate (Slices 1 & 2)
- **Files/commits:** Slice 1 `60338100` (jargon strip `plan-generation-trade-offs.ts:~195`; delete optimizer consolidation push `week-optimizer.ts:1237`). Slice 2 `1fff344b` 2026-05-18 (delete the two surviving sibling pushes — `week-optimizer.ts:1604-1606` live co-equal-2× branch + `:1756-1758` dead twin; `stacking`, the `if (stacking)` guard, PM-timing `place(...)`, and the pin-divergence push all preserved). D-018.
- **Behavior:** the same-day quality-run + lower-body-strength consolidation trade-off has ONE owner — the builder's realized-grid `collectQualityRunLowerBodyTradeOffs` (`generate-combined-plan/week-builder.ts:~2109`). The optimizer no longer emits its canonical-pattern copy (which fired before the builder could split the day via `enforceHardEasy(grid, allowConsolidatedHardException=false)`, so it could name a day that never realizes). Athlete-visible "Schedule adjustments" shows the line once, day-accurate.
- **Verification:** deno 55/0 (`plan-generation-trade-offs` + `week-optimizer.anchor-contract`); the `04i` reproducer shows the pin-divergence line kept, the consolidation push gone, `thursday=quality_run+lower_body_strength` placement intact; the `§6.5` inline-fixture test green unchanged (no assertion flip needed). Live multi-sport co-equal-2× regen **observed 2026-05-18** — Line 1 absent, builder Line 3 present.
- **Footgun (don't re-litigate):** never re-add a QR+lower consolidation `trade_offs.push` in `week-optimizer.ts` "for parity" — the builder collector is the sole owner (the `:1237`/`:1604`/`:1756` comments say so). Any future optimizer↔builder trade-off de-dup REQUIRES the builder-coverage gate (prove B ⊇ optimizer-emit cases) first. `arc-setup-chat`'s LLM context intentionally loses the line for co-equal-2× weeks (no realized grid there) — accepted, not a regression. The inner dead `if (stacking)` block in the non-co-equal branch (~`:1755`) remains by design — see Q-013.
- **Decision:** D-018.

### Swim protocol arc (Phases 0–3 / SWIM-PROTOCOL §4.1 ramp + §6.2 pools + §6.3 selection)
- **Files/commits:** P0 `95c9b13f` (`SWIM-PROTOCOL.md §4.1` ratification — dormant `phaseProgress`/`lerp` curve documented). P1 `c1c94cec` (`generate-combined-plan/swim-protocol-volumes.ts:resolveSwimSlotYardsWithBudget` clamp → band-as-envelope lerp; `week-builder.ts` wired `swimWeekInPhase = weekInPhaseForTimeline(...)` per ADR-0002; `_shared/swim-program-templates.ts` exports `lerp`/`phaseProgress`/`roundYards`/ramp constants; `swim-volume-ramp.test.ts` regression lock). P2 `ef91c2ee` (`src/lib/plan-tokens/swim-drill-tokens.ts:SWIM_DRILL_POOLS` — `singlearm`/`616` added to base, `scull`/`scullfront`/`zipper` to build, `singlearm` to peak, `fingertipdrag` to taper). S3a `e723d246` (§6.3 ↔ §5 hierarchy ratified + in-code citations + Q-015/Q-016 deferrals). S3b `95b94aba` (`SWIM_DRILL_STROKE_PHASE` map per §6.1 column 2 + Path A distinct-phase pairing). S3d `f53bbf34` (`FOUNDATION_DRILL_SUFFIXES` + `RACE_SPECIFIC_DRILL_SUFFIXES` + `tierBias`; threaded `athleteFitness` through `speedSwim`/`thresholdSwim`/`easySwim`/`cssAerobicSwim`/`enduranceSwim` + dispatcher). Plus Q-014 `ff9600e9` (server-side `swim_equipment_suggested` redundant channel documented as intentional).
- **Behavior:** swim yards now ramp START→PEAK across base/build/race-specific (band IS the envelope; lerp by `phaseProgress(weekInPhase, rampWeeks)`); taper/recovery skip ramp. Drill picker honors §5 per-type counts (Path A = 2-3 for §5.1 Technique Aerobic; Path B = 1 for §5.2/§5.3/§5.4/§5.7/§5.10); §6.3 "rotates" means temporal variation across sessions, not multiple drills within one session. Path A enforces distinct §6.1 stroke phases across the 2-3 chosen drills (permissive fallback when pool diversity is the constraint). Beginner athletes bias toward §6.2 base primaries (catchup/fingertipdrag/singlearm/616); advanced athletes bias toward sighting in peak phase. Drill tokens for §6.2 missing primaries (singlearm/616/scull/scullfront/zipper) wired into the right pools; downstream per-step equipment surfacing (Garmin `SWIM_BUOY`, Form "Pull buoy") works via `inferEquipFromDrillName`'s `/scull/` branch.
- **Verification:** full `swim-drill-tokens.test.ts` + `generate-combined-plan/` deno suite **238/0** (covers `swim-volume-ramp.test.ts` § 4.1 regression lock, `swimDrillStrokePhase` §6.1 mapping, every-DRILL_EQUIPMENT_MAP-suffix-categorized coverage gate, Path A distinct-phase regression, beginner bias, advanced bias, intermediate no-regression lock). `generate-triathlon-plan/` + `materialize-plan/` **12/0** (no collateral on the legacy / token-resolver paths). Pre-existing `_shared/infer-training-fitness.test.ts:52` failure surfaced and filed (Known broken — unrelated; provably pre-existing via stash-out).
- **Footgun (don't re-litigate):** swim within-phase ramp MUST be driven by `weekInPhaseForTimeline(phaseBlocks, weekNum, block)`, NEVER `weekNum - block.startWeek + 1` (which is always 1 per ADR-0002 — silently flattens). Band-as-envelope (Option A) is locked over rewrite-floor/ceil (Option B); floor/ceiling functions are deliberately untouched. §5 per-type drill counts are AUTHORITATIVE; §6.3's "2-3 drills" is the GLOBAL DEFAULT — do NOT "fix" Path B back to 2-3 drills per session (Slice 3a in-code citations prevent this re-litigation). scull/scullfront are pull-buoy-REQUIRED in DRILL_EQUIPMENT_MAP (coaching-conservative; diverges from §6.1 column 4 "None" by design). Tri-generator's 6 `pickSwimDrillInset` call sites are intentionally NOT threaded for `athleteFitness` (legacy path; optional param preserves intermediate-equivalent behavior). Server-side `computed.swim_equipment_suggested` is incomplete on non-pull_focused sessions with pull-buoy drills — REDUNDANT channel, do not "fix" (Q-014). 3c (recent-pick memory) and 3e (§2 ratio scaling) deferred — Q-015 / Q-016.
- **Decision:** D-020.

### Race-week protocol (Phases 1–4 / RACE-WEEK-PROTOCOL §8)
- **Files/commits:** P1 `4a63f44e` (`RaceAnchor.priority`, `PhaseBlock`/`GeneratedWeek.race_week`, priority-driven A/B + chronology guard — `phase-structure.ts`). P2 `9c393119` (`science.ts:raceDaySessionSpec` distance-aware race day; §8.4 hard-fail `validator.ts:findMissingRaceDaySessions` + `index.ts`; Issue-2 `classify-error.ts`). P3 `7221b8d5` (`phase-structure.ts` backward allocation — A-taper full `taperWeeks` never compressed, rebuild floored ≥1, Decision-A `[race-week §8.2/§8.5]` throw). P4 `f7580ec5` (Gap 6 activation-swim `opts.isRaceWeek`) / `3076ba72` (Gap 9 b/c/d soft `validator` guards) / `0b54318d` (Gap 8 T6 realized B=13/A=17 E2E) / `95bd017e` (Gap 7 doc-drift). D-019.
- **Behavior:** A-race = priority-driven (NOT calendar-order), gets the full distance-driven taper (never compressed), preceded by ≥1 rebuild after post-B recovery; B-race = raced-through (no multi-week protected taper; 70.3-B taper already 1wk). Race day = one distance-aware `type:'race'` session, always materialized (hard-fail if missing); activation-swim substitution scoped to the actual race week only. Infeasible B→A spacing hard-fails with an actionable `[race-week §8.2/§8.5]` message (Issue-2 → `race_week_infeasible`/422, wrapper-preserved).
- **Verification:** full `generate-combined-plan` deno suite **189/0** (`race-week-phase{1,2,3,4}.test.ts`, `rebuild-phase`, `classify-error`; Flag-C empty). T6 deterministically locks the realized two-70.3 (B=13/A=17, full 2wk A-taper) end-to-end. Live multi-sport regen user-verified 2026-05-18.
- **Footgun (don't re-litigate):** A/B is priority-driven — do NOT revert to calendar-order; the chronology guard intentionally HARD-FAILS priority/chronology disagreement. A-taper is inviolable — rebuild/recovery/base absorb tight windows, never the A-taper (Decision A = hard-fail, not silent compression). Gap-9 b/c/d are SOFT (advisory `console.warn`); only Gap-9a (race-day-present) is hard-fail. `bikeOpeners` over-broad `phase==='taper'` gate (`week-builder.ts:1298`) is the SAME class as the fixed Gap-6 swim sub but deliberately OUT of §8.6 (POLISH backlog). Synthetic test fixture is B=14/A=18; the *realized* contract is B=13/A=17 — don't conflate.
- **Decision:** D-019.

### §6.1 heavy-Lower classifier is blanket / load-agnostic (verified 2026-05-20 — Q-003)
- **Verified:** `_shared/week-optimizer.ts:464-467` defines `isHigh(k)` as `k === 'long_ride' || k === 'long_run' || k === 'quality_bike' || k === 'quality_run' || k === 'lower_body_strength'`. The classifier is **unconditional** on `lower_body_strength` — it reads neither `strength_protocol` nor `repProfile` nor load magnitude. Every Lower placement gets the full §4.7 24h-pre / 48h-post adjacency protection.
- **Behavior:** durability MS phase Lower gets identical adjacency rules to Strength Build / M+P / Rebuild phase Lower. The implementation is **more conservative** than `STRENGTH-PROTOCOL.md §6.1`'s load-magnitude framing technically allows — the spec lets sub-maximal Hypertrophy / Deload Lower get relaxed adjacency, but the code applies blanket protection. Safe-conservative posture; zero risk of under-protecting heavy Lower.
- **Footgun (don't re-litigate):** the Q-003 premise that "may scope to protocol names" was incorrect — the classifier is intent-blind and load-blind by design. If a future need arises (real Deload-week placement complaints), the spec's load-magnitude relaxation is the natural extension. No observed defect today. See Q-003 (resolved).

### Full IM §3.7 race-spec strength scaling (verified 2026-05-20 — Q-004)
- **Verified:** `generate-combined-plan/week-builder.ts:108-128` `strFreqForPhase` has explicit Full IM branching at line 116 (`isFullIm = d === 'ironman' || d === 'full' || d.includes('iron')`). Build phase + Full IM + `weeklyHours >= 18` → 1× strength (line 120); race-specific phase + Full IM → 1× strength (line 124). `slotsPlanned = slotsOrdered.slice(0, 1)` at `:1632` takes the first slot (Upper by session_index convention). Full IM race-spec realizes "1× upper-only" per §3.7.
- **Behavior:** Full IM athletes in race-spec get 1× strength (down from 70.3's 2×); Build at 18+hr/wk also drops to 1×. The frequency enforcement is the load-bearing piece (over-prescribing volume was the original concern). Shipped in the v2.1 close-out (`cf5867fa`, 2026-05-12).
- **Partial verification residual:** §3.7 also prescribes the single race-spec session at maintenance load (halved power volume, no depth jumps). Maintenance `repProfile` is present in `triathlon_performance.ts:501/549/1408` — framework Full IM race-spec would consume. Exhaustive trace of `triathlonStrength()` → race_specific × Full IM → maintenance repProfile selection was outside the 30-min Q-004 budget; flagged as asserted-good-by-framework. No observed defect.
- **Footgun (don't re-litigate):** the Full IM check uses substring match (`d.includes('iron')`) so legacy `"ironman"` plus modern `"full"` both resolve correctly. Don't change the substring without auditing fixture goal-distance strings. See Q-004 (resolved).

### Q-006 closure — `swim_fitness` tier override (2026-05-21 fix — D-024)
- **Files/commits:** `8d1315af` — `_shared/infer-training-fitness.ts` (`deriveSwimFitness` pure helper: `learning` → `beginner`, `strong` → `advanced`, `steady` / unset → inherit; case-insensitive); `generate-combined-plan/types.ts` (`AthleteState.swim_fitness` optional field); `create-goal-and-materialize-plan/index.ts:1681-1687` (populates `swim_fitness` alongside `training_fitness`); `generate-combined-plan/week-builder.ts:1169` (`swimFitness = athleteState.swim_fitness ?? trainFitness`) swapped at **exactly five** swim-specific call sites: `:1216` `getSwimSlotTemplates(athleteFitness)`, `:1260` `resolveSwimSlotYardsWithBudget({fitness})`, `:1302` `applyOverdistanceIfApplicable({athleteFitness})`, `:1313` `swimFromTplOpts.athleteFitness`, `:1319` `enduranceOverdistanceNote` Full IM gate. 4 `deriveSwimFitness` pin tests in `_shared/infer-training-fitness.test.ts` + 3 Q-006 composition tests in `swim-protocol-volumes.test.ts`. D-024 in `DECISIONS-LOG.md`; Q-006 → RESOLVED in `OPEN-QUESTIONS.md`.
- **Behavior:** the Ticket-B per-session learner cap (D-022) now reaches the population D-022 named in its footgun ("high-CTL learner resolving to intermediate"). Plan #60 W6 / Plan #78 athletes (`swim_experience='learning'` + `training_fitness='intermediate'` via high CTL) derive `swim_fitness='beginner'` and hit the cap: 70.3 build threshold ceiling **2000yd** (was 3200 silently), aerobic ceiling **2500yd** (was 3200). Symmetric strong-side: a beginner-global athlete who declares `swim_experience='strong'` derives `swim_fitness='advanced'` and unlocks the full bmax + Full IM OD note eligibility. Steady / unset swimmers inherit `training_fitness` unchanged. Non-swim consumers (CTL fallback, weekly-hours bucket, loading pattern, run band selection, strength protocol, bike templates) read `training_fitness` unchanged — zero downstream behavior shift for non-swim paths.
- **Verification:** `_shared/infer-training-fitness.test.ts` → 373/0 (+4 new). `generate-combined-plan/swim-protocol-volumes.test.ts` → 245/0 (+3 new Q-006 composition: Plan #78 chain, strong-swimmer unlock, steady inherits + ceiling parity). Full sweep `generate-combined-plan/` + `_shared/` → **589/0**. Deployed BOTH `create-goal-and-materialize-plan` (populates field) AND `generate-combined-plan` (consumes field) 2026-05-21.
- **Footgun (don't re-litigate):**
  - **5-site threading is exact.** `shouldMaintainTwoSwimsInRecovery` (`week-builder.ts:1197`) is **NOT** in the swap list — it already takes `swim_experience` directly + has internal learner-awareness. Don't "complete" the threading by swapping it; that's redundant and dilutes the carefully-scoped surface.
  - **`?? trainFitness` fallback is load-bearing.** Legacy `athleteState` payloads or external test fixtures that don't populate `swim_fitness` keep working at the intermediate tier. Don't remove the `??`.
  - **The soft `-1` signal in `inferTrainingFitnessLevel` STAYS** (D-002). D-024 supplements it — the soft signal handles borderline athletes where the global tier SHOULD also nudge (low-CTL learner); the hard clamp handles explicit-signal cases where global tier should NOT change but swim should.
  - **Issue 1 NOT closed by D-024.** Plan #78's "learning swimmer gets `[threshold, race_specific_aerobic]` Week-1 rotation" is a SEPARATE swim arc slice (spec-first per `docs/SWIM-PROTOCOL.md §X`, then beginner rotation variant). D-024 closes the cap path (Issue 2) only. `raceTwoSwimRotationSlotMeta` is a pure function of `planWeek % 4`; making it fitness-aware is its own slice.
  - **Symmetric strong-side clamp lifts ceilings by design.** An explicitly-declared strong swimmer at global beginner tier now unlocks `advanced` bands + OD windows. This is intentional symmetry, not a regression.
- **Decision:** D-024 (supersedes D-002's "swim experience as soft signal only" stance — the soft signal stays, this is the additive explicit-signal hard clamp the original Q-006 entry named as the "proper closure").

### Run protocol arc Phases 0-3 (closed 2026-05-21 — D-023)
- **Files/commits:** P0 spec `50921629` (`docs/RUN-PROTOCOL.md` 415-line companion to SWIM-PROTOCOL.md). P1 fix `60c23de2` — `science.ts` additive exports `longRunMilesForWeek` / `brickRunMilesForWeek` / `racePacePeakMiles` / `rampWeeksForPhase` + `LONG_RUN_RAMP_ENDPOINTS` / `BRICK_RUN_RAMP_ENDPOINTS` tables; `session-factory.ts vo2Run` signature gains optional `weekInPhase` (N=3→6 reps); `week-builder.ts` 6 sites threaded via hoisted `runWeekInPhase = weekInPhaseForTimeline(...)`; `run-volume-ramp.test.ts` NEW (2 regression tests). P0 spec patch bundled in P1: §4.5 taper/recovery multipliers corrected to engine reality (taper 0.45, recovery 0.40; draft had 0.40/0.55). P2 strides ship (this session, 2026-05-21) — `session-factory.ts addStridesToEasyRun` helper (pure; +5min wall-clock; intensity stays EASY; TSS re-derived via `estimateSessionTSS`/`weightedTSS`; appends `strides_${reps}x${sec}s` token already resolved by `materialize-plan/index.ts:1222-1257`); `week-builder.ts shouldInjectStridesOnEasyRun` predicate computed once per week as `stridesOnEasyRun`, applied at the three easy-run emission sites (taper Wed `:~1407`, run-only Thursday `:~1546`, mid-week Friday `:~1595`); `run-strides-injection.test.ts` NEW (7 tests — 6 buildWeek end-to-end + 1 pure predicate). P3 ship (this session, 2026-05-21) — 70.3 long-run peak 11 → 13mi in BOTH `science.ts longRunPeakTarget` (ramp source) and `longRunFloorMiles` (validator/legacy source); half / half_marathon aliases moved together; default fallback also 13.0. Obsolete `validate-training-floors.ts:394` `≤25 min … transition stimulus` comment replaced (the brick run is 5.5mi / ~55min per §5.7; this function is bike-leg-only because it measures *long-ride* volume, not because the run is short); companion `:306` "11 mi for 70.3" → "13 mi". 15 `long-day-volume-floors.test.ts` assertions + comments updated, `rebuild-phase.test.ts:205` rebuild floor 9.5 → 11.0, `run-volume-ramp.test.ts` `later` selector bumped to wip ≥ 3 (new endpoint band rounds wip 1+2 both to 8.5mi).
- **Behavior:** within-phase ramps fire for base/build/race_specific via band-as-envelope lerp + `weekInPhaseForTimeline` (recovery-non-resetting in-phase index, NEVER `weekInBlock` ≡ 1 per ADR-0002). Realized 70.3 long-run progression: base 8.5 → 10mi (wk 1-6), build 10 → 11mi (wk 1-4), RS 11 → 13mi (wk 1-4). Strides inject on the easy run for **performance-intent** athletes in build / race_specific / taper (plus late base, wip ≥ 4 per §4.1's "optional" allowance); never in race week (§5.8 over §9.1 — see Q-017), recovery, or rebuild weeks. Comeback / completion / first_race intents do not get strides per §2 protocol table. `limiter_sport='run'` is deliberately NOT consulted — Phase 4 of the future arc adds an additional dose on top of this base. Base intervals 4 → 6 reps (was flat 4); build VO2max 3 → 6 × 3min (was hardcoded 5×3min); RS race-pace 3 → 6mi (was flat 4); brick run RS 4.5 → 5.5mi (was flat 5.5).
- **Verification:** `generate-combined-plan/` deno suite **213/0** (Phase 2: 206→213, 7 new strides tests; Phase 3: same 213 after updating the 15 long-day + 1 rebuild + 1 run-volume-ramp tests for the new floor values). `_shared/` **369/0**. Eval-script verified the 70.3 ramp endpoints (`base 10, build 11, RS 13`). Deployed `generate-combined-plan` 2026-05-21 (single deploy bundling Phases 2 + 3; `create-goal-and-materialize-plan` picks up via HTTP payload — no separate deploy).
- **Footguns (don't re-litigate):**
  - **ADR-0002 anti-regression:** within-phase index MUST be `weekInPhaseForTimeline(phaseBlocks, weekNum, block)`, NEVER `weekInBlock` (≡ 1; silently flattens the ramp). Same anti-regression rule the swim arc ratified at §4.1.
  - **Two peak-source rule:** `longRunPeakTarget` (ramp) and `longRunFloorMiles` (validator) must move in lockstep — both at 13.0 for 70.3 now. Phase 1 deliberately preserved the legacy values; Phase 3 lifted both together. The `brickRunTargetMiles` legacy function is similarly preserved unchanged for validator + rebuild-test backward compat.
  - **Strides gate:** `shouldInjectStridesOnEasyRun` is the SOLE owner of the gate; do not inline the conditions at sites. `training_intent === 'performance'` is the AND-gate; race-week / recovery / rebuild are HARD veto. `limiter_sport='run'` is intentionally not in the gate (Phase 4 scope).
  - **Strides token already wired:** the engine emits `strides_NxYs` — materialize-plan / Garmin / analyzer already handled this token pre-Phase-2; no downstream changes needed. Future strider-rate / pace-target enrichments must NOT bypass `addStridesToEasyRun`.
  - **Brick-run comment correction:** the function `maxLongRideMinutes` is bike-leg-only because it measures long-ride volume, NOT because the brick's run is short. Per §5.7 the 70.3 race-spec brick run is 5.5mi / ~55min — a meaningful run stimulus. Do not re-introduce a "≤ 25 min transition" framing.
  - **§5.8 vs §9.1 conflict:** race-week strides are gated OFF (Q-017). If a future session re-reads §9.1 ("optional race-day priming") as a green light for stride emission in race week, check Q-017 for the resolution + rationale before editing.
  - **§4.5 realized-progression table drift:** the spec table interior weeks differ from engine output by ±0.5mi at a few weeks due to half-mile rounding granularity (Q-018). Endpoints (8.5 → 10, 10 → 11, 11 → 13) match exactly. Don't tune the engine to match the table — update the table next time it's edited.
- **Pending:** Phase 4 (`limiter_sport='run'` intensity dial) **deferred to its own arc** — separate architectural-decision blocker per `TICKET-B-WIRING-AUDIT.md` Field 2 §7 (additive vs. replacing the current +7% TSS allocation in `science.ts:295-330 getBaseDistribution`).
- **Decisions:** D-023 (arc-wide); Phase 1 spec-correction sub-decision (taper 0.40→0.45 / recovery 0.55→0.40) recorded inline in `60c23de2` and rolled into D-023. Open questions: Q-017 (§5.8/§9.1), Q-018 (§4.5 table drift).

### `swim-protocol-volumes` Ticket B learner per-session cap (2026-05-20 fix — D-022)
- **Files/commits:** `95e712cc` — `generate-combined-plan/swim-protocol-volumes.ts` (`learnerSessionCap()` helper + restructured `getProtocolCeiling()` to apply the cap as `Math.min` AFTER the base ceiling switch). 10 Ticket B pin tests added to `swim-protocol-volumes.test.ts`. D-022 in `DECISIONS-LOG.md`.
- **Behavior:** `fitness === 'beginner' && distance ∈ {'70.3', 'full'}` athletes get per-session yardage capped at **2500yd aerobic** (`css_aerobic` / `race_specific_aerobic` / `technique_aerobic` / `endurance` / `kick_focused` / `pull_focused`) and **2000yd threshold** (`threshold` / `speed`). `easy` unchanged (existing `raceYd × 0.5` cap is already lower). Endurance OD window's 4600yd is gated for beginners — they never hit OD volume regardless of phase. Sprint/olympic/intermediate/advanced pass through unchanged. Phase-by-phase impact: 4-17% reduction for 70.3 beginner aerobic; 22-38% for full beginner aerobic.
- **Verification:** `deno test swim-protocol-volumes.test.ts` → 20/0 (10 prior + 10 new pin tests). `deno test generate-combined-plan/` → 204/0 (194 prior + 10 new). Deployed `generate-combined-plan`.
- **Footgun (don't re-litigate):** the cap is a `Math.min` OVERLAY — it cannot raise the ceiling, only lower it. Do NOT change `learnerSessionCap` to set the ceiling unconditionally; the band table is the canonical bmax source, the cap is a clamp on top. Endurance OD window is intentionally gated for beginners — do NOT re-elevate beginners into the OD path (protocol prescribes lower per-session yards for learners). **Plan #60 W6 documented athlete is NOT closed by this fix** — that athlete (high-CTL learner, `swim_experience='learning'`) resolves to `intermediate` per Q-006, not `beginner`. The Q-006 structural fix (separate `swim_fitness` tier override) is the proper closure for that population; the Ticket B cap closes athletes who DO resolve to beginner. Olympic distance excluded per documented scope (band 2600-2800, marginally over 2500) — extend if Olympic athlete complaints surface.

### `scaledWeeklyTSS` endurance-hours-based budget (2026-05-20 fix — D-021, Q-005 resolved)
- **Files/commits:** `2a9deab5` — `src/lib/session-frequency-defaults.ts` (`SessionFrequencyDefaults.endurance_hours` field exposed from existing `:284` local), `generate-combined-plan/week-builder.ts:677` (`scaledWeeklyTSS` consumes `endurance_hours ?? weekly_hours_available`), `generate-combined-plan/index.ts:481` (mirrored swap for `plan_contract_v1.weekly_tss_target`). 3 Q-005 pin tests added to `_shared/session-frequency-defaults.test.ts`. D-021 in `DECISIONS-LOG.md`.
- **Behavior:** TSS budget now scales on endurance hours (post-§2.1 strength-load deduction) instead of declared hours. Hybrid 11hr/7d × 2× perf-strength athlete: budgets on 9.5hr endurance → `hourFactor` 1.1 → 0.95 → ~15-20% TSS budget reduction at tier boundary (build week: ~700-800 → ~550-650). Plan #60 W6 predicted to land ~11h vs prior 11h55m. Endurance-only athletes: zero change (`endurance_hours === hours_per_week`). Production plans frozen; behavior is opt-in via next regenerate.
- **Verification:** `deno test _shared/session-frequency-defaults.test.ts` → 71/0 (68 prior + 3 new). `deno test generate-combined-plan/` → 194/0 (no collateral; no fixture asserts on concrete TSS values). `deno test _shared/` → 369/0. Deployed `generate-combined-plan` (`create-goal-and-materialize-plan` picks up via HTTP payload).
- **Footgun (don't re-litigate):** do NOT conflate `endurance_hours` with `hours_per_week`. Declared hours stay the user-facing wizard value + canonical workout-time budget; endurance hours are the TSS-budgeting axis ONLY. The §2.1 deduction at `session-frequency-defaults.ts:284` is the SINGLE SOURCE OF TRUTH — do not duplicate the formula. Any new TSS-scaled budget must use `endurance_hours` with the same `??` fallback for the reconciler short-circuit path (no-`long_run_day` case). See D-021, Q-005 (resolved).

### `inferTrainingFitnessLevel` swims90 null-arc penalty (2026-05-20 fix)
- **Files/commits:** fix `3b228dc8` (`_shared/infer-training-fitness.ts:157-165` — both `>= 14` and `<= 1` swims90 branches gated inside `if (swim != null)` with a `Number.isFinite` guard; null arc = "unknown", not "zero sessions"). Originally filed as Known Broken in `7751541d` (2026-05-19, surfaced during swim Phase 1 `c1c94cec` gate sweep — pre-existing, confirmed via stash-out).
- **Behavior:** athletes with `arc.swim_training_from_workouts === null` (wizard-only signups, mid-onboarding) no longer get a spurious -1 score penalty from the prior `?? 0` coalesce. The penalty correctly fires only when the arc row exists AND records ≤1 observed sessions. High-CTL athletes (≥58) with no swim arc now resolve to `'advanced'` instead of `'intermediate'`. Downstream: `create-goal-and-materialize-plan/index.ts:1506-1543` CTL seed (40→65 when `recentLoads.length === 0`), weekly-hours fallback (10→14 when wizard didn't supply), `athlete_state.training_fitness` payload → `week-builder.ts:1095` swim band selection (intermediate→advanced templates).
- **Verification:** `deno test supabase/functions/_shared/` → **366/0** (was 365/1 with the documented Known Broken red; +2 new pin-tests in `infer-training-fitness.test.ts` lock the null-vs-observed-zero distinction). Deployed `create-goal-and-materialize-plan` (single direct importer per `_shared/infer-training-fitness.ts` consumer audit).
- **Footgun (don't re-litigate):** do NOT re-collapse the null-vs-observed-zero distinction. The two new pin-tests ("null arc → no spurious -1 penalty" and "observed swim_training_from_workouts with zero sessions DOES penalize") are the regression lock — future refactors must preserve both. The low-CTL→beginner test fixture (`:62`) was tightened CTL 18→15 because the prior CTL was depending on the bogus penalty to compose `ctl_le_22` (-1) + `swims90` (-1) = -2; the new fixture hits `ctl_le_16` (-2) directly. Production athletes shift `intermediate → advanced` on next regenerate (organic, no in-place rewrite).

---

## Known broken (filed, not blocking)

Behaviors that are demonstrably wrong but intentionally deferred. Don't propose fixes unless you have new information — the deferral was a scoping call, and the list below documents the cost so the next implementer can pick up cleanly.

### `limiter_sport` intensity-side handling not implemented
- **Symptom:** spec at `docs/SESSION-FREQUENCY-DEFAULTS.md §4` says "Run limiter is handled through intensity, not frequency. Adding run sessions increases injury risk disproportionately. The engine addresses a run limiter by making existing run sessions more productive (longer long run, higher-quality intervals, strides on easy days) rather than adding a 4th session." Implementation today: frequency side is correctly a no-op for run limiter; intensity side has zero implementation. The +7% TSS allocation bump in `science.ts:268-278 getBaseDistribution()` is a percentage shift across all phases, not a per-session intensity boost.
- **Files (where wiring would land):** `supabase/functions/generate-combined-plan/science.ts` (extend `brickRunTargetMiles()` and `longRunFloorMiles()` to accept `limiterSport` — note: the cycling mirror is `longRideFloorHours`; do not confuse), `session-factory.ts` (interval modulation), `week-builder.ts` (stride logic).
- **Predicted effect:** ~+65-70 TSS/week for run-limiter athlete (long run +15-20%, quality run +1 tempo interval, strides on 1-2 easy runs).
- **Why deferred:** multi-file medium-risk change; needs an architectural decision on whether the +7% TSS allocation stays additive with the new intensity dial or gets replaced by it. Documented in `docs/TICKET-B-WIRING-AUDIT.md` Field 2.

### "Run — Tempo" vs "Run Intervals 4×1000m" label divergence
- **Symptom:** Same workout renders with two different titles across surfaces. Today's Efforts uses the workout's stored `name` directly. `AllPlansInterface.tsx:881-885` and `PlannedWorkoutSummary.tsx:34-66` both use regex against `description`/`tags` but with slightly different heuristics. Compounded by Monday-May-18 swim title case ("Swim — Drills" vs "Race-Specific Aerobic Swim") which suggests the surfaces also read different upstream data shapes.
- **Files:** `src/components/PlannedWorkoutSummary.tsx:34-66`, `src/components/AllPlansInterface.tsx:881-885`, `src/components/TodaysEffort.tsx` (uses `workout.name` directly).
- **Fix shape:** consolidate the title-derivation into a single shared utility — same architectural pattern as the `useStrengthOrderingPreference` consolidation. Solving at the data layer (one canonical session name per workout, derived once at materialize time) is cleaner than patching label-by-label downstream.
- **Why deferred:** predates the universal fixes; cosmetic, not protocol-violating; queued behind higher-signal work.

### Cycling TREND dashed HR line never draws (historical `avg_hr` null)
- **Symptom:** cycling TREND shows the power line + current-ride "· {bpm}" label but no dashed HR line.
- **File:** `analyze-cycling-workout/index.ts:~2108` reads `r.computed.overall.avg_hr` (frequently null); SELECT at `:2077` omits the reliable `workouts.avg_heart_rate` column. → all historical TREND points `avg_hr: null` → `SessionNarrative.tsx` `TrendSparkline` `hasHr (≥3)` gate fails.
- **Fix shape:** add `avg_heart_rate` to the SELECT; resolve `computed.overall.avg_hr ?? workout_analysis.fact_packet_v1.facts.avg_hr ?? r.avg_heart_rate`. Same projection/field-source footgun class as `cead4e9e`/`41d1582d`/`f9efb893`.
- **RESOLVED (2026-05-17, `4177c05c`):** added `avg_heart_rate` to the loop SELECT; `hrH` resolves `computed.overall.avg_hr ?? fact_packet_v1.facts.avg_hr ?? r.avg_heart_rate` (each candidate guarded). Wide backfill verified 26/26 rides-with-a-trend now have ≥3 HR points → dashed line draws. Kept here as the was-broken record (Known-broken doubles as the fix log, per the pwr20 precedent). Q-007 closed.

### Type-filtered `pwr20_trend_v1` won't populate from a single recompute
- **Symptom:** `pwr20_trend_v1` null on a reclassified ride despite `computed.power_curve['20min']` existing.
- **Cause:** the series filters historical rides by their **stored** `classified_type`; post-VI-gate, a single recompute re-derives only the current ride — historical rides keep stale stored types until re-analyzed. Not a code defect.
- **Fix shape:** historical re-analysis backfill across recent rides. See Q-008 / SESSION-CONTEXT open item #2.
- **RESOLVED (2026-05-17):** one-off script, run wide. `scripts/verify-cycling-vi-if-fix.mjs --all` (`fae293e7` + `--all` `83d07fdb`) replays the full recompute chain via service role. Wide run: 180 d / 30 rides, 0 failed, 26/26 cap-present consistent; 16 historical `null → type`; post-backfill every ride in-window has a stored type and recovery/threshold/climbing/endurance/tempo each ≥3 (pwr20-eligible). No longer broken; kept here as the data-caveat record (a fresh single recompute still only re-derives one ride — re-run `--all` after future classifier-input changes). See Q-008.

### #8 race-course segment matching — blocked on GPX dependency
- **Symptom:** no race-course-relevant tagging on segment history.
- **Cause:** needs course-segment geometry from race-course GPX (Data-Dependency ❌); not in the #6 unblock decisions. Forward hook `cycling_segment_history.race_course_relevant` is in place.
- **Why deferred:** documented blocker; product decision owed (GPS-track matcher vs Strava-only). See Q-009 / `docs/CYCLING-ANALYSIS-DESIGN.md`.

---

## Questioned (worth verifying)

Believed-working but never explicitly verified. Listed here so the next session can pick up the verification cheaply, not so anyone re-implements.

_All previously-listed entries verified 2026-05-20 and moved to Solid (Q-003 §6.1 scoping; Q-004 Full IM §3.7). Section currently empty — append here when a future session ends with an unverified claim._

---

## When to update this doc

Append to **Solid** when a fix ships and is verified.
Append to **Known broken** when a bug surfaces and is intentionally deferred.
Append to **Questioned** when a session ends with an unverified claim.
Move items between sections as their state changes — promotion (Questioned → Solid) requires a verification method documented inline.
