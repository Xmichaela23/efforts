# Plan engine — stack-ranked fix backlog

**Inputs.** This backlog synthesizes `docs/plan-engine-contract-audit.md`, `docs/plan-seasons-e2e-audit.md`, and **Plan Generation Contract v1 (May 2026)** (`docs/PLAN-CONTRACT.md` — **check in if missing**).

**Rules for execution.** Each row is ordered by **severity × leverage**: restore athlete trust first, then architectural integrity, then completeness/polish.

**Acceptance tests.** Prefer **Deno** tests colocated with functions (`supabase/functions/**`), consistent with `week-optimizer.anchor-contract.test.ts` and `generate-combined-plan/scheduler-anchor.contract.test.ts`. Where noted **“(new)”**, add the described suite; extend existing files where cheaper.

---

## Priority stack

| Rank | Item | Contract | Files to change | Acceptance test |
|:----:|------|----------|-----------------|-----------------|
| **1** | **Optimizer `strength_frequency` must honor `training_prefs`, not `strength[]?.length \|\| 2`** | **§2.3** (intent); **§4.2** (anchors/preferences); **§1.1** (athlete authority eroded when silent wrong frequency) | `supabase/functions/create-goal-and-materialize-plan/index.ts` (`backfillTriTrainingPrefsDefenseInDepth`: derive `strength_frequency` from `training_prefs.strength_frequency` with safe clamp; only fall back to array length when scalar absent); callers/tests that assume old behavior | **(new)** `supabase/functions/create-goal-and-materialize-plan/backfill-strength-frequency.contract.test.ts`: fixtures — `strength_frequency: 0` + **no** `preferred_days.strength` → `WeekOptimizerInputs.preferences.strength_frequency === 0`; optional second assertion via exported helper or snapshot of merged `preferred_days` after backfill that no strength slots appear when 0× |
| **2** | **Persist `assessment_week_preference` on goals (`training_prefs`) so `generate-combined-plan` can prepend assessment week** | **§3.1** (beginner assessment path); **§11.3** (`athlete_state.assessment_week_preference`); **§1.3** (honesty — chosen path not dropped) | `src/components/ArcSetupWizard.tsx` (`assemblePayload`: merge top-level assessment into A-race `training_prefs`); `src/lib/arc-setup-persistence.ts` if normalization strips unknown keys; verify `create-goal-and-materialize-plan` already reads prefs (no regression) | **(new)** Deno or Vitest: serialized wizard payload / goal row shape contains `training_prefs.assessment_week_preference === 'assessment_first'` → **`generate-combined-plan/index.ts`** path sets `sessions_by_week['1']` assessment tags (`buildAssessmentWeekSessions` contract): assert week **1** sessions include `tags` containing `assessment` for tri fixture OR unit-test payload mapping without full HTTP |
| **3** | **Eliminate silent “combined failed → standalone tri” when athlete/session intended combined** | **§1.3** (honesty); **§12** (correctness — implementation matches claimed engine) | `supabase/functions/create-goal-and-materialize-plan/index.ts` (post–`buildCombinedPlan`: if `combine === true` and non-preview and result `null`, return **actionable error** or enqueue retry — **do not** fall through to `generate-triathlon-plan` without explicit product consent); possibly `src/lib/arc-setup-persistence.ts` (`combine` semantics doc-only unless UX needs sibling visibility delay handling) | **(new)** Integration-style Deno test with mocked DB/function responses **or** contract test on pure routing helper: `combine && !preview && combinedResult===null` ⇒ response **not** `{ success, plan_id }` from standalone tri path; document expected HTTP body |
| **4** | **Surfacing optimizer conflicts / material trade-offs before final calendar commit (wizard path)** | **§1.3**; **§8** (hard-no communication); **Appendix A.3** (no silent downgrade to 1× strength without resolution narrative) | `src/hooks/useArcSetupComplete.ts` (branch on preview/conflicts); `supabase/functions/create-goal-and-materialize-plan/index.ts` (expose conflicts in JSON when `planPreview`/dry-run); client UI to capture **§8.5**-style resolution where needed | **(new)** E2E or API contract test: payload that triggers `deriveOptimalWeek.conflicts.length > 0` or `used_co_equal_1x_fallback` ⇒ client receives **blocking** payload **or** persisted `generation_trade_offs` includes template id **before** `activate-plan`; fixture aligned with **§10.4** intent (conflict detected — plan blocked or choices required) |
| **5** | **`swims_per_week` derived from `swim_intent` / prefs when `swim[]` missing — avoid blind `\|\| 2`** | **§2.3**; **§4** (swim anchors) | `supabase/functions/create-goal-and-materialize-plan/index.ts` (same backfill function: `swims_per_week` from explicit prefs field or `swim_intent === 'focus' ? 3 : 2` per contract vocabulary, then clamp 0–3) | **(new)** `backfill-swims.contract.test.ts`: missing `preferred_days.swim` + `swim_intent: 'race'` ⇒ optimizer gets **2**; `'focus'` ⇒ **3** |
| **6** | **Either emit full `preferred_days` from wizard OR formally treat sparse prefs as first-class (skip full-sheet validation)** | **§1.1**; **§4**; **§10.1** (Wed quality bike pinned when declared) | `src/components/ArcSetupWizard.tsx` (`assemblePayload` / `inferDays`: persist inferred `quality_bike`, `easy_bike`, `quality_run`, `easy_run` **or** change `hasFullPreferred` in `create-goal-and-materialize-plan/index.ts` to match wizard contract); `mergeCombinedSchedulePrefs` only if new fields need parsing | Extend **`generate-combined-plan/scheduler-anchor.contract.test.ts`** **or** **§10** suite: **§10.1** scenario — Wednesday **`session_kind === 'quality_bike'`** when group ride Wed competitive; Tuesday **no** `quality_bike`; Thursday **`lower_body`** strength present (**tags** / **`session_kind`**) |
| **7** | **Pass Arc/learned baselines into combined `athlete_state` and use in prescription surfaces** | **§2.2**; **§11.1** (`zone_targets`); **Arc rule** — single athlete truth | `supabase/functions/create-goal-and-materialize-plan/index.ts` (`buildCombinedPlan`: map `getArcContext` / `user_baselines` → `bike_ftp`, `run_threshold_pace`, `swim_threshold_pace`); `supabase/functions/generate-combined-plan/session-factory.ts` (and/or `week-builder.ts`) consume fields for zone copy — minimal viable: bike/run thresholds reflected in `zone_targets` or descriptions where templates allow | **(new)** Deno: `buildWeek` or session factory with **non-null** `AthleteState.bike_ftp` ⇒ at least one bike planned session string/`zone_targets` reflects FTP-derived labeling (snapshot or substring); **Appendix A.4** partial baseline case documented in test name |
| **8** | **Mesocycle / phase continuity for strength — remove perpetual `start_week: 1` reset** | **§9.4**; **§9.5** | `supabase/functions/generate-combined-plan/session-factory.ts` (`toStrengthPhase`: derive `weeks_in_phase` / progression index from **plan week index** and phase timeline, not constants) | **(new)** progression contract test: two **build** weeks **W_a < W_b** same phase block ⇒ representative **`percent_1rm`** or load metadata on performance strength sessions satisfies **non-decrease** **§9.5** (exclude recovery weeks); add **§10.1** check **week 5 ≥ week 3** loads post-recovery when fixtures stabilize phase indices |
| **9** | **Single scheduling authority — sequential rules owned once, `week-builder` consumes optimizer output** | **§5–6**; **§12** (implementation satisfies contract, not duplicate matrices); duplication called out in audits | `supabase/functions/_shared/week-optimizer.ts` (export shared sequential helpers if needed); `supabase/functions/generate-combined-plan/week-builder.ts` (delete duplicated branching where safe; import shared module); `supabase/functions/_shared/schedule-session-constraints.ts` if matrix footnotes centralize | **Extend** `week-optimizer.anchor-contract.test.ts` **and** add **parity test**: same input prefs → `preferred_days` graph vs **`buildWeek`** placement **`session_kind`** per weekday for canonical fixture — diff must be empty for declared scenarios |
| **10** | **Standalone `tri-generator.ts` vs `generate-combined-plan` parity for wizard-origin tri** | **§1.1**; **§4**; **§12** | Either route single-race tri through **`generate-combined-plan`** (`create-goal-and-materialize-plan/index.ts`, `buildCompleteContext` / `combine` rules) **or** align **`generate-triathlon-plan/generators/tri-generator.ts`** inputs with post-backfill `preferred_days` + same `AthleteState` shape | **(new)** golden-week comparison test (behind feature flag if needed): **same** `training_prefs` JSON ⇒ within tolerance same **hard anchors** (`quality_bike` day, `long_ride` day) on canonical week |
| **11** | **Enumerate **§8** message templates + structured resolution ids (esp. **§8.5**) — no silent `co_equal` downgrade without athlete path** | **§8**; **§1.3**; **Appendix A.3** | `supabase/functions/_shared/plan-generation-trade-offs.ts`; UI resolver (`src/components` / Arc surfaces — wherever trade-offs render); `create-goal-and-materialize-plan/index.ts` (gate persistence) | Assertion test on **`PlanGenerationTradeOff`**: presence of `message_template_id` for **§8.5** scenario; **§10.4** automated expectation — trade-off emitted with suggested actions **before** full plan activation |
| **12** | **Run-only / non-traditional schedule — remove fixed Thursday easy-run assumption where prefs declare otherwise** | **§4.4** | `supabase/functions/generate-combined-plan/week-builder.ts` (run-only branch ~Thu easy run) | **`§10.3`** acceptance fixture: long run **Tuesday**, quality **Friday** ⇒ **Sunday** not long run; sessions align with indices from prefs |
| **13** | **Centralize athlete-level recovery cadence (beginner / returning) vs scattered phase logic** | **§3.1**; **§3.2**; **§7.2** | `supabase/functions/generate-combined-plan/phase-structure.ts`, `applyLoadingPattern` (locate and consolidate); possibly `types.ts` | **(new)** table-driven test: `first_race` + beginner flags ⇒ recovery interval matches contract wording for first 8 weeks (assert phase labels / `isRecovery` pattern sequence) |
| **14** | **`§11` output completeness — `plan_contract_v1` + serialized sessions schema** | **§11.1–11.2** | `supabase/functions/generate-combined-plan/index.ts`; downstream readers if fields missing | **(new)** schema/assert snapshot test on one generated plan JSON: required keys **`duration_weeks`**, **`phase_by_week`** (if emitted), per-session **`session_kind`**, **`tags`**, **`timing`** when consolidated |
| **15** | **Full **§10** acceptance suite (four narratives) in one discoverable file** | **§10** | Touches many files from rows above; test file is primary deliverable | **`supabase/functions/generate-combined-plan/plan-contract.acceptance.test.ts` (new)** implementing **§10.1–10.4** — **§10.4** may assert API/UI gate (“blocked pending resolution”) until template plumbing lands |

---

## Dependency notes

- **Rows 1–5** are **localized** and unblock trust quickly; they do not require full refactor.
- **Rows 6–10** are **coupled**: sparse prefs (6), baseline plumbing (7), strength progression (8), dedupe (9), and routing (10) should be sequenced after **1–2** so tests aren’t flaky on wrong inputs.
- **Row 15** is the **umbrella** acceptance artifact; early rows should **migrate** their tests into it over time to avoid duplicate fixtures.

---

## Contract traceability (quick map)

| Theme | Sections |
|-------|-----------|
| Athlete schedule authority & anchors | **§1.1**, **§4**, **§10.1**, **§10.3** |
| Honesty & hard-no surfacing | **§1.3**, **§8**, **§10.4**, Appendix **A.3** |
| Fitness / baselines | **§2.2**, **§11.1**, Appendix **A.4** |
| Levels & modifiers | **§3**, **§7** |
| Matrix & sequencing | **§5**, **§6** |
| Progression | **§9**, **§10.1** (week 5 ≥ week 3) |
| Output shape | **§11** |
| Meta / versioning | **§12–13** |

---

*Document only — no production code changes in this commit.*
