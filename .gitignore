# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env

# Local Netlify folder
.netlify

# Throwaway local verification/debug scripts (D-NNN verify, dbg, etc.)
scripts/_*

# Screenshot tool — saved login session (holds an auth token; never commit)
scripts/screens/.screenshot-auth.json
# Captured app screenshots (personal data) — keep local unless you choose to share
docs/screens/

# Sweep output — regenerate with `deno run --allow-read --allow-write scripts/dump-plans.ts`.
# The SCRIPT is the artefact; 61 built plans are not source.
test-outputs/
