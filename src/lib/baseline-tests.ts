/**
 * THE BASELINE TEST SESSIONS, AS ROWS — the bodies live in `supabase/functions/_shared/baseline-test-rows.ts`
 * (moved 2026-09-10, audit H-W03), because the server now inserts the "Know your numbers?" week-one
 * tests with the plan. Training Baselines and Adjust still build their standalone test rows from here.
 */
export * from '../../supabase/functions/_shared/baseline-test-rows.ts';
